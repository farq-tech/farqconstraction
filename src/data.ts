/**
 * Mock RFQ/BOQ fixtures removed for production delivery.
 * Live data:
 * - Suppliers → src/api/constructionSuppliers.ts → GET /api/construction/catalog
 * - RFQs / offers / comparison / award → src/api/constructionClient.ts
 * - Upload draft BOQ (session only until createRfq) → src/store/session.ts
 */

export {}
