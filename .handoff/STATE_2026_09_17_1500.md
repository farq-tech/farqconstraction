# الحالة — 2026-09-17 15:00 — كل الوكلاء متوقفون، بانٍ واحد

## ما هو جاهز، وأين

| | الفرع | الرأس | مدفوع | منشور |
|---|---|---|---|---|
| API | `release/cpo-v10-clean` (من `origin/main` = `a666cebb3`) | `e9630b282` | نعم | **لا** |
| الواجهة | `feat/ai-named-suggestion` (من `release/standalone-construction-parser`) | `71e6a3e` | نعم | **لا** |

### فرع الـAPI — ثلاثة التزامات فوق main
1. `11d748d5c` ملفات v10 والخريطة كملفات **جديدة فقط** (وكيل الإيداع، قبل إيقافه).
2. `9d420dfdb` الجدول الزمني المشتق من `dispatch_attempts`.
3. `e9630b282` الغلافان حول `matchBoqCatalog`: اقتراح الخريطة ثم المسار الذكي.

- خارج مسارات البناء الخمسة: **فراغ تام** (`git diff --stat origin/main...HEAD` بالاستثناءات).
- ملفان فقط من main معدَّلان، وكلاهما بإضافات: `supabase-repository.js` (الجدول الزمني + طريقتا قراءة) و`runtime.js` (سلسلة الأغلفة). **`boq-directory-matching.js` و`matchSuppliers` لم يُمسّا.**
- **لا هجرة جديدة.** الجداول الأربعة التي يقرؤها الكود الجديد معرَّفة في هجرات main.
- الاختبارات: main 695 / 15 فاشلًا؛ الفرع 835 / 17. الفاشلان الزائدان في `construction-intent-supplier-map-attribute.test.js`، ملف غير موجود على main، ويفشلان بنفس الشكل على فرعهما الأصلي. يختبران قاعدة «ذكر المادة في جملة وصفية ليس ادّعاء توريد» في **بناء** الخريطة لا في قراءتها — وهذا قد يفسّر بعض الموردين العامّين في الخريطة، ويستحق عملًا مستقلًا.
- **الأعلام الثلاثة الجديدة غير مضبوطة في الإنتاج،** فالدمج لا يغيّر أي استجابة عدا الجدول الزمني، حتى يضبطها المالك:
  - `CONSTRUCTION_BOQ_MATCH_MAP_SUGGESTIONS=1` + `CONSTRUCTION_INTENT_SUPPLIER_MAP_ENABLED=1`
  - `CONSTRUCTION_PROCUREMENT_INTENT_AI=1` (و`CONSTRUCTION_AI_ENABLED` مضبوط أصلًا، قيمته غير مقروءة)

### فرع الواجهة — أربعة التزامات فوق الفرع المنشور
إرسال تسمية v10 مع صفوف `/boq/match` · عرض اقتراح الخريطة والاقتراح الآلي بمكوّن واحد · التسميات العربية من بيانات الأنطولوجيا · رسالة صادقة للصفحة القديمة بعد نشر جديد. `tsc` صفر أخطاء.
الحقل المرسَل غير ضار على خادم لا يعرفه، فيمكن نشر الواجهة قبل الـAPI.

## ما قيس (بلا نشر)
الكراسة المرجعية 14:06: من الـ29 «مادة غير محدّدة»، **16 تُسمّى وكلها لها موردون في خريطة الإنتاج**؛ البنود التي تعرض موردين 30 → 46 من 68. التفاصيل في `REFERENCE_68_THROUGH_THE_NEW_CHAIN_2026_09_17.md`. **العدد ليس الملاءمة.**

## حقائق تشغيلية ثبتت اليوم
- `api.farq.sa` = خدمة `farq-api-test-oregon`، تبني من `farq-tech/farq` فرع `main` بجذر `/api` ومراقبة `/api/**`. **الدمج في main هو النشر.**
- الـDockerfile الذي يبني الإنتاج هو `api/Dockerfile`. الـDockerfile الجذري معطوب (ينسخ من `tools/` غير المتبوع) لكنه لا يبني الإنتاج. لا يُرفع شيء من جذر المستودع.
- `farq-api-canary` مسبار للقراءة فقط يرفض كل مسارات المنتج؛ هي الآن منزَّلة بأمر المالك، ومفاتيحها عادت إلى 57 كما كانت. مشروع `canary-land` فارغ (لا أسرار، لا نطاق) وينتظر حذف المالك.
- قاعدة Railway: لا أمر يغيّر حالة قبل طبع `railway status` وذكر المشروع والخدمة والبيئة.
- مشروع Supabase «farq-main» ليس الإنتاج.

## القرار الوحيد المتبقي، وهو للمالك
رؤية الرحلة قبل الدمج: خدمة Railway جديدة بجذر `/api` من `release/cpo-v10-clean`، بالحدّ الأدنى من المتغيّرات (رابط قاعدة البناء، علم القراءة، سرّ التوقيع، CORS، والأعلام الجديدة)، **وكل أعلام الكتابة والإرسال وواتساب وحراج وGmail والصندوق والعمّال على صفر**، ومعاينة Vercel من `feat/ai-named-suggestion` موجّهة إليها. الرابط المستقل الحالي لا يُوجَّه إليها.
