# فيكسچر مرجعي ثانٍ — كراسة مركز البيانات — 2026-09-17

`fixtures/boq/datacenter-cyber-01.groundtruth.json` — 180 صفًّا، أكواد `DC-001`…`DC-180` بلا فجوات ولا تكرار.

## المصدر والطريقة (مستقلّة عن قارئ التطبيق)

- الملف: `fixtures/boq/datacenter-cyber-01.pdf` (802,266 بايت، 25 صفحة، 23 صفحة جداول، طبقة نصّ موجودة). نفس MD5 للملف في Downloads: `b24c220c…`.
- المستخرِج: PyMuPDF `page.get_text("words")` وتجميع بالإحداثيات على ستة أعمدة ثابتة (الرمز 730–800، الفئة 550–700، البند 420–550، المواصفة 285–420، الكمية 200–250، الوحدة 70–120). مرساة كل صف هي كود الثلاثة أرقام في عمود الرمز، وحدود الصف منتصف المسافة إلى الكود المجاور.
- `find_tables()` في PyMuPDF أعاد صفرًا على كل الصفحات؛ الجداول بلا حدود مرسومة.
- نُزع تذييل الصفحة («وثيقة اختبار آلي - FARQ-TEST-DC-180 …») من 53 صفًّا كان يلتصق بآخر صف في الصفحة.
- كل صف يحمل اسم بند وكمية رقمية ووحدة. الوحدات: جهاز 102، وحدة 56، قطعة 16، طقم 6.
- تحفّظ: ترتيب الكلمات داخل النصوص المختلطة (عربي/لاتيني) قد يختلف عن الترتيب البصري (مثال: `Generator Load Web`). المقارنة بالكلمات لا بالسلسلة.

## ما قيس على الرابط المنشور بهذا المرجع (13:01–13:02، من الواجهة)

- 180 من 180 بطاقة، زمن القراءة 12.5 ث من مهلة 180.
- الكميات: 180/180 مطابقة. الوحدات: 164/180 مطابقة؛ الـ16 المتبقية «قطعة» عُرضت «عدد» — تطبيع مقصود في `parseBoq.ts` (جدول المرادفات)، لا خطأ قراءة.
- الوصف: 25 صفًّا تحمل اسم البند كاملًا، 4 جزئيًّا، **151 صفًّا (84%) فقدت اسم البند**؛ 120 بطاقة تعرض عنوان الفئة مكان الاسم و150 تعرض شذرات من المواصفة.
- الحسم: 0 من 180. الموردون: 0. الملاحظة على الشاشة: «قُرئت البنود بالكامل، ولا يوجد لأي بند مورد مؤكد في سجل فرق.» — وهي نصّ نجاح على قراءة فاشلة.

## الاستخدام

حارس فقدان عمود الوصف يُقاس على هذا الملف وعلى `reference-etimad-groundtruth.json` معًا: يجب أن يُشعل هنا (≥84% فقد) ولا يُشعل على المرجعية (0 فقد).
