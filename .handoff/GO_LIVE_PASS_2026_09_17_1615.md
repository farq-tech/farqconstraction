# Go-live pass — 2026-09-17, 15:50–16:15

Owner order: open real sending, delete mock data, no demo build, no dead button. Success is counted in bugs found.

## Live now (source: Vercel project `farq-construction`, deployed 16:06 by CLI)

- `VITE_READ_ONLY` removed from Vercel production. Real emails now reach real suppliers.
- Frontend commits `67c87d2` and `eacd5c9` on `feat/ai-named-suggestion`, also pushed to `release/standalone-construction-parser`.
- 523 frontend tests pass, tsc 0 errors.

## Measured live after the deploy (signed-in browser, 16:08–16:13, موقع مكة المكرمة.pdf)

- Read and matched in 250.7 s. 1,039 lines. No guard fired.
- 358 cards with confirmed suppliers, 125 from the map, 44 AI-named, 467 unidentified. 527 of 1,039 show suppliers.
- The contradictory footer note appears only under the 467 unidentified cards.
- Signed out: the sign-in form, no console errors, no failed requests. Settings save persists and inputs keep focus.
- Headline changed to «سعّر مناقصتك بدقائق», confirmed in the live bundle `index-CvNVuDWu.js`.

## Prepared, NOT deployed (needs the owner's yes: merging to main deploys api.farq.sa)

- Branch `fix/supplier-scope-and-spec-in-email` (`c6cadd6b7`) in `farq-tech/farq`:
  a supplier outside every package gets no lines instead of all lines, and the invitation email prints the line's specification. 13 tests pass.
- Branch `fix/ai-miss-gate-client-verdict` (`b64cc3f85`), from earlier.
- Branch `feat/boq-read-history`, uncommitted, booklet history per account.

## Not done, owner decision required

- Production DB holds 9 RFQs, all in the owner's account (read-only query, 16:09, source `CONSTRUCTION_DB_URL`).
  4 are unsent drafts named «مشروع تجريبي» / «x». 5 were really sent (97, 29, 1, and 61 across two) and 2 suppliers quoted.
  Nothing was deleted. Deleting the 4 drafts is safe; deleting the sent ones removes real supplier correspondence.
- `GET /suppliers/import-batches` returns 404 on production: the batch list and «تراجع» exist only on an unmerged branch.

## Bug ledger added in this pass (32 onward; 1–31 are in the 15:05 doc and chat)

32. Production bundle sent `x-construction-demo-user`; live CORS refused it, so every signed-out read was a network error. FIXED.
33. App opened on home with no session. FIXED: opens on sign-in.
34. «المتابعة بدون تسجيل دخول» shipped in production. FIXED.
35. `contact_name: 'عميل تجريبي'` on every real RFQ. FIXED.
36. Unreadable quantity sent as 1. FIXED: refused by name.
37. Missing unit sent as «عدد», pack as «قطعة». FIXED.
38. Unclassified tender sent as ELECTRICAL. FIXED: buyer chooses.
39. «لا تُرسل من هذه القراءة» was text only. FIXED: enforced in the send form.
40. Work-only lines could be sent. FIXED.
41. No recipient list before the first email. FIXED.
42. Stored 45-line «صالات الانتظار» table with invented quantities. DELETED.
43. Canned line names overwrote the booklet's names. DELETED.
44. Three hardcoded project names triggered by ordinary words. DELETED.
45. Supplier evidence graded by list position. FIXED.
46. Invented supplier category «مواد بناء» and city «المملكة العربية السعودية». FIXED.
47. Password change toasted success with no API call. REMOVED.
48. «حفظ التغييرات» saved nothing. FIXED: saves, and the sender uses it.
49. Settings inputs lost focus on every keystroke. FIXED.
50. ComparisonView was a constant «لا توجد عروض». REWRITTEN against the API.
51. الترسية had no entry point anywhere. FIXED via comparison.
52. Award stored «Client demo award…» as approval note. FIXED.
53. SendFailureView was a constant «لا إخفاقات». DELETED with SuccessView.
54. Home drop zone discarded the file. FIXED.
55. «إضافة مورد» form saved nothing. FIXED through the import endpoint.
56. «إدارة الصلاحيات» opened the access-denied screen. REMOVED.
57. Supplier portal asserted tax-inclusive prices and an accepted declaration. FIXED.
58. RFQ resend showed no outcome when the re-read failed, and dropped failure reasons. FIXED.
59. WhatsApp tabs opened in a loop and were counted when blocked. FIXED.
60. Server variable names shown to buyers in five error messages and the settings screen. FIXED.
61. Server: supplier in no package receives the whole RFQ. FIX ON BRANCH.
62. Server: invitation email omits the specification. FIX ON BRANCH.
63. Server: email subject is every package name joined, so 20 lines make a 20-name subject. OPEN.
64. Server: `unique (rfq_version_id, item_id)` — two booklet lines matched to one catalog item may collide. UNTESTED.
65. No idempotency key on RFQ create: a timeout after a server-side success can create a second RFQ. OPEN. Invite send itself is de-duplicated server-side.
66. No deep links: refresh always returns to home. OPEN.
67. The earlier "verification" journey at 15:38 never signed in (it clicked the wrong button), so it verified nothing. Script fixed.
68. macOS `._pack-*` sidecar files in `.git/objects/pack` made git print errors. Removed.
