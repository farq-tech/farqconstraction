# `audit_timeline` — لماذا كان فارغًا، وما الذي أُصلح — 2026-09-17

## السبب (من الكود ومن الـAPI الحيّ، 13:10)

- الجدول الزمني في `serializeRfq` (`api/lib/construction/supabase-repository.js`) يقرأ `procurement_audit_events` فقط لكيان `RFQ`/`AWARD`.
- مسار الإرسال **لا يكتب أي حدث** في هذا الجدول. الأحداث الوحيدة على مستوى الطلب هي `RFP_SUBMISSIONS_CLOSED` و`RFP_ENVELOPES_OPENED`.
- مقيس على `api.farq.sa` بجلسة المالك: 9 طلبات، كلها `audit_timeline: []`، بما فيها طلب بـ162 دعوة و97 إرسالًا ناجحًا.
- دليل الإرسال موجود أصلًا في `dispatch_attempts` (لكل دعوة ولكل قناة: `SENT` / `NOT_SENT` / `SKIPPED_*` مع `sent_at` و`failure_code`)، وتُحمَّل في الدالة نفسها.

**تحذير مقيس:** مشروع Supabase «farq-main» نسخة شبه فارغة (طلب واحد، حدث واحد). الإنتاج على `CONSTRUCTION_DB_URL` كما يشرح `db-target.js`. لا تُقاس أرقام البناء من Supabase.

## الإصلاح — اشتقاق عند القراءة، بلا كتابة

- وحدة جديدة `api/lib/construction/rfq-audit-timeline.js`: تدمج الأحداث المسجّلة مع أحداث مشتقة من المحاولات وطوابع الطلب.
- كل حدث يحمل `source`: `RECORDED` (كتبه النظام لحظتها) أو `DERIVED` (أُعيد بناؤه عند القراءة). **الإعادة لا تتنكّر في زيّ السجل.**
- الأحداث المشتقة: `RFQ_CREATED`، `RFQ_VERSION_CREATED` (للنسخة > 1)، `RFQ_DISPATCHED` / `RFQ_DISPATCH_FAILED` (دفعة لكل تجمّع محاولات ضمن 10 دقائق، مع عدّادات `sent`/`not_sent`/`skipped` لكل قناة و`failure_codes`)، توأما `AWARD_*`، وإغلاق/فتح المظاريف إن وُجد الطابع بلا حدث مسجّل.
- التغيير في `supabase-repository.js` ثلاثة أسطر: `require`، إضافة `a.attempted_at` لاستعلام المحاولات، واستبدال `audit_timeline: auditResult.rows` بالاشتقاق. **مقصود أن يكون صغيرًا** لأن الملف مأخوذ من مسار البوابة.
- 10 اختبارات وحدة ناجحة في `api/tests/construction-rfq-audit-timeline.test.js`. اختبار `construction-boq-rfq-readiness` يفشل 1 من 2 **على الأساس `44d8acc97` قبل التغيير أيضًا** — ليس من هذا التغيير.

## أين يعيش

- API: فرع `fix/rfq-audit-timeline-2026-09-17`، شجرة عمل `/Users/m4pro/farq-wt-audit-timeline`، أساسه `release/cpo-v10-api` (= `44d8acc97`). **غير مدفوع وغير منشور.** يُدمج مع مسار نشر v10.
- الواجهة: `RFQDetailView.tsx` يعرض تسمية عربية لكل حدث، وشارة «مسجّل»/«مشتق»، وسطر الأرقام تحت حدث الإرسال، ونصّ الفراغ صار «لا أحداث مسجّلة ولا محاولات إرسال لهذا الطلب» بدل ادّعاء «سجّل N دعوة». مودَع على `release/standalone-construction`.
- `tsc` يعطي خطأً واحدًا قبل التغيير وبعده (`apiUnreachableAdvice` غير مصدَّر من `apiBase.ts`) — سابق للتغيير.

## إعادة العرض على الطلبات التسعة الحيّة (بمحاولات الـAPI المُزالة التكرار، فالعدّادات أدنى من الحقيقية)

| الطلب | الحالة | الجدول الزمني المشتق |
|---|---|---|
| 51d17af6 | SENT | أُنشئ ← أُرسل (97 ناجحة) |
| d9e7fbd7 | SENT | أُنشئ ← أُرسل (30) |
| b998b3f1 | SENT | أُنشئ ← أُرسل (1) |
| 6f833385 | SENT | أُنشئ ← أُرسل (88) |
| 546e65ec | PARTIALLY_SENT | أُنشئ ← أُرسل (2 من 6 محاولات) |
| المسودّات الأربع | DRAFT_NOT_SENT | أُنشئ فقط — **لا حدث إرسال** |

على الخادم ستكون العدّادات كاملة لأن الاستعلام صار يحمل `attempted_at` للمحاولات المتخطّاة أيضًا.

## ما يعطيه هذا للبوابة B

بعد النشر، «لم يُرسل شيء» يُثبَت من الجدول الزمني نفسه: طلب تجريبي يجب أن يعرض `RFQ_CREATED` فقط وصفر أحداث `*_DISPATCH*`. وهذا الشاهد لم يكن موجودًا بالأمس.
