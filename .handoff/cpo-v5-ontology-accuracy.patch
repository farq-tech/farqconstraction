diff --git a/package.json b/package.json
--- a/package.json
+++ b/package.json
@@ -11,7 +11,9 @@
     "test": "vitest run",
     "test:watch": "vitest",
     "ontology:ids": "node scripts/ontology-ids.mjs",
-    "ontology:coverage": "node scripts/intent-coverage-report.mjs"
+    "ontology:coverage": "node scripts/intent-coverage-report.mjs",
+    "ontology:audit": "node scripts/tender-audit.mjs",
+    "ontology:language": "node scripts/ontology-language-audit.mjs"
   },
   "dependencies": {
     "pdfjs-dist": "^4.10.38",
diff --git a/src/lib/procurementOntology.data.json b/src/lib/procurementOntology.data.json
index 4e92ca6..0190402 100644
--- a/src/lib/procurementOntology.data.json
+++ b/src/lib/procurementOntology.data.json
@@ -1,187 +1,530 @@
 {
-  "version": "cpo-v2",
+  "version": "cpo-v5",
   "note": "Compositional Procurement Ontology. Four EXPLICIT levels: sector -> family -> category -> intent. Terms are three-tier (strong decides / weak+context decides / weak alone never decides) and inherit down the chain. Veto is declared at sector or family level and inherited; an intent only adds its own exceptions. FACETS carry all variation (dimensions, grades, capacities, model numbers) and each declares supplier_pool_affecting, which sibling caches key on. Never add an entry per product SKU. IDs in this file are NOT a hardcodable list: consumers must read them through listIntentIds()/listFamilyIds()/listOntologyIds() (or scripts/ontology-ids.mjs) and compare ontology_version, because intents split and any copied list rots silently. This file must stay byte-identical between farqconstraction/src/lib and farq/api/lib/construction.",
   "learning": { "promote_after_occurrences": 5 },
-
   "archetypes": {
-    "structural_steel_supplier": { "label_ar": "موردو الحديد الإنشائي", "patterns": ["حديد انشائي", "قطاعات حديد", "فولاذ", "structural steel", "steel sections", "مصنع حديد", "steel stockist", "stockist"] },
-    "metal_trading": { "label_ar": "تجارة المعادن", "patterns": ["تجاره معادن", "معادن", "metal trading", "metals", "steel trading", "steel distributor"] },
-    "rebar_supplier": { "label_ar": "موردو حديد التسليح", "patterns": ["حديد تسليح", "اسياخ حديد", "rebar", "reinforcement steel"] },
-    "ready_mix_supplier": { "label_ar": "الخرسانة الجاهزة", "patterns": ["خرسانه", "خرسانه جاهزه", "ready mix", "readymix", "concrete", "صبه جاهزه", "بلوك"] },
-    "cladding_supplier": { "label_ar": "موردو الكلادينج والتجليد", "patterns": ["كلادينج", "تجليد", "كسوه", "cladding", "الوكبوند", "acp panels", "تلبيس"] },
-    "fitout_contractor": { "label_ar": "مقاولو التشطيبات", "patterns": ["تشطيبات", "تشطيب", "fit out", "fitout", "ديكور", "ديكورات", "interior fit", "اعمال داخليه"] },
-    "joinery_supplier": { "label_ar": "النجارة والأعمال الخشبية", "patterns": ["نجاره", "اعمال خشبيه", "joinery", "carpentry", "اثاث خشبي", "خشب", "mdf"] },
-    "interior_finishing": { "label_ar": "التشطيبات الداخلية", "patterns": ["تشطيبات داخليه", "interior finishing", "جبس", "اسقف مستعاره", "قواطع"] },
-    "paint_only_supplier": { "label_ar": "موردو الدهانات فقط", "patterns": ["دهانات", "دهان", "طلاء", "بويات", "بويه", "paints", "paint", "coatings only"] },
-    "waterproofing_supplier": { "label_ar": "العزل", "patterns": ["عزل", "waterproofing", "ممبرين", "insulation", "عزل مايي", "عزل حراري"] },
-    "flooring_supplier": { "label_ar": "الأرضيات", "patterns": ["ارضيات", "بلاط", "سيراميك", "بورسلان", "flooring", "tiles", "رخام", "marble"] },
-    "general_building_materials": { "label_ar": "مواد البناء العامة", "patterns": ["مواد بناء", "building materials", "مقاولات عامه", "general contracting", "مواد انشاييه"] },
-    "general_trading": { "label_ar": "تجارة عامة", "patterns": ["تجاره عامه", "general trading", "مؤسسه تجاريه", "trading est", "trading company"] },
-    "warehouse_equipment_supplier": { "label_ar": "تجهيزات المستودعات", "patterns": ["رفوف", "مستودعات", "racking", "shelving", "storage systems", "warehouse", "تخزين"] },
-    "material_handling_supplier": { "label_ar": "معدات المناولة", "patterns": ["مناوله", "رافعات شوكيه", "forklift", "material handling", "معدات مناوله", "pallet truck"] },
-    "lifting_equipment_supplier": { "label_ar": "معدات الرفع", "patterns": ["معدات رفع", "اوناش", "crane", "lifting", "hoist", "سلاسل رفع", "rigging"] },
-    "weighing_supplier": { "label_ar": "الموازين", "patterns": ["ميزان", "موازين", "scales", "weighing", "weighbridge"] },
-    "pump_supplier": { "label_ar": "المضخات", "patterns": ["مضخات", "مضخه", "pumps", "pumping"] },
-    "valve_supplier": { "label_ar": "المحابس والصمامات", "patterns": ["محابس", "صمامات", "valves", "valve"] },
-    "pipe_fittings_supplier": { "label_ar": "المواسير والوصلات", "patterns": ["مواسير", "انابيب", "pipes", "fittings", "وصلات", "piping"] },
-    "water_treatment_supplier": { "label_ar": "معالجة المياه", "patterns": ["معالجه مياه", "تحليه", "water treatment", "فلاتر مياه", "reverse osmosis"] },
-    "hvac_supplier": { "label_ar": "التكييف والتهوية", "patterns": ["تكييف", "تهويه", "hvac", "تبريد", "air conditioning", "chiller", "ventilation"] },
-    "plumbing_only_supplier": { "label_ar": "السباكة فقط", "patterns": ["سباكه", "plumbing", "اعمال صحيه"] },
-    "sanitary_ware_supplier": { "label_ar": "الأدوات الصحية", "patterns": ["ادوات صحيه", "اطقم حمامات", "sanitary ware", "خلاطات", "مغاسل"] },
-    "fire_fighting_supplier": { "label_ar": "مكافحة الحريق", "patterns": ["مكافحه الحريق", "اطفاء", "fire fighting", "fire protection", "حريق", "طفايات", "sprinkler"] },
-    "fire_alarm_supplier": { "label_ar": "أنظمة إنذار الحريق", "patterns": ["انذار", "fire alarm", "كشف الحريق", "انظمه انذار", "detection systems"] },
-    "electrical_distributor": { "label_ar": "موزعو المواد الكهربائية", "patterns": ["مواد كهرباييه", "كهرباييه", "electrical", "كهرباء", "electric supplies", "electrical distribution"] },
-    "switchgear_supplier": { "label_ar": "لوحات التوزيع", "patterns": ["لوحات كهرباييه", "switchgear", "لوحات توزيع", "panel boards", "mdb", "mcc"] },
-    "cable_supplier": { "label_ar": "الكابلات", "patterns": ["كابلات", "كيابل", "cables", "اسلاك", "wires", "wiring"] },
-    "generator_supplier": { "label_ar": "المولدات", "patterns": ["مولدات", "generators", "genset", "مولدات ديزل", "diesel generator"] },
-    "ups_supplier": { "label_ar": "أنظمة الطاقة الاحتياطية", "patterns": ["ups", "انظمه طاقه", "بطاريات", "batteries", "power systems", "طاقه احتياطيه", "power protection"] },
-    "lighting_supplier": { "label_ar": "الإنارة", "patterns": ["اناره", "اضاءه", "lighting", "مصابيح", "led lighting", "luminaire"] },
-    "transformer_supplier": { "label_ar": "المحولات", "patterns": ["محولات", "transformers", "transformer"] },
-    "safety_equipment_supplier": { "label_ar": "معدات السلامة", "patterns": ["سلامه", "safety", "مهمات سلامه", "وقايه", "معدات سلامه", "safety equipment", "industrial safety"] },
-    "ppe_supplier": { "label_ar": "مهمات الوقاية الشخصية", "patterns": ["مهمات وقايه", "ppe", "personal protective", "ملابس عمل", "workwear", "احذيه سلامه", "قفازات"] },
-    "industrial_tools_supplier": { "label_ar": "العدد والأدوات الصناعية", "patterns": ["عدد وادوات", "ادوات", "tools", "hardware", "معدات صناعيه", "industrial supplies", "toolings"] },
-    "welding_supplier": { "label_ar": "اللحام", "patterns": ["لحام", "welding", "الكترودات", "electrodes", "welding supplies"] },
-    "gas_equipment_supplier": { "label_ar": "الغازات ومعداتها", "patterns": ["غازات", "gases", "اسطوانات", "cylinders", "اكسجين", "oxygen", "acetylene"] },
-    "cleaning_equipment_supplier": { "label_ar": "معدات النظافة", "patterns": ["نظافه", "cleaning", "تنظيف", "غسالات ضغط", "cleaning equipment"] },
-    "instrumentation_supplier": { "label_ar": "أجهزة القياس والمعايرة", "patterns": ["اجهزه قياس", "قياس", "instruments", "instrumentation", "معايره", "calibration", "اجهزه فحص", "test equipment"] },
-    "data_center_integrator": { "label_ar": "مكاملو مراكز البيانات", "patterns": ["مراكز البيانات", "data center", "datacenter", "حلول مراكز بيانات", "dc solutions"] },
-    "cybersecurity_integrator": { "label_ar": "مكاملو الأمن السيبراني", "patterns": ["امن سيبراني", "cybersecurity", "cyber security", "امن معلومات", "information security", "infosec"] },
-    "network_integrator": { "label_ar": "مكاملو الشبكات", "patterns": ["شبكات", "networks", "networking", "اتصالات", "telecom", "حلول شبكات", "تيار خفيف"] },
-    "it_hardware_supplier": { "label_ar": "موردو أجهزة الحاسب", "patterns": ["حاسب", "كمبيوتر", "computers", "تقنيه المعلومات", "information technology", "اجهزه حاسب", "خوادم", "servers", "storage", "it solutions"] },
-    "cctv_security_supplier": { "label_ar": "أنظمة المراقبة", "patterns": ["مراقبه", "كاميرات", "cctv", "surveillance", "انظمه امنيه", "security systems"] },
-    "access_control_supplier": { "label_ar": "أنظمة التحكم بالدخول", "patterns": ["تحكم بالدخول", "access control", "بوابات", "انظمه دخول", "turnstile"] },
-    "av_supplier": { "label_ar": "الصوتيات والمرئيات", "patterns": ["صوتيات", "audio visual", "شاشات عرض", "display systems"] },
-    "printing_supplier": { "label_ar": "الطباعة والترقيم", "patterns": ["طباعه", "printing", "ملصقات", "labels", "barcode", "ترقيم"] }
+    "structural_steel_supplier": {
+      "label_ar": "موردو الحديد الإنشائي",
+      "patterns": ["حديد انشائي", "قطاعات حديد", "فولاذ", "structural steel", "steel sections", "مصنع حديد", "steel stockist", "stockist"]
+    },
+    "metal_trading": {
+      "label_ar": "تجارة المعادن",
+      "patterns": ["تجاره معادن", "معادن", "metal trading", "metals", "steel trading", "steel distributor"]
+    },
+    "rebar_supplier": {
+      "label_ar": "موردو حديد التسليح",
+      "patterns": ["حديد تسليح", "اسياخ حديد", "rebar", "reinforcement steel"]
+    },
+    "ready_mix_supplier": {
+      "label_ar": "الخرسانة الجاهزة",
+      "patterns": ["خرسانه", "خرسانه جاهزه", "ready mix", "readymix", "concrete", "صبه جاهزه", "بلوك"]
+    },
+    "cladding_supplier": {
+      "label_ar": "موردو الكلادينج والتجليد",
+      "patterns": ["كلادينج", "تجليد", "كسوه", "cladding", "الوكبوند", "acp panels", "تلبيس"]
+    },
+    "fitout_contractor": {
+      "label_ar": "مقاولو التشطيبات",
+      "patterns": ["تشطيبات", "تشطيب", "fit out", "fitout", "ديكور", "ديكورات", "interior fit", "اعمال داخليه"]
+    },
+    "joinery_supplier": {
+      "label_ar": "النجارة والأعمال الخشبية",
+      "patterns": ["نجاره", "اعمال خشبيه", "joinery", "carpentry", "اثاث خشبي", "خشب", "mdf"]
+    },
+    "interior_finishing": {
+      "label_ar": "التشطيبات الداخلية",
+      "patterns": ["تشطيبات داخليه", "interior finishing", "جبس", "اسقف مستعاره", "قواطع"]
+    },
+    "paint_only_supplier": {
+      "label_ar": "موردو الدهانات فقط",
+      "patterns": ["دهانات", "دهان", "طلاء", "بويات", "بويه", "paints", "paint", "coatings only"]
+    },
+    "waterproofing_supplier": {
+      "label_ar": "العزل",
+      "patterns": ["عزل", "waterproofing", "ممبرين", "insulation", "عزل مايي", "عزل حراري"]
+    },
+    "flooring_supplier": {
+      "label_ar": "الأرضيات",
+      "patterns": ["ارضيات", "بلاط", "سيراميك", "بورسلان", "flooring", "tiles", "رخام", "marble"]
+    },
+    "general_building_materials": {
+      "label_ar": "مواد البناء العامة",
+      "patterns": ["مواد بناء", "building materials", "مقاولات عامه", "general contracting", "مواد انشاييه"]
+    },
+    "general_trading": {
+      "label_ar": "تجارة عامة",
+      "patterns": ["تجاره عامه", "general trading", "مؤسسه تجاريه", "trading est", "trading company"]
+    },
+    "warehouse_equipment_supplier": {
+      "label_ar": "تجهيزات المستودعات",
+      "patterns": ["رفوف", "مستودعات", "racking", "shelving", "storage systems", "warehouse", "تخزين"]
+    },
+    "material_handling_supplier": {
+      "label_ar": "معدات المناولة",
+      "patterns": ["مناوله", "رافعات شوكيه", "forklift", "material handling", "معدات مناوله", "pallet truck"]
+    },
+    "lifting_equipment_supplier": {
+      "label_ar": "معدات الرفع",
+      "patterns": ["معدات رفع", "اوناش", "crane", "lifting", "hoist", "سلاسل رفع", "rigging"]
+    },
+    "weighing_supplier": {
+      "label_ar": "الموازين",
+      "patterns": ["ميزان", "موازين", "scales", "weighing", "weighbridge"]
+    },
+    "pump_supplier": {
+      "label_ar": "المضخات",
+      "patterns": ["مضخات", "مضخه", "pumps", "pumping"]
+    },
+    "valve_supplier": {
+      "label_ar": "المحابس والصمامات",
+      "patterns": ["محابس", "صمامات", "valves", "valve"]
+    },
+    "pipe_fittings_supplier": {
+      "label_ar": "المواسير والوصلات",
+      "patterns": ["مواسير", "انابيب", "pipes", "fittings", "وصلات", "piping"]
+    },
+    "water_treatment_supplier": {
+      "label_ar": "معالجة المياه",
+      "patterns": ["معالجه مياه", "تحليه", "water treatment", "فلاتر مياه", "reverse osmosis"]
+    },
+    "hvac_supplier": {
+      "label_ar": "التكييف والتهوية",
+      "patterns": ["تكييف", "تهويه", "hvac", "تبريد", "air conditioning", "chiller", "ventilation"]
+    },
+    "plumbing_only_supplier": {
+      "label_ar": "السباكة فقط",
+      "patterns": ["سباكه", "plumbing", "اعمال صحيه"]
+    },
+    "sanitary_ware_supplier": {
+      "label_ar": "الأدوات الصحية",
+      "patterns": ["ادوات صحيه", "اطقم حمامات", "sanitary ware", "خلاطات", "مغاسل"]
+    },
+    "fire_fighting_supplier": {
+      "label_ar": "مكافحة الحريق",
+      "patterns": ["مكافحه الحريق", "اطفاء", "fire fighting", "fire protection", "حريق", "طفايات", "sprinkler"]
+    },
+    "fire_alarm_supplier": {
+      "label_ar": "أنظمة إنذار الحريق",
+      "patterns": ["انذار", "fire alarm", "كشف الحريق", "انظمه انذار", "detection systems"]
+    },
+    "electrical_distributor": {
+      "label_ar": "موزعو المواد الكهربائية",
+      "patterns": ["مواد كهرباييه", "كهرباييه", "electrical", "كهرباء", "electric supplies", "electrical distribution"]
+    },
+    "switchgear_supplier": {
+      "label_ar": "لوحات التوزيع",
+      "patterns": ["لوحات كهرباييه", "switchgear", "لوحات توزيع", "panel boards", "mdb", "mcc"]
+    },
+    "cable_supplier": {
+      "label_ar": "الكابلات",
+      "patterns": ["كابلات", "كيابل", "cables", "اسلاك", "wires", "wiring"]
+    },
+    "generator_supplier": {
+      "label_ar": "المولدات",
+      "patterns": ["مولدات", "generators", "genset", "مولدات ديزل", "diesel generator"]
+    },
+    "ups_supplier": {
+      "label_ar": "أنظمة الطاقة الاحتياطية",
+      "patterns": ["ups", "انظمه طاقه", "بطاريات", "batteries", "power systems", "طاقه احتياطيه", "power protection"]
+    },
+    "lighting_supplier": {
+      "label_ar": "الإنارة",
+      "patterns": ["اناره", "اضاءه", "lighting", "مصابيح", "led lighting", "luminaire"]
+    },
+    "transformer_supplier": {
+      "label_ar": "المحولات",
+      "patterns": ["محولات", "transformers", "transformer"]
+    },
+    "safety_equipment_supplier": {
+      "label_ar": "معدات السلامة",
+      "patterns": ["سلامه", "safety", "مهمات سلامه", "وقايه", "معدات سلامه", "safety equipment", "industrial safety"]
+    },
+    "ppe_supplier": {
+      "label_ar": "مهمات الوقاية الشخصية",
+      "patterns": ["مهمات وقايه", "ppe", "personal protective", "ملابس عمل", "workwear", "احذيه سلامه", "قفازات"]
+    },
+    "industrial_tools_supplier": {
+      "label_ar": "العدد والأدوات الصناعية",
+      "patterns": ["عدد وادوات", "ادوات", "tools", "hardware", "معدات صناعيه", "industrial supplies", "toolings"]
+    },
+    "welding_supplier": {
+      "label_ar": "اللحام",
+      "patterns": ["لحام", "welding", "الكترودات", "electrodes", "welding supplies"]
+    },
+    "gas_equipment_supplier": {
+      "label_ar": "الغازات ومعداتها",
+      "patterns": ["غازات", "gases", "اسطوانات", "cylinders", "اكسجين", "oxygen", "acetylene"]
+    },
+    "cleaning_equipment_supplier": {
+      "label_ar": "معدات النظافة",
+      "patterns": ["نظافه", "cleaning", "تنظيف", "غسالات ضغط", "cleaning equipment"]
+    },
+    "instrumentation_supplier": {
+      "label_ar": "أجهزة القياس والمعايرة",
+      "patterns": ["اجهزه قياس", "قياس", "instruments", "instrumentation", "معايره", "calibration", "اجهزه فحص", "test equipment"]
+    },
+    "data_center_integrator": {
+      "label_ar": "مكاملو مراكز البيانات",
+      "patterns": ["مراكز البيانات", "data center", "datacenter", "حلول مراكز بيانات", "dc solutions"]
+    },
+    "cybersecurity_integrator": {
+      "label_ar": "مكاملو الأمن السيبراني",
+      "patterns": ["امن سيبراني", "cybersecurity", "cyber security", "امن معلومات", "information security", "infosec"]
+    },
+    "network_integrator": {
+      "label_ar": "مكاملو الشبكات",
+      "patterns": ["شبكات", "networks", "networking", "اتصالات", "telecom", "حلول شبكات", "تيار خفيف"]
+    },
+    "it_hardware_supplier": {
+      "label_ar": "موردو أجهزة الحاسب",
+      "patterns": ["حاسب", "كمبيوتر", "computers", "تقنيه المعلومات", "information technology", "اجهزه حاسب", "خوادم", "servers", "storage", "it solutions"]
+    },
+    "cctv_security_supplier": {
+      "label_ar": "أنظمة المراقبة",
+      "patterns": ["مراقبه", "كاميرات", "cctv", "surveillance", "انظمه امنيه", "security systems"]
+    },
+    "access_control_supplier": {
+      "label_ar": "أنظمة التحكم بالدخول",
+      "patterns": ["تحكم بالدخول", "access control", "بوابات", "انظمه دخول", "turnstile"]
+    },
+    "av_supplier": {
+      "label_ar": "الصوتيات والمرئيات",
+      "patterns": ["صوتيات", "audio visual", "شاشات عرض", "display systems"]
+    },
+    "printing_supplier": {
+      "label_ar": "الطباعة والترقيم",
+      "patterns": ["طباعه", "printing", "ملصقات", "labels", "barcode", "ترقيم"]
+    },
+    "insulation_supplier": {
+      "label_ar": "موردو مواد العزل",
+      "patterns": ["عزل", "insulation", "ارمافليكس", "armaflex", "رockwool", "صوف صخري", "فوم", "foam", "بولي ستايرين", "polystyrene"]
+    },
+    "glass_glazing_supplier": {
+      "label_ar": "موردو الزجاج والواجهات الزجاجية",
+      "patterns": ["زجاج", "glass", "glazing", "مرايا", "سكريت", "securit", "تزجيج"]
+    },
+    "aluminium_supplier": {
+      "label_ar": "موردو الألمنيوم وقطاعات الواجهات",
+      "patterns": ["المنيوم", "الومنيوم", "aluminium", "aluminum", "قطاعات المنيوم", "curtain wall", "واجهات"]
+    },
+    "precast_concrete_supplier": {
+      "label_ar": "موردو الخرسانة مسبقة الصب",
+      "patterns": ["مسبق الصب", "مسبقه الصب", "precast", "بريكاست", "غرف تفتيش", "manhole", "انترلوك"]
+    },
+    "paving_supplier": {
+      "label_ar": "موردو البلاط والرصف",
+      "patterns": ["انترلوك", "interlock", "رصف", "paving", "بردوره", "kerb", "بلاط رصف"]
+    },
+    "geosynthetics_supplier": {
+      "label_ar": "موردو المواد الأرضية الصناعية",
+      "patterns": ["جيوتكستايل", "geotextile", "جيوجريد", "geogrid", "geosynthetic", "geomembrane", "ممبرين ارضي"]
+    },
+    "block_masonry_supplier": {
+      "label_ar": "موردو البلوك ومواد المباني",
+      "patterns": ["بلوك", "block factory", "مصنع بلوك", "طوب", "brick", "مواد بناء"]
+    },
+    "concrete_chemicals_supplier": {
+      "label_ar": "موردو الكيماويات والإضافات الخرسانية",
+      "patterns": ["كيماويات", "chemicals", "اضافات", "admixture", "سيكا", "sika", "mapei", "fosroc", "basf"]
+    },
+    "cement_supplier": {
+      "label_ar": "موردو الأسمنت والمواد الرابطة",
+      "patterns": ["اسمنت", "cement", "مونه", "mortar", "جبس", "بلاستر", "plaster"]
+    },
+    "road_furniture_supplier": {
+      "label_ar": "موردو أثاث ولوحات الطرق",
+      "patterns": ["لوحات مروريه", "traffic sign", "اثاث حضري", "street furniture", "سلامه مروريه", "road safety", "مظلات"]
+    },
+    "irrigation_supplier": {
+      "label_ar": "موردو أنظمة الري والزراعة",
+      "patterns": ["ري", "irrigation", "زراعه", "agriculture", "تنقيط", "مشاتل", "landscape"]
+    },
+    "pipe_support_supplier": {
+      "label_ar": "موردو حوامل ومساند المواسير",
+      "patterns": ["حوامل", "supports", "hangers", "unistrut", "يونيسترت", "مساند"]
+    },
+    "joinery_panel_supplier": {
+      "label_ar": "موردو الأخشاب والألواح",
+      "patterns": ["اخشاب", "خشب", "wood", "timber", "plywood", "ابلاكاش", "mdf", "نجاره", "joinery"]
+    }
   },
-
   "facets": {
-    "dimensions": { "kind": "measure", "label_ar": "الأبعاد", "supplier_pool_affecting": false, "pattern": "(\\d+(?:\\.\\d+)?)\\s*x\\s*(\\d+(?:\\.\\d+)?)(?:\\s*x\\s*(\\d+(?:\\.\\d+)?))?" },
-    "section_type": { "kind": "enum", "label_ar": "نوع القطاع", "supplier_pool_affecting": false, "values": {
-      "rhs": ["rhs", "rectangular hollow", "قطاع مستطيل", "مستطيل مجوف"],
-      "shs": ["shs", "square hollow", "قطاع مربع", "مربع مجوف"],
-      "chs": ["chs", "circular hollow", "قطاع دايري", "انبوب دايري"],
-      "ipe": ["ipe", "ipn", "i beam", "i-beam", "كمره", "كمرة"],
-      "heb": ["heb", "hea", "hem", "h beam", "h-beam"],
-      "upn": ["upn", "upe", "channel", "قطاع يو", "قناه"],
-      "angle": ["angle", "زاويه", "زاوية", "equal angle", "قطاع زاويه"],
-      "flat": ["flat bar", "فلات", "شريحه حديد", "بار مسطح"],
-      "round": ["round bar", "حديد دايري", "سيخ دايري"],
-      "plate": ["plate", "لوح حديد", "بليت"]
-    } },
-    "grade": { "kind": "measure", "label_ar": "الدرجة", "supplier_pool_affecting": false, "pattern": "\\b(s235|s275|s355|a36|a572|st37|st52|gr\\s?\\d{2,3}|ss\\s?304l?|ss\\s?316l?|304l?|316l?)\\b" },
-    "finish": { "kind": "enum", "label_ar": "التشطيب", "supplier_pool_affecting": true, "values": {
-      "galvanized": ["مجلفن", "galvanized", "galvanised", "hdg", "gi", "مغلفن"],
-      "painted": ["مدهون", "painted", "powder coated", "مطلي", "دهان فرن"],
-      "black": ["اسود", "black steel", "خام"],
-      "coated": ["coated", "مغلف", "epoxy coated", "pvdf"]
-    } },
-    "length_m": { "kind": "measure", "label_ar": "الطول", "supplier_pool_affecting": false, "pattern": "(?:طول|ارتفاع|length|height)\\s*(\\d+(?:\\.\\d+)?)\\s*(?:م|m|متر)?" },
-    "thickness_mm": { "kind": "measure", "label_ar": "السماكة", "supplier_pool_affecting": false, "pattern": "(?:سماكه|سماكة|thickness)\\s*(\\d+(?:\\.\\d+)?)|(\\d+(?:\\.\\d+)?)\\s*(?:mm|مم)\\b" },
-    "material": { "kind": "enum", "label_ar": "المادة", "supplier_pool_affecting": true, "values": {
-      "stainless": ["ستانلس", "stainless", "ss 304", "ss 316", "316l", "304l"],
-      "cast_iron": ["زهر", "cast iron", "ductile iron", "حديد زهر"],
-      "brass": ["نحاس اصفر", "brass", "bronze", "برونز"],
-      "copper": ["نحاس", "copper", "cu"],
-      "steel": ["حديد", "فولاذ", "steel", "carbon steel"],
-      "aluminium": ["المنيوم", "aluminium", "aluminum", "acp", "الوكبوند"],
-      "pvc": ["pvc", "upvc", "بي في سي"],
-      "polymer": ["pe", "hdpe", "ppr", "ptfe", "epdm", "بولي", "polymer", "nitrile", "نيتريل"],
-      "wood": ["خشب", "mdf", "hpl", "خشبي", "plywood", "ابلكاش"],
-      "gypsum": ["جبس", "gypsum", "جبسم"],
-      "stone": ["حجر", "رخام", "granite", "جرانيت", "marble"],
-      "glass": ["زجاج", "glass", "polycarbonate", "بولي كربونيت"],
-      "concrete": ["خرسانه", "concrete"]
-    } },
-    "surface": { "kind": "enum", "label_ar": "السطح", "supplier_pool_affecting": false, "values": {
-      "column": ["اعمده", "اعمدة", "عمود", "column", "columns", "pillar"],
-      "wall": ["جدار", "جداري", "جدران", "wall", "walls"],
-      "ceiling": ["سقف", "اسقف", "سقفي", "ceiling", "soffit"],
-      "facade": ["واجهه", "واجهات", "facade", "elevation"],
-      "floor": ["ارضيه", "ارضيات", "floor", "flooring"]
-    } },
-    "application": { "kind": "enum", "label_ar": "الاستخدام", "supplier_pool_affecting": true, "values": {
-      "interior": ["داخلي", "داخليه", "interior", "indoor"],
-      "exterior": ["خارجي", "خارجيه", "exterior", "outdoor"],
-      "industrial": ["صناعي", "صناعيه", "industrial", "heavy duty"],
-      "marine": ["بحري", "marine", "offshore"],
-      "hazardous": ["مناطق خطره", "hazardous", "atex", "explosion proof", "مضاد للانفجار"]
-    } },
-    "pump_type": { "kind": "enum", "label_ar": "نوع المضخة", "supplier_pool_affecting": true, "values": {
-      "end_suction": ["end suction", "سحب طرفي"],
-      "vertical_multistage": ["vertical multistage", "multistage", "متعدده المراحل", "عموديه متعدده"],
-      "submersible": ["submersible", "غاطسه", "غاطسة", "dewatering", "نزح"],
-      "sump": ["sump", "حفره", "sump pit"],
-      "sewage": ["sewage", "صرف صحي", "مجاري", "non-clog"],
-      "fire": ["fire pump", "مضخه حريق", "jockey"],
-      "booster": ["booster", "تعزيز", "pressure boosting"],
-      "dosing": ["dosing", "جرعات", "جرعه", "metering pump", "كيماويات"],
-      "circulation": ["circulation", "تدوير", "inline", "circulator"],
-      "diaphragm": ["diaphragm", "غشاييه"],
-      "gear": ["gear pump", "ترسيه"]
-    } },
-    "valve_type": { "kind": "enum", "label_ar": "نوع المحبس", "supplier_pool_affecting": false, "values": {
-      "gate": ["gate valve", "محبس بوابه", "سكينه"],
-      "butterfly": ["butterfly", "فراشه"],
-      "check": ["check valve", "non return", "عدم رجوع", "dual plate"],
-      "globe": ["globe valve", "كرويه خانقه"],
-      "ball": ["ball valve", "محبس كره", "كروي"],
-      "pressure_reducing": ["pressure reducing", "تخفيض ضغط", "prv"],
-      "strainer": ["strainer", "مصفاه", "y-strainer"],
-      "air_release": ["air release", "air vent", "تصريف هواء"],
-      "flexible_joint": ["flexible joint", "وصله مرنه", "expansion joint"],
-      "dismantling": ["dismantling joint", "وصله فك"],
-      "safety_relief": ["relief valve", "safety valve", "محبس امان"]
-    } },
-    "flow_rate": { "kind": "measure", "label_ar": "التصرف", "supplier_pool_affecting": false, "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:m3\\s*/?\\s*(?:hr|h)|م\\s*3\\s*/?\\s*ساعه|gpm|lpm|l\\s*/\\s*h|cfm|cmh)" },
-    "head_pressure": { "kind": "measure", "label_ar": "الضغط/الرفع", "supplier_pool_affecting": false, "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:bar|psi)|pn\\s?(\\d+)" },
-    "diameter": { "kind": "measure", "label_ar": "القطر", "supplier_pool_affecting": false, "pattern": "dn\\s?(\\d+)|(\\d+(?:\\.\\d+)?)\\s*(?:inch|بوصه|بوصة)" },
-    "power_kw": { "kind": "measure", "label_ar": "القدرة", "supplier_pool_affecting": false, "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:kw|كيلو\\s*واط|hp|حصان)\\b" },
-    "capacity": { "kind": "measure", "label_ar": "السعة", "supplier_pool_affecting": false, "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:kva|kvar|ah|ton|طن|لتر|ltr|l\\b|م\\s*3|m3|kg|كجم)" },
-    "voltage": { "kind": "measure", "label_ar": "الجهد", "supplier_pool_affecting": true, "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:kv|vdc|vac|volt)\\b" },
-    "protection_ip": { "kind": "measure", "label_ar": "درجة الحماية", "supplier_pool_affecting": false, "pattern": "\\bip\\s?(\\d{2})\\b" },
-    "data_speed": { "kind": "measure", "label_ar": "السرعة", "supplier_pool_affecting": false, "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:gbe|gbps|gb\\s*/\\s*s|mbps|tbps|g\\b)" },
-    "storage_capacity": { "kind": "measure", "label_ar": "سعة التخزين", "supplier_pool_affecting": false, "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:tb|gb|pb)\\b" },
-    "rack_units": { "kind": "measure", "label_ar": "وحدات الرف", "supplier_pool_affecting": false, "pattern": "(?:بارتفاع|ارتفاع|مقاس|height)?\\s*(\\d+)\\s*u\\b" },
-    "range_m": { "kind": "measure", "label_ar": "المدى", "supplier_pool_affecting": false, "pattern": "(?:مدى|بطول|range|بمدي)\\s*(\\d+(?:\\.\\d+)?)\\s*(?:م|m|متر)?" },
-    "accuracy": { "kind": "measure", "label_ar": "الدقة", "supplier_pool_affecting": false, "pattern": "(?:دقه|دقة|accuracy|±)\\s*±?\\s*(\\d+(?:\\.\\d+)?)" },
-    "standard": { "kind": "measure", "label_ar": "المواصفة", "supplier_pool_affecting": true, "pattern": "\\b(en\\s?\\d{2,5}|iec\\s?\\d{3,5}|astm\\s?[a-z]?\\d+|ansi\\s?[a-z0-9.]+|ul\\s?\\/?\\s?fm|nfpa\\s?\\d+|iso\\s?\\d{3,5})\\b" },
-    "protection_class": { "kind": "enum", "label_ar": "فئة الحماية", "supplier_pool_affecting": false, "values": {
-      "arc_flash": ["arc flash", "قوس كهربايي"],
-      "flame_retardant": ["مقاوم للهب", "flame retardant", "fr coverall", "lszh"],
-      "cut_resistant": ["مقاوم للقطع", "cut resistant", "cut level"],
-      "chemical": ["كيمياييه", "chemical resistant", "type 5", "type 6"],
-      "anti_static": ["مضاد للكهرباء الساكنه", "anti static", "esd"],
-      "high_visibility": ["عاليه الوضوح", "high visibility", "hi-vis", "عاكسه", "class 3"]
-    } },
-    "interface": { "kind": "enum", "label_ar": "الواجهة", "supplier_pool_affecting": false, "values": {
-      "usb": ["usb"],
-      "ethernet": ["ethernet", "شبكه", "rj45", "lan"],
-      "wireless": ["wifi", "wireless", "لاسلكي", "bluetooth"],
-      "serial": ["serial", "rs232", "rs485"],
-      "hdmi": ["hdmi", "displayport", "vga"]
-    } },
-    "print_technology": { "kind": "enum", "label_ar": "تقنية الطباعة", "supplier_pool_affecting": true, "values": {
-      "thermal": ["حراريه", "thermal transfer", "direct thermal", "حراري"],
-      "laser": ["ليزر", "laser"],
-      "inkjet": ["نافثه للحبر", "inkjet"],
-      "dye_sublimation": ["dye sublimation", "retransfer", "تصعيد"],
-      "dot_matrix": ["dot matrix", "مصفوفه نقطيه"]
-    } },
-    "camera_form": { "kind": "enum", "label_ar": "شكل الكاميرا", "supplier_pool_affecting": false, "values": {
-      "dome": ["dome", "قبه"],
-      "bullet": ["bullet", "اسطوانيه"],
-      "ptz": ["ptz", "pan tilt zoom", "متحركه"],
-      "fisheye": ["fisheye", "بانوراميه", "360"]
-    } },
-    "resolution_mp": { "kind": "measure", "label_ar": "الدقة", "supplier_pool_affecting": false, "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:mp|ميجابكسل|megapixel)" },
-    "credential_type": { "kind": "enum", "label_ar": "نوع بيانات الدخول", "supplier_pool_affecting": false, "values": {
-      "rfid": ["rfid", "mifare", "125khz", "13.56"],
-      "biometric": ["بصمه", "biometric", "fingerprint", "وجه", "face"],
-      "pin": ["pin", "رقم سري", "keypad"],
-      "mobile": ["mobile credential", "ble", "nfc"]
-    } }
+    "dimensions": {
+      "kind": "measure",
+      "label_ar": "الأبعاد",
+      "supplier_pool_affecting": false,
+      "pattern": "(\\d+(?:\\.\\d+)?)\\s*x\\s*(\\d+(?:\\.\\d+)?)(?:\\s*x\\s*(\\d+(?:\\.\\d+)?))?"
+    },
+    "section_type": {
+      "kind": "enum",
+      "label_ar": "نوع القطاع",
+      "supplier_pool_affecting": false,
+      "values": {
+        "rhs": ["rhs", "rectangular hollow", "قطاع مستطيل", "مستطيل مجوف"],
+        "shs": ["shs", "square hollow", "قطاع مربع", "مربع مجوف"],
+        "chs": ["chs", "circular hollow", "قطاع دايري", "انبوب دايري"],
+        "ipe": ["ipe", "ipn", "i beam", "i-beam", "كمره", "كمرة"],
+        "heb": ["heb", "hea", "hem", "h beam", "h-beam"],
+        "upn": ["upn", "upe", "channel", "قطاع يو", "قناه"],
+        "angle": ["angle", "زاويه", "زاوية", "equal angle", "قطاع زاويه"],
+        "flat": ["flat bar", "فلات", "شريحه حديد", "بار مسطح"],
+        "round": ["round bar", "حديد دايري", "سيخ دايري"],
+        "plate": ["plate", "لوح حديد", "بليت"]
+      }
+    },
+    "grade": {
+      "kind": "measure",
+      "label_ar": "الدرجة",
+      "supplier_pool_affecting": false,
+      "pattern": "\\b(s235|s275|s355|a36|a572|st37|st52|gr\\s?\\d{2,3}|ss\\s?304l?|ss\\s?316l?|304l?|316l?)\\b"
+    },
+    "finish": {
+      "kind": "enum",
+      "label_ar": "التشطيب",
+      "supplier_pool_affecting": true,
+      "values": {
+        "galvanized": ["مجلفن", "galvanized", "galvanised", "hdg", "gi", "مغلفن"],
+        "painted": ["مدهون", "painted", "powder coated", "مطلي", "دهان فرن"],
+        "black": ["اسود", "black steel", "خام"],
+        "coated": ["coated", "مغلف", "epoxy coated", "pvdf"]
+      }
+    },
+    "length_m": {
+      "kind": "measure",
+      "label_ar": "الطول",
+      "supplier_pool_affecting": false,
+      "pattern": "(?:طول|ارتفاع|length|height)\\s*(\\d+(?:\\.\\d+)?)\\s*(?:م|m|متر)?"
+    },
+    "thickness_mm": {
+      "kind": "measure",
+      "label_ar": "السماكة",
+      "supplier_pool_affecting": false,
+      "pattern": "(?:سماكه|سماكة|thickness)\\s*(\\d+(?:\\.\\d+)?)|(\\d+(?:\\.\\d+)?)\\s*(?:mm|مم)\\b"
+    },
+    "material": {
+      "kind": "enum",
+      "label_ar": "المادة",
+      "supplier_pool_affecting": true,
+      "values": {
+        "stainless": ["ستانلس", "stainless", "ss 304", "ss 316", "316l", "304l"],
+        "cast_iron": ["زهر", "cast iron", "ductile iron", "حديد زهر"],
+        "brass": ["نحاس اصفر", "brass", "bronze", "برونز"],
+        "copper": ["نحاس", "copper", "cu"],
+        "steel": ["حديد", "فولاذ", "steel", "carbon steel"],
+        "aluminium": ["المنيوم", "aluminium", "aluminum", "acp", "الوكبوند"],
+        "pvc": ["pvc", "upvc", "بي في سي"],
+        "polymer": ["pe", "hdpe", "ppr", "ptfe", "epdm", "بولي", "polymer", "nitrile", "نيتريل"],
+        "wood": ["خشب", "mdf", "hpl", "خشبي", "plywood", "ابلكاش"],
+        "gypsum": ["جبس", "gypsum", "جبسم"],
+        "stone": ["حجر", "رخام", "granite", "جرانيت", "marble"],
+        "glass": ["زجاج", "glass", "polycarbonate", "بولي كربونيت"],
+        "concrete": ["خرسانه", "concrete"]
+      }
+    },
+    "surface": {
+      "kind": "enum",
+      "label_ar": "السطح",
+      "supplier_pool_affecting": false,
+      "values": {
+        "column": ["اعمده", "اعمدة", "عمود", "column", "columns", "pillar"],
+        "wall": ["جدار", "جداري", "جدران", "wall", "walls"],
+        "ceiling": ["سقف", "اسقف", "سقفي", "ceiling", "soffit"],
+        "facade": ["واجهه", "واجهات", "facade", "elevation"],
+        "floor": ["ارضيه", "ارضيات", "floor", "flooring"]
+      }
+    },
+    "application": {
+      "kind": "enum",
+      "label_ar": "الاستخدام",
+      "supplier_pool_affecting": true,
+      "values": {
+        "interior": ["داخلي", "داخليه", "interior", "indoor"],
+        "exterior": ["خارجي", "خارجيه", "exterior", "outdoor"],
+        "industrial": ["صناعي", "صناعيه", "industrial", "heavy duty"],
+        "marine": ["بحري", "marine", "offshore"],
+        "hazardous": ["مناطق خطره", "hazardous", "atex", "explosion proof", "مضاد للانفجار"]
+      }
+    },
+    "pump_type": {
+      "kind": "enum",
+      "label_ar": "نوع المضخة",
+      "supplier_pool_affecting": true,
+      "values": {
+        "end_suction": ["end suction", "سحب طرفي"],
+        "vertical_multistage": ["vertical multistage", "multistage", "متعدده المراحل", "عموديه متعدده"],
+        "submersible": ["submersible", "غاطسه", "غاطسة", "dewatering", "نزح"],
+        "sump": ["sump", "حفره", "sump pit"],
+        "sewage": ["sewage", "صرف صحي", "مجاري", "non-clog"],
+        "fire": ["fire pump", "مضخه حريق", "jockey"],
+        "booster": ["booster", "تعزيز", "pressure boosting"],
+        "dosing": ["dosing", "جرعات", "جرعه", "metering pump", "كيماويات"],
+        "circulation": ["circulation", "تدوير", "inline", "circulator"],
+        "diaphragm": ["diaphragm", "غشاييه"],
+        "gear": ["gear pump", "ترسيه"]
+      }
+    },
+    "valve_type": {
+      "kind": "enum",
+      "label_ar": "نوع المحبس",
+      "supplier_pool_affecting": false,
+      "values": {
+        "gate": ["gate valve", "محبس بوابه", "سكينه"],
+        "butterfly": ["butterfly", "فراشه"],
+        "check": ["check valve", "non return", "عدم رجوع", "dual plate"],
+        "globe": ["globe valve", "كرويه خانقه"],
+        "ball": ["ball valve", "محبس كره", "كروي"],
+        "pressure_reducing": ["pressure reducing", "تخفيض ضغط", "prv"],
+        "strainer": ["strainer", "مصفاه", "y-strainer"],
+        "air_release": ["air release", "air vent", "تصريف هواء"],
+        "flexible_joint": ["flexible joint", "وصله مرنه", "expansion joint"],
+        "dismantling": ["dismantling joint", "وصله فك"],
+        "safety_relief": ["relief valve", "safety valve", "محبس امان"]
+      }
+    },
+    "flow_rate": {
+      "kind": "measure",
+      "label_ar": "التصرف",
+      "supplier_pool_affecting": false,
+      "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:m3\\s*/?\\s*(?:hr|h)|م\\s*3\\s*/?\\s*ساعه|gpm|lpm|l\\s*/\\s*h|cfm|cmh)"
+    },
+    "head_pressure": {
+      "kind": "measure",
+      "label_ar": "الضغط/الرفع",
+      "supplier_pool_affecting": false,
+      "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:bar|psi)|pn\\s?(\\d+)"
+    },
+    "diameter": {
+      "kind": "measure",
+      "label_ar": "القطر",
+      "supplier_pool_affecting": false,
+      "pattern": "dn\\s?(\\d+)|(\\d+(?:\\.\\d+)?)\\s*(?:inch|بوصه|بوصة)"
+    },
+    "power_kw": {
+      "kind": "measure",
+      "label_ar": "القدرة",
+      "supplier_pool_affecting": false,
+      "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:kw|كيلو\\s*واط|hp|حصان)\\b"
+    },
+    "capacity": {
+      "kind": "measure",
+      "label_ar": "السعة",
+      "supplier_pool_affecting": false,
+      "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:kva|kvar|ah|ton|طن|لتر|ltr|l\\b|م\\s*3|m3|kg|كجم)"
+    },
+    "voltage": {
+      "kind": "measure",
+      "label_ar": "الجهد",
+      "supplier_pool_affecting": true,
+      "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:kv|vdc|vac|volt)\\b"
+    },
+    "protection_ip": {
+      "kind": "measure",
+      "label_ar": "درجة الحماية",
+      "supplier_pool_affecting": false,
+      "pattern": "\\bip\\s?(\\d{2})\\b"
+    },
+    "data_speed": {
+      "kind": "measure",
+      "label_ar": "السرعة",
+      "supplier_pool_affecting": false,
+      "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:gbe|gbps|gb\\s*/\\s*s|mbps|tbps|g\\b)"
+    },
+    "storage_capacity": {
+      "kind": "measure",
+      "label_ar": "سعة التخزين",
+      "supplier_pool_affecting": false,
+      "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:tb|gb|pb)\\b"
+    },
+    "rack_units": {
+      "kind": "measure",
+      "label_ar": "وحدات الرف",
+      "supplier_pool_affecting": false,
+      "pattern": "(?:بارتفاع|ارتفاع|مقاس|height)?\\s*(\\d+)\\s*u\\b"
+    },
+    "range_m": {
+      "kind": "measure",
+      "label_ar": "المدى",
+      "supplier_pool_affecting": false,
+      "pattern": "(?:مدى|بطول|range|بمدي)\\s*(\\d+(?:\\.\\d+)?)\\s*(?:م|m|متر)?"
+    },
+    "accuracy": {
+      "kind": "measure",
+      "label_ar": "الدقة",
+      "supplier_pool_affecting": false,
+      "pattern": "(?:دقه|دقة|accuracy|±)\\s*±?\\s*(\\d+(?:\\.\\d+)?)"
+    },
+    "standard": {
+      "kind": "measure",
+      "label_ar": "المواصفة",
+      "supplier_pool_affecting": true,
+      "pattern": "\\b(en\\s?\\d{2,5}|iec\\s?\\d{3,5}|astm\\s?[a-z]?\\d+|ansi\\s?[a-z0-9.]+|ul\\s?\\/?\\s?fm|nfpa\\s?\\d+|iso\\s?\\d{3,5})\\b"
+    },
+    "protection_class": {
+      "kind": "enum",
+      "label_ar": "فئة الحماية",
+      "supplier_pool_affecting": false,
+      "values": {
+        "arc_flash": ["arc flash", "قوس كهربايي"],
+        "flame_retardant": ["مقاوم للهب", "flame retardant", "fr coverall", "lszh"],
+        "cut_resistant": ["مقاوم للقطع", "cut resistant", "cut level"],
+        "chemical": ["كيمياييه", "chemical resistant", "type 5", "type 6"],
+        "anti_static": ["مضاد للكهرباء الساكنه", "anti static", "esd"],
+        "high_visibility": ["عاليه الوضوح", "high visibility", "hi-vis", "عاكسه", "class 3"]
+      }
+    },
+    "interface": {
+      "kind": "enum",
+      "label_ar": "الواجهة",
+      "supplier_pool_affecting": false,
+      "values": {
+        "usb": ["usb"],
+        "ethernet": ["ethernet", "شبكه", "rj45", "lan"],
+        "wireless": ["wifi", "wireless", "لاسلكي", "bluetooth"],
+        "serial": ["serial", "rs232", "rs485"],
+        "hdmi": ["hdmi", "displayport", "vga"]
+      }
+    },
+    "print_technology": {
+      "kind": "enum",
+      "label_ar": "تقنية الطباعة",
+      "supplier_pool_affecting": true,
+      "values": {
+        "thermal": ["حراريه", "thermal transfer", "direct thermal", "حراري"],
+        "laser": ["ليزر", "laser"],
+        "inkjet": ["نافثه للحبر", "inkjet"],
+        "dye_sublimation": ["dye sublimation", "retransfer", "تصعيد"],
+        "dot_matrix": ["dot matrix", "مصفوفه نقطيه"]
+      }
+    },
+    "camera_form": {
+      "kind": "enum",
+      "label_ar": "شكل الكاميرا",
+      "supplier_pool_affecting": false,
+      "values": {
+        "dome": ["dome", "قبه"],
+        "bullet": ["bullet", "اسطوانيه"],
+        "ptz": ["ptz", "pan tilt zoom", "متحركه"],
+        "fisheye": ["fisheye", "بانوراميه", "360"]
+      }
+    },
+    "resolution_mp": {
+      "kind": "measure",
+      "label_ar": "الدقة",
+      "supplier_pool_affecting": false,
+      "pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:mp|ميجابكسل|megapixel)"
+    },
+    "credential_type": {
+      "kind": "enum",
+      "label_ar": "نوع بيانات الدخول",
+      "supplier_pool_affecting": false,
+      "values": {
+        "rfid": ["rfid", "mifare", "125khz", "13.56"],
+        "biometric": ["بصمه", "biometric", "fingerprint", "وجه", "face"],
+        "pin": ["pin", "رقم سري", "keypad"],
+        "mobile": ["mobile credential", "ble", "nfc"]
+      }
+    }
   },
-
   "sectors": {
     "STRUCTURAL_METALS": {
       "domain": "structural",
@@ -262,9 +605,22 @@
       "negative_terms": [],
       "soft_conflicts": ["general_trading", "general_building_materials"],
       "hard_conflicts": ["ready_mix_supplier", "paint_only_supplier", "sanitary_ware_supplier", "plumbing_only_supplier", "rebar_supplier"]
+    },
+    "INFRASTRUCTURE": {
+      "domain": "infrastructure",
+      "context_terms": ["طريق", "طرق", "رصف", "تصريف", "بنيه تحتيه", "infrastructure", "خارجي", "موقع"],
+      "supplier_terms": ["بنيه تحتيه", "طرق", "رصف", "infrastructure"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["cybersecurity_integrator", "it_hardware_supplier", "printing_supplier", "sanitary_ware_supplier"]
+    },
+    "CIVIL_CONCRETE": {
+      "domain": "civil",
+      "context_terms": ["خرسانه", "concrete", "مباني", "اسمنت", "cement", "مونه", "صب"],
+      "supplier_terms": ["مواد بناء", "خرسانه", "اسمنت", "بلوك"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["cybersecurity_integrator", "it_hardware_supplier", "printing_supplier", "ppe_supplier"]
     }
   },
-
   "families": [
     {
       "id": "structural_steel",
@@ -273,6 +629,14 @@
       "strong_terms": ["قطاع حديد", "قطاعات حديد", "حديد انشايي", "فولاذ انشايي", "structural steel", "steel section", "hollow section", "rolled section", "steel profile", "كمره حديد", "زاويه حديد", "rhs", "shs", "chs", "upn", "upe", "ipe", "ipn", "heb", "hea", "rectangular hollow section", "square hollow section", "circular hollow section", "channel section", "angle bar", "flat bar", "قطاع مجوف", "بروفيل حديد"],
       "weak_terms": ["قطاع", "section", "beam", "angle", "channel", "زاويه"],
       "context_terms": ["حديد", "فولاذ", "steel", "s275", "s355", "mill certificate", "مجلفن"],
+      "term_guards": [
+        {
+          "terms": ["قطاع", "section", "beam", "angle", "channel", "زاويه"],
+          "require_any": ["حديد", "فولاذ", "steel", "s235", "s275", "s355", "a36", "mill certificate", "rhs", "shs", "chs", "ipe", "ipn", "heb", "hea", "upn", "upe", "كمره", "انشايي", "بروفيل", "مجوف"],
+          "block_any": ["stud", "track", "جبس", "جبسي", "قواطع جافه", "drywall", "furring", "omega", "سقف معلق", "قطاع اوميغا"],
+          "note": "«قطاع»/«زاويه» name a SHAPE, so they decide only with real structural evidence. A 0.45 mm galvanized drywall stud is a section by shape and a gypsum product by trade — it must never reach a steel stockist."
+        }
+      ],
       "supplier_terms": ["حديد", "فولاذ", "steel", "قطاعات", "معادن"],
       "preferred_archetypes": ["structural_steel_supplier", "metal_trading"],
       "allowed_archetypes": ["general_building_materials", "rebar_supplier"],
@@ -288,8 +652,22 @@
         {
           "id": "steel_sections",
           "label_ar": "القطاعات الحديدية",
-          "strong_terms": ["قطاع حديد", "steel section", "hollow section"],
+          "strong_terms": ["قطاع حديد", "steel section", "hollow section", "قطاعات حديد", "بارد التشكيل", "باردة التشكيل", "cold formed", "cold rolled section"],
           "intents": [
+            {
+              "id": "cold_formed_section",
+              "label_ar": "قطاعات باردة التشكيل",
+              "strong_terms": ["بارد التشكيل", "باردة التشكيل", "قطاع بارد التشكيل", "قطاعات باردة التشكيل", "cold formed section", "cold formed steel", "cold rolled section", "purlin", "مرزب", "مرازب", "c purlin", "z purlin", "قطاع c", "قطاع z", "sigma section", "lipped channel"],
+              "context_terms": ["حديد", "steel", "مجلفن", "galvanized", "سماكه", "thickness", "مم", "mm"],
+              "note": "Cold-formed sections are a DIFFERENT TRADE from hot-rolled structural steel — roll-formers and purlin mills, not steel stockists — and the held-out corpus found the whole steel-structures sector 100% unusable on 4,914 rows of exactly this product, because «قطاعات باردة التشكيل» only ever matched the generic weak «قطاع» and never named a family.",
+              "facets": [
+                { "name": "section_type", "supplier_pool_affecting": false },
+                { "name": "dimensions", "supplier_pool_affecting": false },
+                { "name": "grade", "supplier_pool_affecting": false },
+                { "name": "finish", "supplier_pool_affecting": true },
+                { "name": "length_m", "supplier_pool_affecting": false }
+              ]
+            },
             {
               "id": "structural_steel_section",
               "label_ar": "قطاع حديد إنشائي",
@@ -324,6 +702,13 @@
         { "name": "grade", "supplier_pool_affecting": false },
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "material", "supplier_pool_affecting": true }
+      ],
+      "term_guards": [
+        {
+          "terms": ["صاج", "sheet", "plate", "لفايف", "coil"],
+          "block_any": ["enclosure", "لوحه فارغه", "خزانه", "cabinet", "ip54", "ip55", "ip65", "ip66", "rack", "صندوق توزيع", "consumer unit", "busbar"],
+          "note": "An electrical enclosure is MADE OF sheet steel but is not sheet steel; it is bought from a panel builder, not a steel stockist."
+        }
       ]
     },
     {
@@ -355,9 +740,15 @@
       "facets": [
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "grade", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "welded_mesh",
+          "label_ar": "شبك تسليح",
+          "strong_terms": ["شبك تسليح", "شبك ملحوم", "welded mesh", "welded wire mesh", "شبك تسليح ملحوم", "brc mesh", "شبك حديد تسليح"]
+        }
       ]
     },
-
     {
       "id": "sandwich_panel",
       "sector": "BUILDING_ENVELOPE",
@@ -389,13 +780,20 @@
         { "name": "thickness_mm", "supplier_pool_affecting": false },
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "material", "supplier_pool_affecting": true }
+      ],
+      "term_guards": [
+        {
+          "terms": ["twinwall", "twin wall", "multiwall", "polycarbonate", "بولي كربونيت", "لوح بولي"],
+          "block_any": ["ماسوره", "مواسير", "conduit", "كهرباء", "emt", "imc", "تمديدات", "cable", "كابل", "drainage", "صرف"],
+          "note": "«Twinwall» describes a twin-walled EXTRUSION, which is polycarbonate roofing sheet in the envelope trade and HDPE conduit in the electrical trade. Exposed once conduit stopped being read as water pipe."
+        }
       ]
     },
     {
       "id": "waterproofing",
       "sector": "BUILDING_ENVELOPE",
       "label_ar": "العزل المائي والحراري",
-      "strong_terms": ["ممبرين", "waterproofing membrane", "tpo", "بيتومين", "bitumen", "عزل مايي", "عزل حراري", "waterproofing", "صوف صخري", "rockwool"],
+      "strong_terms": ["ممبرين", "waterproofing membrane", "tpo", "بيتومين", "bitumen", "عزل مايي", "waterproofing"],
       "weak_terms": ["عزل", "membrane", "insulation"],
       "context_terms": ["سماكه", "مايي", "حراري", "سقف", "اساسات", "roll"],
       "supplier_terms": ["عزل", "waterproofing", "ممبرين"],
@@ -423,9 +821,15 @@
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "material", "supplier_pool_affecting": true },
         { "name": "finish", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "door_hardware",
+          "label_ar": "حديديات أبواب",
+          "strong_terms": ["door closer", "مغلق باب", "door coordinator", "door stop", "مصد باب", "drop seal", "intumescent seal", "kick plate", "حديديات ابواب", "panic bar", "push bar", "مقبض باب", "door handle", "مفصله باب", "lockset", "cylinder lock", "door holder", "flush bolt", "panic exit device", "smoke seal", "اسطوانه قفل", "euro profile cylinder", "mortise lock", "قفل mortise", "مغلاق باب", "floor spring", "مفصله زنبركيه", "double action hinge", "مفصله ستانلس", "ball bearing hinge", "lever handle", "مقبض lever", "باب مغلاق"]
+        }
       ]
     },
-
     {
       "id": "architectural_cladding",
       "sector": "ARCHITECTURAL_FINISHES",
@@ -496,6 +900,13 @@
         { "name": "material", "supplier_pool_affecting": true },
         { "name": "application", "supplier_pool_affecting": true },
         { "name": "capacity", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "primer_coating",
+          "label_ar": "برايمر ودهانات",
+          "strong_terms": ["برايمر", "primer", "دهان اكريليك", "acrylic paint", "دهان ايبوكسي", "epoxy paint", "intumescent paint", "دهان زنك", "zinc rich primer", "دهان بولي يوريثان", "pu paint", "دهان تمهيدي", "طلاء واقي", "دهانات وطلاءات"]
+        }
       ]
     },
     {
@@ -528,6 +939,18 @@
       "facets": [
         { "name": "material", "supplier_pool_affecting": true },
         { "name": "capacity", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "tile_grout_adhesive",
+          "label_ar": "جراوت ولواصق بلاط",
+          "strong_terms": ["جراوت", "grout", "tile grout", "epoxy grout", "جراوت بلاط", "tile adhesive", "لاصق بلاط", "ceramic adhesive", "مونه لاصقه"]
+        },
+        {
+          "id": "construction_sealant",
+          "label_ar": "سيلانت",
+          "strong_terms": ["سيلانت", "sealant", "ms polymer", "silicone sealant", "سيليكون", "polyurethane sealant", "acrylic sealant", "fire rated sealant", "backer rod", "مواد لاصقه وسيلانت"]
+        }
       ]
     },
     {
@@ -545,6 +968,50 @@
         { "name": "material", "supplier_pool_affecting": true },
         { "name": "thickness_mm", "supplier_pool_affecting": false },
         { "name": "dimensions", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "drywall_framing",
+          "label_ar": "قطاعات قواطع جافة",
+          "strong_terms": ["قطاع stud", "قطاع track", "stud مجلفن", "track مجلفن", "c stud", "u track", "drywall stud", "قطاعات قواطع جافه", "furring channel", "omega profile", "قطاع اوميغا", "wall angle", "perimeter channel", "قطاع جبس"]
+        },
+        {
+          "id": "acoustic_ceiling_tile",
+          "label_ar": "بلاطات أسقف",
+          "strong_terms": ["بلاطه سقف", "بلاطات سقف", "ceiling tile", "mineral fiber tile", "بلاطه سقف جبس", "perforated ceiling tile", "acoustic tile", "سقف مستعار", "tegular edge", "صوف معدني سقف", "لوح اسمنتي", "الواح اسمنتيه", "cement board", "fiber cement board"]
+        },
+        {
+          "id": "metal_ceiling",
+          "label_ar": "أسقف معدنية",
+          "strong_terms": ["سقف معدني", "اسقف معدنيه", "metal ceiling", "metal ceiling tile", "بلاطه سقف معدني", "بلاطه سقف معدنيه", "بلاطات سقف معدنيه", "aluminium ceiling", "سقف المنيوم", "اسقف المنيوم", "شرايح المنيوم", "strip ceiling", "linear metal ceiling", "baffle ceiling", "سقف شرايح", "open cell ceiling", "سقف خلوي", "expanded mesh ceiling"],
+          "context_terms": ["مستعار", "معلق", "suspended", "perforated", "مثقب", "600x600", "lay in", "clip in"],
+          "note": "«سقف معدني مستعار» never matched the family's «سقف مستعار» because ARABIC PUTS THE ADJECTIVE BETWEEN THE NOUN AND ITS QUALIFIER, and a two-word phrase term requires the two words to be adjacent. Metal ceilings also genuinely belong to a different supplier — a metal-ceiling roll-former, not a mineral-fibre tile distributor — so they earn an intent of their own rather than a longer alias list on the tile intent.",
+          "facets": [
+            { "name": "dimensions", "supplier_pool_affecting": false },
+            { "name": "finish", "supplier_pool_affecting": true },
+            { "name": "surface", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "access_panel",
+          "label_ar": "فتحات صيانة",
+          "strong_terms": ["access panel", "فتحه صيانه", "فتحات صيانه", "inspection hatch", "باب تفتيش", "access hatch"]
+        },
+        {
+          "id": "wall_protection",
+          "label_ar": "حماية حوائط",
+          "strong_terms": ["wall protection", "حمايه حوايط", "wall guard", "corner guard", "حامي زوايا", "crash rail", "bumper rail", "wall cladding sheet vinyl"]
+        },
+        {
+          "id": "toilet_partition",
+          "label_ar": "قواطع دورات مياه",
+          "strong_terms": ["قواطع دورات مياه", "قواطع حمامات", "toilet partition", "cubicle partition", "compact laminate لقواطع", "head rail", "indicator bolt", "gravity hinge", "ملحقات قواطع دورات المياه", "adjustable leg", "u-channel ستانلس", "رجل adjustable"]
+        },
+        {
+          "id": "raised_floor",
+          "label_ar": "أرضيات مرتفعة",
+          "strong_terms": ["ارضيه مرتفعه", "ارضيات مرتفعه", "raised floor", "access floor", "calcium sulphate panel", "steel cementitious panel", "pedestal floor"]
+        }
       ]
     },
     {
@@ -562,15 +1029,45 @@
         { "name": "material", "supplier_pool_affecting": true },
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "finish", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "resilient_flooring",
+          "label_ar": "أرضيات مرنة",
+          "strong_terms": ["lvt", "بلاطات lvt", "luxury vinyl tile", "vinyl flooring", "homogeneous vinyl", "sheet vinyl", "ارضيات lvt", "spc flooring", "lvt plank", "ارضيات فينيل"]
+        },
+        {
+          "id": "carpet_tile",
+          "label_ar": "موكيت بلاطات",
+          "strong_terms": ["carpet tile", "موكيت بلاطات", "بلاطات موكيت", "loop pile", "cut pile", "level loop", "broadloom carpet", "موكيت"]
+        },
+        {
+          "id": "stone_slab",
+          "label_ar": "أحجار وأسطح",
+          "strong_terms": ["رخام", "جرانيت", "ترافرتين", "marble slab", "granite slab", "travertine", "احجار واسطح", "quartz surface", "كوارتز", "بازلت", "حجر طبيعي"],
+          "term_guards": [
+            {
+              "terms": ["رخام", "جرانيت", "ترافرتين", "marble slab", "granite slab", "travertine", "احجار واسطح", "quartz surface", "كوارتز", "بازلت", "حجر طبيعي"],
+              "blocked_by_head": ["الياف", "fiber", "fibre", "لاصق", "سيلانت", "sealant", "جراوت", "grout", "دهان", "طلاء", "مونه", "منظف", "عزل"],
+              "note": "Stone names the MATERIAL in «ألياف بازلت» and «لاصق رخام»; the product is the fibre and the adhesive."
+            }
+          ]
+        }
       ]
     },
-
     {
       "id": "storage_racking",
       "sector": "WAREHOUSE_LOGISTICS",
       "label_ar": "أنظمة الرفوف والتخزين",
       "strong_terms": ["pallet rack", "pallet racking", "racking system", "rack protector", "shelving bay", "long span shelving", "boltless shelving", "cantilever rack", "cantilever upright", "cantilever arm", "bin storage cabinet", "رفوف تخزين", "رف بالتات", "mezzanine floor", "rack upright", "rack beam"],
       "weak_terms": ["rack", "shelving", "رف", "رفوف", "cantilever", "upright", "beam", "bay", "طبليه", "بالته"],
+      "term_guards": [
+        {
+          "terms": ["cantilever arm", "cantilever", "rack beam", "beam"],
+          "block_any": ["strut", "unistrut", "قناه اسناد", "channel support", "مواسير", "pipe", "hvac", "تكييف"],
+          "note": "A cantilever arm on a STRUT channel is a support bracket, not warehouse racking, and it is bought from the channel-support supplier. Warehouse racking keeps the word whenever the line is about pallets, bays or uprights."
+        }
+      ],
       "context_terms": ["تخزين", "مستودع", "pallet", "حموله", "kg", "galvanized", "storage", "shelves", "مواد طويله", "long materials", "arm", "heavy duty", "بلاستيك"],
       "supplier_terms": ["رفوف", "مستودعات", "racking", "shelving", "تخزين", "storage systems"],
       "preferred_archetypes": ["warehouse_equipment_supplier"],
@@ -618,6 +1115,13 @@
       "label_ar": "معدات ومستلزمات الرفع",
       "strong_terms": ["web sling", "حزام رفع", "chain sling", "سلسله رفع", "wire rope sling", "shackle", "شاكل", "eye bolt", "lifting hook", "خطاف رفع", "lifting beam", "load binder", "ratchet strap", "حزام تثبيت", "beam clamp", "مشبك عارضه", "plate clamp", "ملزمه رفع", "lifting magnet", "مغناطيس رفع", "permanent lifting magnet", "load cell", "crane load cell", "ميزان حموله", "dynamometer", "مقياس حموله", "swivel hoist ring"],
       "weak_terms": ["sling", "rope", "hook", "strap", "حبل", "clamp", "مشبك", "مغناطيس", "magnet"],
+      "term_guards": [
+        {
+          "terms": ["beam clamp", "plate clamp", "clamp"],
+          "block_any": ["strut", "unistrut", "قناه اسناد", "channel support", "مواسير", "pipe hanger", "حامل مواسير", "conduit"],
+          "note": "A beam clamp sold for a STRUT or pipe-support system is a fixing, not rigging equipment; a rigger's beam clamp names a working load instead. Lifting gear keeps the word whenever the line is about slings, hoists or loads."
+        }
+      ],
       "context_terms": ["رفع", "حموله", "ton", "طن", "lifting", "سعه", "rigging", "kg", "crane", "ونش"],
       "supplier_terms": ["معدات رفع", "lifting", "rigging", "سلامه"],
       "preferred_archetypes": ["lifting_equipment_supplier", "material_handling_supplier"],
@@ -659,7 +1163,6 @@
         { "name": "material", "supplier_pool_affecting": true }
       ]
     },
-
     {
       "id": "pumps",
       "sector": "MEP_WATER",
@@ -716,6 +1219,13 @@
         { "name": "diameter", "supplier_pool_affecting": false },
         { "name": "head_pressure", "supplier_pool_affecting": false },
         { "name": "material", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "control_valve",
+          "label_ar": "صمامات تحكم",
+          "strong_terms": ["picv", "pressure independent control valve", "differential pressure control valve", "motorized valve", "motorized 2-way valve", "motorized 3-way valve", "2-way valve", "3-way valve", "balancing valve", "صمام تحكم", "صمام موتور", "صمام موازنه", "zone valve"]
+        }
       ]
     },
     {
@@ -725,6 +1235,14 @@
       "strong_terms": ["ماسوره", "مواسير", "انبوب", "انابيب", "steel pipe", "ppr pipe", "upvc pipe", "hdpe pipe", "فلنجه", "flange", "كوع", "elbow", "coupling"],
       "weak_terms": ["pipe", "piping", "fitting", "وصله", "tee"],
       "context_terms": ["dn", "inch", "بوصه", "سماكه", "مياه", "صرف", "schedule"],
+      "term_guards": [
+        {
+          "terms": ["ماسوره", "مواسير", "انبوب", "انابيب", "pipe", "piping"],
+          "blocked_by_head": ["عزل", "insulation", "حامل", "حوامل", "hanger", "clevis", "support", "bracket", "مثبت", "مثبتات", "clamp", "طوق", "اطواق", "غطاء", "دهان", "طلاء", "شريط", "لاصق", "قاطع", "كاشف", "shoe", "u bolt", "ubolt", "split ring", "ring", "roller", "riser", "saddle", "strut", "anchor", "تثبيت", "قاعده", "كرسي"],
+          "block_any": ["ماسوره كهرباء", "مواسير كهرباء", "تمديدات كهربايي", "emt", "imc", "conduit", "trunking", "gi conduit", "pvc conduit", "flexible conduit"],
+          "note": "Product-head rule: insulation FOR pipes, a hanger FOR pipes and a clamp FOR pipes are not pipes — the pipe is the object, not the purchase. The rule is positional, so «ماسورة معزولة» (pre-insulated pipe) is still a pipe. Separately, electrical conduit shares the word «ماسورة» with water pipe and belongs to the electrical trade."
+        }
+      ],
       "supplier_terms": ["مواسير", "pipes", "fittings", "سباكه"],
       "preferred_archetypes": ["pipe_fittings_supplier", "plumbing_only_supplier"],
       "allowed_archetypes": ["valve_supplier", "pump_supplier", "hvac_supplier", "general_building_materials", "fire_fighting_supplier"],
@@ -732,6 +1250,14 @@
         { "name": "diameter", "supplier_pool_affecting": false },
         { "name": "material", "supplier_pool_affecting": true },
         { "name": "head_pressure", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "pipe_fitting",
+          "label_ar": "وصلات مواسير",
+          "strong_terms": ["butt weld fitting", "butt fusion", "socket fusion", "electrofusion", "end cap", "stub end", "concentric reducer", "eccentric reducer", "reducer concentric", "reducer eccentric", "grooved coupling", "flexible coupling", "mechanical tee", "كوع grooved", "تي grooved", "تي متساوي", "وصله hdpe", "وصله upvc", "وصله ppr", "وصله carbon steel", "long radius elbow", "weldolet", "sockolet"],
+          "weak_terms": ["reducer", "cap", "elbow", "coupler"]
+        }
       ]
     },
     {
@@ -749,6 +1275,13 @@
         { "name": "flow_rate", "supplier_pool_affecting": false },
         { "name": "accuracy", "supplier_pool_affecting": false },
         { "name": "protection_ip", "supplier_pool_affecting": false }
+      ],
+      "term_guards": [
+        {
+          "terms": ["meter", "مقياس", "gauge", "switch"],
+          "block_any": ["energy meter", "عداد طاقه", "kwh", "kvarh", "ammeter", "voltmeter", "امبير", "فولت", "power meter", "عداد كهرباء", "multifunction meter"],
+          "note": "«meter» must not claim electrical metering, which is a switchgear/metering trade with a different supplier pool."
+        }
       ]
     },
     {
@@ -788,7 +1321,7 @@
       "label_ar": "تجهيزات التكييف والتهوية",
       "strong_terms": ["ahu", "وحده مناوله هواء", "air handling unit", "fahu", "fresh air unit", "وحده هواء نقي", "fcu", "fan coil", "vrf", "مكيف", "تكييف", "chiller", "مبرد مياه", "duct", "دكت", "مجري هواء", "exhaust fan", "شفاط", "دايفيوزر", "diffuser", "air outlet", "vav box", "vav", "vav terminal", "package unit", "package ac", "وحده تكييف مجمعه", "split unit", "hepa filter", "فلتر hepa", "bag filter", "فلتر كيسي", "pre filter", "فلتر هواء", "air filter", "fire smoke damper", "smoke damper", "دامبر حريق", "دامبر دخان", "motorized damper", "دامبر هواء", "volume damper", "heat recovery wheel", "منفاخ هواء صناعي", "high velocity blower", "دفايه صناعيه", "space heater"],
       "weak_terms": ["مروحه", "fan", "grille", "cooling", "تبريد", "damper", "دامبر", "filter", "فلتر", "blower", "منفاخ", "دفايه", "heater"],
-      "context_terms": ["هواء", "cfm", "تكييف", "تهويه", "ton", "kw", "air", "ventilation", "tr", "gel seal", "pressure independent", "heat recovery", "actuator", "صناعي"],
+      "context_terms": ["هواء", "cfm", "تكييف", "تهويه", "ton", "kw", "air", "ventilation", "tr", "gel seal", "pressure independent", "heat recovery", "actuator", "صناعي", "l/s", "m3/h", "hp"],
       "supplier_terms": ["تكييف", "hvac", "تهويه", "تبريد"],
       "preferred_archetypes": ["hvac_supplier"],
       "allowed_archetypes": ["electrical_distributor", "pipe_fittings_supplier", "general_building_materials", "data_center_integrator"],
@@ -839,6 +1372,41 @@
             }
           ]
         }
+      ],
+      "intents": [
+        {
+          "id": "air_damper",
+          "label_ar": "دامبرز",
+          "strong_terms": ["volume control damper", "fire damper", "smoke damper", "backdraft damper", "pressure relief damper", "motorized damper", "دامبر", "دامبرز", "vcd damper"],
+          "weak_terms": ["damper"]
+        },
+        {
+          "id": "air_terminal_device",
+          "label_ar": "مخارج هواء",
+          "strong_terms": ["ceiling diffuser", "square ceiling diffuser", "swirl diffuser", "slot diffuser", "jet nozzle", "return air grille", "supply air grille", "egg crate grille", "linear bar grille", "fresh air louver", "مخرج هواء", "مخارج هواء", "محبس هواء", "شبك تكييف"],
+          "weak_terms": ["diffuser", "grille"]
+        },
+        {
+          "id": "duct_accessories",
+          "label_ar": "ملحقات مجاري الهواء",
+          "strong_terms": ["canvas connector", "flexible duct connector", "duct access door", "duct silencer", "sound attenuator", "turning vane", "ملحقات مجاري الهواء", "مجري هواء مرن", "flexible duct"]
+        },
+        {
+          "id": "fan_unit",
+          "label_ar": "مراوح",
+          "strong_terms": ["centrifugal fan", "inline fan", "axial fan", "exhaust fan", "مروحه شفط", "مروحه طرد مركزي", "cabinet fan", "jet fan", "roof fan", "مروحه سقفيه", "مراوح", "مروحه centrifugal", "مروحه inline", "centrifugal cabinet fan"],
+          "weak_terms": ["مروحه"]
+        },
+        {
+          "id": "water_heater",
+          "label_ar": "سخانات مياه",
+          "strong_terms": ["سخان مياه", "سخانات مياه", "water heater", "electric water heater", "calorifier", "heat pump water heater", "سخان كهربايي", "storage water heater"]
+        },
+        {
+          "id": "expansion_vessel",
+          "label_ar": "أوعية ضغط وتمدد",
+          "strong_terms": ["expansion vessel", "وعاء تمدد", "خزان تمدد", "pressure vessel", "diaphragm tank", "buffer tank", "اوعيه ضغط"]
+        }
       ]
     },
     {
@@ -853,9 +1421,35 @@
       "allowed_archetypes": ["pipe_fittings_supplier", "general_building_materials"],
       "facets": [
         { "name": "material", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "wc_sanitaryware",
+          "label_ar": "أدوات صحية",
+          "strong_terms": ["مرحاض", "مراحيض", "close-coupled", "wall-hung", "back-to-wall", "مبوله", "urinal", "حوض غسيل", "washbasin", "wash basin", "مغسله", "bidet", "شطاف", "water closet", "dual flush"]
+        },
+        {
+          "id": "washroom_accessories",
+          "label_ar": "إكسسوارات دورات مياه",
+          "strong_terms": ["حامل مناديل", "jumbo roll", "baby changing station", "soap dispenser", "موزع صابون", "hand dryer", "مجفف ايدي", "اكسسوارات دورات مياه", "paper towel dispenser", "toilet roll holder", "مراه حمام"]
+        },
+        {
+          "id": "accessibility_grab_bar",
+          "label_ar": "مساند ذوي الإعاقة",
+          "strong_terms": ["grab bar", "مسند ذوي الاعاقه", "مساند ذوي الاعاقه", "مقبض امان", "folding grab bar", "drop down rail", "مسند قابل للطي"]
+        },
+        {
+          "id": "floor_drain",
+          "label_ar": "مصارف أرضية",
+          "strong_terms": ["floor drain", "مصرف ارضي", "مصارف ارضيه", "shower drain", "linear shower drain", "gully trap", "مصرف دش", "صفايه ارضيه"]
+        },
+        {
+          "id": "mixer_tap",
+          "label_ar": "خلاطات ومحابس",
+          "strong_terms": ["خلاط", "خلاطات", "mixer tap", "shower mixer", "خلاط دش", "خلاط مغسله", "خلاط مطبخ", "basin mixer", "kitchen mixer", "thermostatic mixer", "single lever mixer", "حنفيه مغسله", "faucet", "حنفيه حوض"]
+        }
       ]
     },
-
     {
       "id": "fire_fighting",
       "sector": "FIRE",
@@ -894,8 +1488,14 @@
             {
               "id": "fire_sprinkler",
               "label_ar": "رشاشات الحريق",
-              "strong_terms": ["sprinkler", "رشاش حريق", "esfr", "upright sprinkler", "pendent sprinkler"],
-              "weak_terms": ["رشاش"],
+              "strong_terms": ["sprinkler", "sprinklers", "رشاش", "رشاشات", "رشاش حريق", "رشاشات حريق", "esfr", "upright sprinkler", "pendent sprinkler", "sidewall sprinkler", "concealed sprinkler", "رشاش معلق", "رشاش قايم", "رشاش جانبي", "رشاش مخفي", "رشاش هبوطي"],
+              "term_guards": [
+                {
+                  "terms": ["sprinkler", "sprinklers", "رشاش", "رشاشات"],
+                  "block_any": ["ري", "تنقيط", "حديقه", "حدايق", "مسطحات خضراء", "نجيل", "زراعه", "مشتل", "irrigation", "pop-up", "pop up", "popup", "lawn", "garden", "turf", "landscape", "gear drive", "impact sprinkler", "rotor", "nozzle radius", "arc adjustable"],
+                  "note": "FIRE AND IRRIGATION BOTH SELL SPRINKLERS; THE TRADES DO NOT OVERLAP. «رشاش» and `sprinkler` are now BOTH strong, because a concept's right to decide must never depend on which language the buyer writes in — the previous split (strong `sprinkler`, weak «رشاش») produced the worst possible inversion: every irrigation pop-up head resolved as a FIRE sprinkler at 0.90 while real fire sprinklers written in Arabic resolved to nothing. Parity alone would only move the error, so the shared word is guarded by trade: a landscape or drip context disowns the word and irrigation_emitter claims it with its own positive vocabulary. A fire-sprinkler line names its head type or K-factor, never a lawn."
+                }
+              ],
               "facets": [
                 { "name": "head_pressure", "supplier_pool_affecting": false },
                 { "name": "standard", "supplier_pool_affecting": true }
@@ -925,6 +1525,13 @@
             }
           ]
         }
+      ],
+      "term_guards": [
+        {
+          "terms": ["حريق", "اطفاء", "حنفيه"],
+          "block_any": ["مقاوم للحريق", "مقاومه للحريق", "مقاوم حريق", "fire rated", "fire resistant", "fire retardant", "flame retardant", "مقاوم للهب", "مانع للهب"],
+          "note": "«حريق» used adjectivally states a fire RATING of some other product (a board, a door, a cable). A rating is not a fire-fighting product."
+        }
       ]
     },
     {
@@ -940,16 +1547,34 @@
       "facets": [
         { "name": "standard", "supplier_pool_affecting": true },
         { "name": "voltage", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "fire_alarm_panel",
+          "label_ar": "لوحات إنذار حريق",
+          "strong_terms": ["لوحه انذار حريق", "لوحات انذار حريق", "fire alarm panel", "fire alarm control panel", "addressable fire alarm panel", "loop panel", "repeater panel", "conventional panel"]
+        },
+        {
+          "id": "fire_alarm_module",
+          "label_ar": "وحدات إنذار حريق",
+          "strong_terms": ["input output module", "io module", "monitor module", "control module", "وحده ادخال اخراج", "addressable module", "isolator module", "loop isolator"]
+        }
       ]
     },
-
     {
       "id": "switchgear_panels",
       "sector": "ELECTRICAL",
       "label_ar": "لوحات التوزيع والقواطع",
       "strong_terms": ["لوحه توزيع", "لوحه كهرباء", "لوحه تحكم", "mdb", "smdb", "mcc panel", "ring main unit", "rmu", "switchgear", "mccb", "mcb", "acb", "capacitor bank", "بنك مكثفات", "active harmonic filter", "busduct", "بس دكت", "ats panel", "changeover switch", "كونتاكتور", "panel board", "distribution board", "lv mdb"],
-      "weak_terms": ["لوحه", "panel", "قاطع", "breaker", "contactor", "ats", "capacitor", "مكثفات"],
-      "context_terms": ["كهرباء", "كهربايي", "a", "ka", "kv", "form", "ip", "withdrawable", "توزيع", "kvar", "pf correction", "bank"],
+      "weak_terms": ["لوحه", "panel", "قاطع", "breaker", "contactor", "ats", "capacitor", "مكثفات", "enclosure", "علبه"],
+      "context_terms": ["كهرباء", "كهربايي", "a", "ka", "kv", "form", "ip", "withdrawable", "توزيع", "kvar", "pf correction", "bank", "electrical", "ss316", "ss304", "stainless", "sheet steel", "mild steel", "grp", "din rail"],
+      "term_guards": [
+        {
+          "terms": ["enclosure", "علبه"],
+          "block_any": ["server", "rack unit", "19 inch", "19 بوصه", "u rack", "data cabinet", "خزانه شبكه", "network rack", "outdoor cabinet telecom"],
+          "note": "An ENCLOSURE IS A PRODUCT WITH A TRADE, not a shape. Electrical enclosures were the single largest unusable group in the held-out corpus — 7,824 rows that matched the bare word «enclosure» owned by the IT-rack family, found no rack context, and settled into a family = null semantic recovery: nominally resolved, countable as coverage, and unable to name a single supplier. Giving the electrical sector its own claim on the word makes those lines poolable; the guard hands the word back to racks_enclosures whenever the line is actually about a server cabinet."
+        }
+      ],
       "supplier_terms": ["لوحات كهرباييه", "switchgear", "كهرباييه", "electrical"],
       "preferred_archetypes": ["switchgear_supplier", "electrical_distributor"],
       "allowed_archetypes": ["transformer_supplier", "cable_supplier", "generator_supplier", "ups_supplier"],
@@ -957,6 +1582,81 @@
         { "name": "capacity", "supplier_pool_affecting": false },
         { "name": "voltage", "supplier_pool_affecting": true },
         { "name": "protection_ip", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "mccb",
+          "label_ar": "قاطع MCCB",
+          "strong_terms": ["mccb", "قاطع mccb", "moulded case circuit breaker", "molded case circuit breaker"]
+        },
+        {
+          "id": "mcb",
+          "label_ar": "قاطع MCB",
+          "strong_terms": ["mcb", "قاطع mcb", "miniature circuit breaker", "curve b", "curve c", "curve d"]
+        },
+        {
+          "id": "acb",
+          "label_ar": "قاطع هوائي ACB",
+          "strong_terms": ["acb", "air circuit breaker", "قاطع هوايي"]
+        },
+        {
+          "id": "residual_current_device",
+          "label_ar": "حماية تسرب أرضي",
+          "strong_terms": ["rcbo", "rccb", "elcb", "حمايه تسرب ارضي", "قاطع تسرب ارضي", "earth leakage circuit breaker", "residual current device", "residual current breaker"],
+          "weak_terms": ["rcd"]
+        },
+        {
+          "id": "isolator_switch",
+          "label_ar": "مفتاح عزل",
+          "strong_terms": ["مفتاح عزل", "مفاتيح عزل", "switch disconnector", "load break switch", "fuse switch disconnector", "isolator 2p", "isolator 3p", "isolator 4p"]
+        },
+        {
+          "id": "transfer_switch",
+          "label_ar": "مفتاح تحويل",
+          "strong_terms": ["automatic transfer switch", "مفتاح تحويل", "مفاتيح تحويل", "changeover switch", "manual transfer switch", "ats 3p", "ats 4p"],
+          "weak_terms": ["ats", "mts"]
+        },
+        {
+          "id": "contactor",
+          "label_ar": "كونتاكتور",
+          "strong_terms": ["contactor", "كونتاكتور", "magnetic contactor", "overload relay", "thermal overload"]
+        },
+        {
+          "id": "protection_relay",
+          "label_ar": "ريليه حماية",
+          "strong_terms": ["protection relay", "ريليه حمايه", "ريليهات حمايه", "differential relay", "overcurrent relay", "earth fault relay", "multifunction protection relay", "motor protection relay"],
+          "weak_terms": ["relay", "ريليه"]
+        },
+        {
+          "id": "current_transformer",
+          "label_ar": "محول تيار",
+          "strong_terms": ["current transformer", "محول تيار", "محولات تيار", "voltage transformer", "محول جهد قياس", "ct class", "summation ct", "core balance ct"]
+        },
+        {
+          "id": "busbar",
+          "label_ar": "قضبان نحاس",
+          "strong_terms": ["busbar", "بس بار", "قضيب نحاس", "قضبان نحاس", "copper busbar", "aluminium busbar", "busbar chamber", "قضيب توزيع"]
+        },
+        {
+          "id": "distribution_board",
+          "label_ar": "لوحة توزيع نهائية",
+          "strong_terms": ["distribution board", "لوحه توزيع نهاييه", "لوحات توزيع نهاييه", "consumer unit", "لوحه توزيع فرعيه", "final distribution board", "way distribution board"]
+        },
+        {
+          "id": "empty_enclosure",
+          "label_ar": "لوحات وصناديق فارغة",
+          "strong_terms": ["لوحه فارغه", "لوحات فارغه", "empty enclosure", "enclosure grp", "enclosure sheet steel", "grp enclosure", "صندوق توزيع فارغ", "wall mounted enclosure", "electrical enclosure", "علبه كهربايي", "صندوق كهربايي", "floor standing enclosure", "wall mount enclosure", "enclosure stainless steel", "stainless steel enclosure"]
+        },
+        {
+          "id": "electrical_metering",
+          "label_ar": "أجهزة قياس كهربائية",
+          "strong_terms": ["energy meter", "عداد طاقه", "عداد كهرباء", "kwh meter", "ammeter", "voltmeter", "multifunction meter", "power meter", "اجهزه قياس كهربايي", "power analyzer", "power quality analyzer"]
+        },
+        {
+          "id": "capacitor_bank",
+          "label_ar": "تحسين معامل القدرة",
+          "strong_terms": ["capacitor bank", "بنك مكثفات", "automatic capacitor bank", "power factor correction", "تحسين معامل القدره", "detuned reactor", "kvar"]
+        }
       ]
     },
     {
@@ -982,6 +1682,13 @@
       "strong_terms": ["كابل", "كيبل", "كابلات", "اسلاك كهرباء", "xlpe", "swa", "كابل نحاس", "كابل مدرع", "كابل مقاوم حريق", "fire resistant cable", "control cable", "كابل تحكم", "lszh cable"],
       "weak_terms": ["cable", "wire", "lszh"],
       "context_terms": ["نحاس", "مساحه", "مم2", "kv", "copper", "armored", "iec", "core"],
+      "term_guards": [
+        {
+          "terms": ["كابل", "كابلات", "كيبل", "cable"],
+          "block_any": ["الياف", "fiber", "fibre", "optic", "om1", "om2", "om3", "om4", "os1", "os2", "multimode", "single mode", "singlemode", "cat6", "cat6a", "cat7", "cat5e", "utp", "ftp", "sftp", "patch cord", "باتش كورد"],
+          "note": "Fibre and structured cabling are a different trade and a different supplier pool from power cable. «كابل» stays fully decisive for copper power cable — it only steps aside when the line itself says fibre or data."
+        }
+      ],
       "supplier_terms": ["كابلات", "cables", "كهرباييه", "electrical"],
       "preferred_archetypes": ["cable_supplier", "electrical_distributor"],
       "allowed_archetypes": ["switchgear_supplier", "network_integrator", "general_trading"],
@@ -990,6 +1697,44 @@
         { "name": "voltage", "supplier_pool_affecting": true },
         { "name": "material", "supplier_pool_affecting": true },
         { "name": "standard", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "lv_power_cable",
+          "label_ar": "كابل قدرة جهد منخفض",
+          "strong_terms": ["كابل جهد منخفض", "lv power cable", "low voltage cable", "al/xlpe/pvc", "cu/xlpe/pvc", "nyy", "n2xy", "0.6/1kv", "600/1000v"],
+          "weak_terms": ["lv"],
+          "term_guards": [
+            {
+              "terms": ["كابل جهد منخفض", "lv power cable", "low voltage cable", "al/xlpe/pvc", "cu/xlpe/pvc", "nyy", "n2xy", "0.6/1kv", "600/1000v"],
+              "block_any": ["control", "instrumentation", "mv", "cts", "11kv", "22kv", "33kv", "6.6kv", "fire resistant", "ph120", "ph30", "fe180", "الياف", "fiber"],
+              "note": "Low voltage is the DEFAULT reading of a power cable, so it must yield whenever the line names a more specific type. Without this, «كابل Cu/PVC Control» reads as LV power cable and the RFQ reaches the wrong cable specialists."
+            }
+          ]
+        },
+        {
+          "id": "mv_power_cable",
+          "label_ar": "كابل جهد متوسط",
+          "strong_terms": ["كابل جهد متوسط", "mv cable", "mv power cable", "medium voltage cable", "xlpe/cts", "cts/pvc", "11kv", "22kv", "33kv", "6.6kv", "mv", "كابل mv", "mv cu", "mv al"],
+          "weak_terms": ["mv", "cts"]
+        },
+        {
+          "id": "control_cable",
+          "label_ar": "كابل تحكم",
+          "strong_terms": ["control cable", "كابل تحكم", "كابل سيطره", "كوابل تحكم"],
+          "weak_terms": ["control"]
+        },
+        {
+          "id": "fire_resistant_cable",
+          "label_ar": "كابل مقاوم للحريق",
+          "strong_terms": ["fire resistant cable", "كابل مقاوم للحريق", "كابل مقاوم حريق", "fire rated cable", "circuit integrity cable", "ph120", "ph30", "fe180", "كوابل مقاومه للحريق", "fire resistant", "مقاوم للحريق lszh"]
+        },
+        {
+          "id": "instrumentation_cable",
+          "label_ar": "كابل أجهزة قياس",
+          "strong_terms": ["instrumentation cable", "كابل اجهزه قياس", "كابل انسترومنت", "twisted pair", "individual shielded", "overall shielded", "pair shielded", "كوابل اجهزه"],
+          "weak_terms": ["instrumentation"]
+        }
       ]
     },
     {
@@ -1006,6 +1751,50 @@
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "material", "supplier_pool_affecting": true },
         { "name": "protection_ip", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "cable_tray",
+          "label_ar": "حوامل كابلات (تراي)",
+          "strong_terms": ["cable tray", "تراي كابلات", "مجري كابلات", "حامل كابلات", "حوامل كابلات", "perforated tray", "wire mesh tray"]
+        },
+        {
+          "id": "cable_ladder",
+          "label_ar": "سلالم كابلات",
+          "strong_terms": ["cable ladder", "سلم كابلات", "سلالم كابلات", "ladder type tray"]
+        },
+        {
+          "id": "cable_trunking",
+          "label_ar": "قنوات كابلات",
+          "strong_terms": ["trunking", "gi trunking", "قناه كابلات", "قنوات كابلات", "pvc trunking", "floor trunking", "skirting trunking"]
+        },
+        {
+          "id": "cable_support_accessories",
+          "label_ar": "ملحقات حوامل كابلات",
+          "strong_terms": ["ملحق cable tray", "ملحقات حوامل كابلات", "tray cross", "tray tee", "tray bend", "tray riser", "tray reducer", "tray coupler", "tray cover", "غطاء تراي", "ladder bend", "tray support bracket"]
+        },
+        {
+          "id": "electrical_conduit",
+          "label_ar": "مواسير وتمديدات كهربائية",
+          "strong_terms": ["ماسوره كهرباء", "مواسير كهرباء", "conduit كهرباء", "emt conduit", "imc conduit", "emt", "imc", "rigid steel conduit", "flexible metallic conduit", "flexible conduit", "gi conduit", "pvc conduit", "upvc conduit", "twinwall conduit", "hdpe twinwall", "تمديدات كهربايي"],
+          "weak_terms": ["conduit"]
+        },
+        {
+          "id": "junction_box",
+          "label_ar": "علب كهربائية",
+          "strong_terms": ["junction box", "علبه كهرباء", "علب كهربايي", "pull box", "علبه تفريع", "adaptable box", "junction box gi"]
+        },
+        {
+          "id": "cable_gland",
+          "label_ar": "جلاند كابلات",
+          "strong_terms": ["cable gland", "جلاند كابل", "جلاند", "brass gland", "nickel plated gland", "gland m"]
+        },
+        {
+          "id": "cable_termination",
+          "label_ar": "نهايات كابلات",
+          "strong_terms": ["cable lug", "copper lug", "bimetal lug", "bimetallic lug", "lug al/cu", "كوسه نحاس", "نهايه كابل", "نهايات كابلات", "termination kit", "heat shrink", "cold shrink", "cable joint", "copper ferrule", "كوسه", "mechanical shear bolt lug", "copper lug tinned", "compression lug", "pin lug", "fork lug"],
+          "weak_terms": ["lug"]
+        }
       ]
     },
     {
@@ -1021,6 +1810,23 @@
       "facets": [
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "material", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "earth_rod",
+          "label_ar": "قضبان تأريض",
+          "strong_terms": ["earth rod", "قضيب تاريض", "سيخ تاريض", "copper bonded rod", "earth electrode", "اقطاب تاريض", "موصل نحاس عاري", "bare copper conductor", "bare copper", "نحاس عاري"]
+        },
+        {
+          "id": "earthing_accessories",
+          "label_ar": "ملحقات تأريض",
+          "strong_terms": ["earth clamp", "bi-metallic connector", "bimetallic connector", "موصل تاريض", "ملحقات تاريض", "earth bar", "earth pit", "غرفه تاريض", "exothermic weld", "rod coupler", "driving head", "inspection pit", "earth boss", "exothermic welding mold", "test clamp", "وصله اختبار", "وصله شريط", "tape to tape connector", "tape clip"]
+        },
+        {
+          "id": "lightning_protection",
+          "label_ar": "حماية من الصواعق",
+          "strong_terms": ["lightning protection", "مانعه صواعق", "مانع صواعق", "air terminal", "down conductor", "موصل هبوط", "copper tape", "شريط نحاس", "early streamer emission"]
+        }
       ]
     },
     {
@@ -1084,6 +1890,55 @@
         { "name": "power_kw", "supplier_pool_affecting": false },
         { "name": "protection_ip", "supplier_pool_affecting": false },
         { "name": "application", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "downlight",
+          "label_ar": "إنارة داخلية (داون لايت)",
+          "strong_terms": ["downlight", "داون لايت", "سبوت لايت", "spot light", "recessed downlight", "surface downlight"]
+        },
+        {
+          "id": "linear_light",
+          "label_ar": "إنارة خطية",
+          "strong_terms": ["linear led", "linear led fixture", "linear batten", "اناره خطيه", "batten fixture", "led strip", "شريط ليد", "trunking light"]
+        },
+        {
+          "id": "led_panel_light",
+          "label_ar": "بانل إنارة",
+          "strong_terms": ["led panel", "بانل ليد", "panel light", "اناره بانل", "backlit panel"]
+        },
+        {
+          "id": "floodlight",
+          "label_ar": "كشافات خارجية",
+          "strong_terms": ["floodlight", "flood light", "كشاف اناره", "كشافات", "projector floodlight"]
+        },
+        {
+          "id": "street_light",
+          "label_ar": "إنارة طرق",
+          "strong_terms": ["street light", "اناره شوارع", "اناره طرق", "road luminaire", "post top luminaire"]
+        },
+        {
+          "id": "lighting_pole",
+          "label_ar": "أعمدة إنارة",
+          "strong_terms": ["عمود اناره", "اعمده اناره", "lighting pole", "lamp post", "high mast", "عمود مجلفن اناره"]
+        },
+        {
+          "id": "highbay_light",
+          "label_ar": "إنارة صناعية",
+          "strong_terms": ["high bay", "highbay", "low bay", "اناره صناعيه"]
+        },
+        {
+          "id": "emergency_light",
+          "label_ar": "إنارة طوارئ",
+          "strong_terms": ["emergency light", "اناره طوارى", "exit sign", "لوحه مخرج", "self contained emergency", "emergency exit light"]
+        },
+        {
+          "id": "track_light",
+          "label_ar": "إنارة تراك",
+          "strong_terms": ["track light", "track lighting", "تراك لايت", "اناره تراك", "spot track", "track spotlight", "مسار اناره", "lighting track", "track system اناره", "3 circuit track", "magnetic track light"],
+          "context_terms": ["w", "k", "lm", "اناره", "اضاءه", "lighting", "cri", "beam angle"],
+          "note": "Track lighting was the largest single unusable category in the lighting sector of the held-out corpus (1,512 of 1,980 rows), and the Arabic transliteration «تراك لايت» is how the trade actually writes it — the family already knew «داون لايت» and «سبوت لايت» in the same transliterated style, so its absence was an omission rather than a design choice. Facets are inherited from the family, as every sibling luminaire intent does."
+        }
       ]
     },
     {
@@ -1099,6 +1954,18 @@
       "facets": [
         { "name": "voltage", "supplier_pool_affecting": true },
         { "name": "protection_ip", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "socket_outlet",
+          "label_ar": "مخارج كهربائية",
+          "strong_terms": ["socket outlet", "مقبس", "مقابس", "مخرج كهرباء", "مخارج كهرباء", "wall socket", "industrial socket", "usb socket", "فيش كهرباء", "افياش", "مخرج مكيف"]
+        },
+        {
+          "id": "light_switch",
+          "label_ar": "مفاتيح إنارة",
+          "strong_terms": ["مفتاح اناره", "مفاتيح اناره", "light switch", "gang switch", "one gang", "two gang", "intermediate switch", "two way switch", "dimmer switch"]
+        }
       ]
     },
     {
@@ -1114,14 +1981,26 @@
       "facets": [
         { "name": "voltage", "supplier_pool_affecting": true },
         { "name": "range_m", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "bms_field_sensor",
+          "label_ar": "حساسات BMS",
+          "strong_terms": ["حساس bms", "bms sensor", "حساسات bms", "co2 sensor", "حساس co2", "duct sensor", "حساس مجري هواء", "humidity sensor", "حساس رطوبه", "differential pressure sensor", "حساس جوده هواء", "voc sensor", "حساس voc", "حساس ضغط تفاضلي", "immersion sensor", "حساس تيار", "حساس منسوب"]
+        },
+        {
+          "id": "bms_controller",
+          "label_ar": "وحدات تحكم BMS",
+          "strong_terms": ["ddc controller", "bms controller", "وحده تحكم bms", "bacnet controller", "lonworks controller", "i/o point controller"],
+          "weak_terms": ["ddc"]
+        }
       ]
     },
-
     {
       "id": "head_protection",
       "sector": "SAFETY_PPE",
       "label_ar": "حماية الرأس",
-      "strong_terms": ["خوذه سلامه", "خوذه", "hard hat", "safety helmet", "قبعه حمايه", "bump cap", "خوذه صناعيه"],
+      "strong_terms": ["حمايه راس", "head protection", "مهمات حمايه الراس"],
       "weak_terms": ["helmet", "cap", "chin strap"],
       "context_terms": ["سلامه", "حمايه", "تهويه", "en", "ansi", "صناعيه"],
       "supplier_terms": ["مهمات سلامه", "سلامه", "ppe", "safety"],
@@ -1130,13 +2009,34 @@
       "facets": [
         { "name": "standard", "supplier_pool_affecting": true },
         { "name": "protection_class", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "safety_helmet",
+          "label_ar": "خوذة السلامة",
+          "strong_terms": ["خوذه سلامه", "خوذه صناعيه", "خوذه", "hard hat", "safety helmet"],
+          "weak_terms": ["helmet"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "protection_class", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "bump_cap",
+          "label_ar": "قبعة الحماية",
+          "strong_terms": ["قبعه حمايه", "bump cap", "قبعه واقيه", "قبعه سلامه"],
+          "weak_terms": ["cap"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true }
+          ]
+        }
       ]
     },
     {
       "id": "eye_face_protection",
       "sector": "SAFETY_PPE",
       "label_ar": "حماية العين والوجه",
-      "strong_terms": ["واقي وجه", "نظاره حمايه", "نظارات حمايه", "نظاره امان", "نظارات امان", "نظاره سلامه", "safety goggle", "safety glasses", "face shield", "عدسه بديله", "درع وجه", "welding shield", "قناع لحام"],
+      "strong_terms": ["حمايه العين", "حمايه الوجه", "eye protection", "face protection"],
       "weak_terms": ["goggles", "نظاره", "نظارات", "shield", "عدسه", "visor"],
       "context_terms": ["سلامه", "امان", "حمايه", "بولي كربونيت", "anti fog", "en", "ansi", "وجه", "عين"],
       "supplier_terms": ["مهمات سلامه", "سلامه", "ppe", "safety"],
@@ -1145,25 +2045,85 @@
       "facets": [
         { "name": "standard", "supplier_pool_affecting": true },
         { "name": "protection_class", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "safety_eyewear",
+          "label_ar": "نظارات السلامة",
+          "strong_terms": ["نظاره حمايه", "نظارات حمايه", "نظاره امان", "نظارات امان", "نظاره سلامه", "safety goggle", "safety glasses", "عدسه بديله"],
+          "weak_terms": ["goggles", "نظاره", "نظارات", "عدسه"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "protection_class", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "face_shield",
+          "label_ar": "واقي الوجه",
+          "strong_terms": ["واقي وجه", "face shield", "درع وجه"],
+          "weak_terms": ["shield", "visor"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true }
+          ]
+        },
+        {
+          "id": "welding_face_shield",
+          "label_ar": "أقنعة اللحام",
+          "strong_terms": ["قناع لحام", "welding shield", "welding helmet", "خوذه لحام", "اقنعه لحام"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true }
+          ]
+        }
       ]
     },
     {
       "id": "hearing_protection",
       "sector": "SAFETY_PPE",
       "label_ar": "حماية السمع",
-      "strong_terms": ["واقي سمع", "سداده اذن", "سدادات اذن", "ear muff", "earmuff", "ear plug", "earplug", "حمايه سمع"],
+      "strong_terms": ["hearing protection", "مهمات حمايه السمع"],
       "weak_terms": ["سمع", "hearing"],
       "context_terms": ["سلامه", "صناعي", "db", "snr", "en", "قابله لاعاده الاستخدام"],
       "supplier_terms": ["مهمات سلامه", "سلامه", "ppe", "safety"],
       "preferred_archetypes": ["ppe_supplier", "safety_equipment_supplier"],
       "allowed_archetypes": ["industrial_tools_supplier", "general_trading"],
-      "facets": [{ "name": "standard", "supplier_pool_affecting": true }]
+      "facets": [
+        { "name": "standard", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "ear_plug",
+          "label_ar": "سدادات الأذن",
+          "strong_terms": ["سداده اذن", "سدادات اذن", "ear plug", "earplug"],
+          "weak_terms": ["plug"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true }
+          ]
+        },
+        {
+          "id": "ear_muff",
+          "label_ar": "كمّامات الأذن الواقية",
+          "strong_terms": ["ear muff", "earmuff", "غطاء اذن", "واقي اذن"],
+          "weak_terms": ["muff"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true }
+          ]
+        },
+        {
+          "id": "hearing_protector",
+          "label_ar": "واقي السمع (غير محدد النوع)",
+          "strong_terms": ["واقي سمع", "حمايه سمع", "hearing protector"],
+          "weak_terms": ["سمع", "hearing"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true }
+          ]
+        }
+      ]
     },
     {
       "id": "respiratory_protection",
       "sector": "SAFETY_PPE",
       "label_ar": "حماية الجهاز التنفسي",
-      "strong_terms": ["قناع تنفس", "جهاز تنفس", "papr", "كمامه", "respirator", "half mask", "full face mask", "مرشح غازات", "فلتر جسيمات", "particulate filter", "scba", "twin filter"],
+      "strong_terms": ["حمايه تنفس", "respiratory protection", "مهمات حمايه التنفس"],
       "weak_terms": ["قناع", "mask", "تنفس", "مرشح"],
       "context_terms": ["تنفس", "غازات", "جسيمات", "سلامه", "filter", "en", "p3", "abek"],
       "supplier_terms": ["مهمات سلامه", "سلامه", "ppe", "safety"],
@@ -1172,13 +2132,53 @@
       "facets": [
         { "name": "standard", "supplier_pool_affecting": true },
         { "name": "protection_class", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "respirator_mask",
+          "label_ar": "أقنعة وكمّامات التنفس",
+          "strong_terms": ["قناع تنفس", "كمامه", "كمامه نصف وجه", "respirator", "half mask", "full face mask", "نصف قناع", "قناع كامل للوجه"],
+          "weak_terms": ["قناع", "mask", "كمامه"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "protection_class", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "breathing_apparatus",
+          "label_ar": "أجهزة التنفس المستقلة",
+          "strong_terms": ["جهاز تنفس", "scba", "papr", "جهاز تنفس مستقل", "airline breathing apparatus"],
+          "weak_terms": ["تنفس"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "capacity", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "respirator_filter",
+          "label_ar": "مرشحات وفلاتر التنفس",
+          "strong_terms": ["مرشح غازات", "فلتر جسيمات", "particulate filter", "twin filter", "فلتر كمامه", "خرطوشه فلتر"],
+          "weak_terms": ["مرشح", "filter"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "protection_class", "supplier_pool_affecting": false }
+          ],
+          "term_guards": [
+            {
+              "terms": ["filter", "مرشح", "فلتر", "cartridge", "خرطوشه"],
+              "require_any": ["تنفس", "respirator", "كمامه", "قناع", "mask", "p1", "p2", "p3", "abek", "abe", "غازات", "ابخره", "جسيمات", "papr", "scba", "breathing", "نصف وجه", "كامل الوجه"],
+              "block_any": ["ري", "irrigation", "تنقيط", "drip", "مياه", "هواء", "hvac", "مكيف", "زيت", "oil", "وقود", "fuel", "hepa", "disc", "شبكي", "رملي", "مضخه"],
+              "note": "A filter is only a respirator filter with respiratory evidence. Irrigation, HVAC, oil and fuel filters are entirely different trades that share the bare word «filter»."
+            }
+          ]
+        }
       ]
     },
     {
       "id": "protective_clothing",
       "sector": "SAFETY_PPE",
       "label_ar": "ملابس الحماية",
-      "strong_terms": ["ستره عاكسه", "سديري عاكس", "صدريه عاكسه", "بدله عمل", "بدله حمايه", "بدله مقاومه للهب", "fr coverall", "coverall", "flame retardant coverall", "معطف مطر", "ستره تبريد", "مريله جلديه", "hi-vis vest", "reflective vest", "ملابس عمل", "workwear", "بدله حمايه كيمياييه", "personal protective equipment", "ppe"],
+      "strong_terms": ["ملابس عمل", "workwear", "ملابس حمايه", "protective clothing", "personal protective equipment", "ppe"],
       "weak_terms": ["ستره", "بدله", "سديري", "صدريه", "vest", "apron", "مريله", "overall", "كم"],
       "context_terms": ["سلامه", "امان", "حمايه", "عاكسه", "عاكس", "class 3", "مقاوم للهب", "مقاومه للهب", "لهب", "flame retardant", "كيمياييه", "en", "عمل"],
       "supplier_terms": ["مهمات سلامه", "ملابس عمل", "ppe", "workwear", "سلامه"],
@@ -1187,25 +2187,89 @@
       "facets": [
         { "name": "standard", "supplier_pool_affecting": true },
         { "name": "protection_class", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "coverall",
+          "label_ar": "بدلات العمل والحماية",
+          "strong_terms": ["بدله عمل", "بدله حمايه", "بدله مقاومه للهب", "بدله حمايه كيمياييه", "fr coverall", "coverall", "flame retardant coverall", "افرول"],
+          "weak_terms": ["بدله", "overall"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "protection_class", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "hi_vis_vest",
+          "label_ar": "السُتر العاكسة",
+          "strong_terms": ["ستره عاكسه", "سديري عاكس", "صدريه عاكسه", "hi-vis vest", "reflective vest", "ستره سلامه"],
+          "weak_terms": ["ستره", "سديري", "صدريه", "vest"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "protection_class", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "work_apron",
+          "label_ar": "المرايل الواقية",
+          "strong_terms": ["مريله جلديه", "مريله لحام", "مريله حمايه", "leather apron", "welding apron"],
+          "weak_terms": ["apron", "مريله"],
+          "facets": [
+            { "name": "material", "supplier_pool_affecting": true }
+          ]
+        },
+        {
+          "id": "weather_protective_clothing",
+          "label_ar": "ملابس الحماية من الأجواء",
+          "strong_terms": ["معطف مطر", "ستره تبريد", "rain coat", "cooling vest", "بدله مطر"],
+          "weak_terms": ["معطف"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true }
+          ]
+        }
       ]
     },
     {
       "id": "safety_footwear",
       "sector": "SAFETY_PPE",
       "label_ar": "أحذية السلامة",
-      "strong_terms": ["حذاء سلامه", "safety boot", "safety shoe", "rubber boot", "pvc boot", "بوت سلامه", "جزمه pvc", "حذاء صناعي"],
+      "strong_terms": ["احذيه سلامه", "safety footwear", "حمايه القدم", "foot protection"],
       "weak_terms": ["جزمه", "boot", "حذاء", "shoe"],
       "context_terms": ["سلامه", "مقاومه", "steel toe", "en", "كيمياييه", "صناعي"],
       "supplier_terms": ["مهمات سلامه", "احذيه سلامه", "ppe", "safety"],
       "preferred_archetypes": ["ppe_supplier", "safety_equipment_supplier"],
       "allowed_archetypes": ["industrial_tools_supplier", "general_trading"],
-      "facets": [{ "name": "standard", "supplier_pool_affecting": true }]
+      "facets": [
+        { "name": "standard", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "safety_boot",
+          "label_ar": "أحذية السلامة",
+          "strong_terms": ["حذاء سلامه", "بوت سلامه", "حذاء صناعي", "safety boot", "safety shoe", "steel toe boot", "حذاء بمقدمه حديديه"],
+          "weak_terms": ["حذاء", "shoe", "boot"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "protection_class", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "rubber_boot",
+          "label_ar": "الجزم المطاطية وPVC",
+          "strong_terms": ["جزمه pvc", "pvc boot", "rubber boot", "جزمه مطاطيه", "حذاء مطاطي", "جزمه بلاستيكيه"],
+          "weak_terms": ["جزمه"],
+          "facets": [
+            { "name": "material", "supplier_pool_affecting": true },
+            { "name": "standard", "supplier_pool_affecting": true }
+          ]
+        }
+      ]
     },
     {
       "id": "hand_protection",
       "sector": "SAFETY_PPE",
       "label_ar": "حماية اليدين",
-      "strong_terms": ["قفاز", "قفازات", "قفاز نيتريل", "قفاز جلدي", "قفاز لحام", "cut resistant glove", "قفاز مضاد للاهتزاز", "قفاز كيميايي", "safety glove"],
+      "strong_terms": ["حمايه اليد", "hand protection", "حمايه اليدين"],
       "weak_terms": ["glove", "gloves"],
       "context_terms": ["سلامه", "حمايه", "نيتريل", "جلدي", "cut level", "en", "يد"],
       "supplier_terms": ["مهمات سلامه", "قفازات", "ppe", "safety"],
@@ -1214,13 +2278,34 @@
       "facets": [
         { "name": "standard", "supplier_pool_affecting": true },
         { "name": "material", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "safety_gloves",
+          "label_ar": "قفازات السلامة",
+          "strong_terms": ["قفاز", "قفازات", "قفاز نيتريل", "قفازات نيتريل", "قفاز جلدي", "قفازات جلديه", "قفاز كيميايي", "قفاز مضاد للاهتزاز", "cut resistant glove", "safety glove"],
+          "weak_terms": ["glove", "gloves"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "material", "supplier_pool_affecting": true }
+          ]
+        },
+        {
+          "id": "welding_gloves",
+          "label_ar": "قفازات اللحام",
+          "strong_terms": ["قفاز لحام", "قفازات لحام", "welding glove", "welding gauntlet"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "material", "supplier_pool_affecting": true }
+          ]
+        }
       ]
     },
     {
       "id": "fall_protection",
       "sector": "SAFETY_PPE",
       "label_ar": "الحماية من السقوط",
-      "strong_terms": ["حزام سلامه", "حزام امان", "full body harness", "harness", "lanyard", "حبل مزدوج", "ممتص صدمه", "shock absorber", "srl", "جهاز سحب ذاتي", "self retracting lifeline", "خط حياه", "horizontal lifeline", "نقطه تثبيت", "anchor point", "rope access", "descender", "carabiner", "كارابينر", "fall arrest", "twin lanyard"],
+      "strong_terms": ["حمايه من السقوط", "fall protection", "مهمات العمل على الارتفاعات"],
       "weak_terms": ["حزام", "حبل", "lifeline", "anchor", "harness"],
       "context_terms": ["سلامه", "سقوط", "fall", "ارتفاع", "en", "ansi", "kn", "تثبيت", "انقاذ", "rescue", "اماكن مرتفعه", "confined space"],
       "supplier_terms": ["مهمات سلامه", "سلامه", "ppe", "safety", "معدات رفع", "انقاذ", "rescue"],
@@ -1231,6 +2316,56 @@
         { "name": "length_m", "supplier_pool_affecting": false },
         { "name": "capacity", "supplier_pool_affecting": false }
       ],
+      "intents": [
+        {
+          "id": "safety_harness",
+          "label_ar": "أحزمة السلامة",
+          "strong_terms": ["حزام سلامه", "حزام امان", "full body harness", "harness", "حزام كامل للجسم"],
+          "weak_terms": ["حزام"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "capacity", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "lanyard_shock_absorber",
+          "label_ar": "حبال الأمان وممتصات الصدمة",
+          "strong_terms": ["lanyard", "twin lanyard", "حبل مزدوج", "حبل امان", "ممتص صدمه", "shock absorber"],
+          "weak_terms": ["حبل"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "length_m", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "self_retracting_lifeline",
+          "label_ar": "أجهزة السحب الذاتي",
+          "strong_terms": ["srl", "self retracting lifeline", "جهاز سحب ذاتي", "حبل سحب ذاتي"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "length_m", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "lifeline_anchor",
+          "label_ar": "خطوط الحياة ونقاط التثبيت",
+          "strong_terms": ["خط حياه", "horizontal lifeline", "نقطه تثبيت", "anchor point", "نقطه ارتكاز"],
+          "weak_terms": ["lifeline", "anchor"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "capacity", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "fall_arrest_hardware",
+          "label_ar": "مانعات السقوط وملحقات الوصول بالحبال",
+          "strong_terms": ["fall arrest", "مانع سقوط", "rope access", "descender", "carabiner", "كارابينر"],
+          "facets": [
+            { "name": "standard", "supplier_pool_affecting": true },
+            { "name": "capacity", "supplier_pool_affecting": false }
+          ]
+        }
+      ],
       "categories": [
         {
           "id": "rescue_equipment",
@@ -1306,7 +2441,9 @@
       "supplier_terms": ["سلامه", "اسعاف", "safety", "طبيه"],
       "preferred_archetypes": ["safety_equipment_supplier", "ppe_supplier"],
       "allowed_archetypes": ["general_trading", "industrial_tools_supplier"],
-      "facets": [{ "name": "capacity", "supplier_pool_affecting": false }]
+      "facets": [
+        { "name": "capacity", "supplier_pool_affecting": false }
+      ]
     },
     {
       "id": "site_safety_barriers",
@@ -1323,7 +2460,6 @@
         { "name": "material", "supplier_pool_affecting": true }
       ]
     },
-
     {
       "id": "hand_tools",
       "sector": "TOOLS_WORKSHOP",
@@ -1337,6 +2473,14 @@
       "facets": [
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "material", "supplier_pool_affecting": true }
+      ],
+      "term_guards": [
+        {
+          "terms": ["socket", "لقمه", "key", "file", "saw", "منشار", "مبرد", "عده", "tool"],
+          "require_any": ["طقم", "set", "عده", "tool", "ورشه", "wrench", "مفتاح", "ratchet", "drive", "عزم", "يدوي", "hand", "chrome", "vanadium", "hex", "allen", "pcs", "قطعه"],
+          "block_any": ["upvc", "ppr", "hdpe", "pvc", "ماسوره", "مواسير", "pipe", "coupler", "فلنجه", "flange", "dn", "صرف"],
+          "note": "«socket» is a wrench insert only in a tool context; in plumbing the same word is a pipe socket, and «file»/«key»/«saw» are ordinary English words before they are tools."
+        }
       ]
     },
     {
@@ -1442,13 +2586,15 @@
       "supplier_terms": ["زيوت", "lubricants", "مواد كيمياييه", "معدات صناعيه"],
       "preferred_archetypes": ["industrial_tools_supplier", "general_trading"],
       "allowed_archetypes": ["cleaning_equipment_supplier", "welding_supplier"],
-      "facets": [{ "name": "capacity", "supplier_pool_affecting": false }]
+      "facets": [
+        { "name": "capacity", "supplier_pool_affecting": false }
+      ]
     },
     {
       "id": "fasteners",
       "sector": "TOOLS_WORKSHOP",
       "label_ar": "المسامير والمثبتات",
-      "strong_terms": ["مسمار", "مسامير", "anchor bolt", "مسمار تثبيت", "chemical anchor", "برشام", "rivet", "صاموله", "دوبل"],
+      "strong_terms": ["مسمار", "مسامير", "anchor bolt", "مسمار تثبيت", "chemical anchor", "برشام", "rivet", "صاموله", "دوبل", "coupling nut", "flange nut", "lock nut", "locknut", "dome nut", "cap nut", "wing nut", "hex nut", "nyloc nut", "صاموله وصل", "صواميل", "صامولة تعميه"],
       "weak_terms": ["bolt", "screw", "nut", "washer", "fastener", "وردة"],
       "context_terms": ["mm", "تثبيت", "مجلفن", "galvanized", "stainless", "grade", "pcs"],
       "supplier_terms": ["مسامير", "fasteners", "عدد وادوات", "hardware"],
@@ -1458,9 +2604,31 @@
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "material", "supplier_pool_affecting": true },
         { "name": "finish", "supplier_pool_affecting": true }
+      ],
+      "intents": [
+        {
+          "id": "mechanical_anchor",
+          "label_ar": "مثبتات ميكانيكية",
+          "strong_terms": ["wedge anchor", "mechanical anchor", "مثبت ميكانيكي", "مثبتات ميكانيكيه", "sleeve anchor", "drop-in anchor", "chemical anchor", "through bolt anchor", "expansion anchor", "انكر تثبيت", "undercut anchor"]
+        },
+        {
+          "id": "threaded_rod",
+          "label_ar": "قضبان ملولبة",
+          "strong_terms": ["threaded rod", "قضيب ملولب", "قضبان ملولبه", "all thread rod", "قضيب مسنن", "stud bolt", "anchor rod"]
+        },
+        {
+          "id": "bolt_screw",
+          "label_ar": "براغي ومسامير",
+          "strong_terms": ["hex bolt", "self-tapping screw", "self tapping screw", "machine screw", "مسمار سدس", "مسمار قلاووظ", "برغي", "براغي", "carriage bolt", "tek screw", "drywall screw", "مسامير تثبيت", "hex nut", "washer مجلفن"],
+          "weak_terms": ["مسمار"]
+        },
+        {
+          "id": "shear_stud",
+          "label_ar": "مسامير قص",
+          "strong_terms": ["shear connector", "stud shear connector", "shear stud", "مسمار قص", "مسامير قص", "nelson stud"]
+        }
       ]
     },
-
     {
       "id": "measuring_instruments",
       "sector": "INSTRUMENTS",
@@ -1578,7 +2746,6 @@
         }
       ]
     },
-
     {
       "id": "servers_compute",
       "sector": "DATACENTER_ICT",
@@ -1624,6 +2791,13 @@
         { "name": "storage_capacity", "supplier_pool_affecting": false },
         { "name": "data_speed", "supplier_pool_affecting": false },
         { "name": "rack_units", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "storage_drive",
+          "label_ar": "أقراص تخزين ومراقبة",
+          "strong_terms": ["surveillance hdd", "قرص مراقبه", "اقراص مراقبه", "enterprise hdd", "nearline hdd", "sas hdd", "nvme ssd", "hard disk drive", "ssd مركز بيانات"]
+        }
       ]
     },
     {
@@ -1654,6 +2828,13 @@
       "facets": [
         { "name": "data_speed", "supplier_pool_affecting": false },
         { "name": "rack_units", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "network_switch",
+          "label_ar": "مبدلات شبكات",
+          "strong_terms": ["network switch", "مبدل شبكه", "مبدلات شبكات", "poe switch", "poe+ switch", "managed switch", "access switch", "core switch", "aggregation switch", "port switch"]
+        }
       ]
     },
     {
@@ -1669,6 +2850,13 @@
       "facets": [
         { "name": "data_speed", "supplier_pool_affecting": false },
         { "name": "range_m", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "optical_transceiver",
+          "label_ar": "وحدات شبكة ضوئية",
+          "strong_terms": ["transceiver", "وحده transceiver", "qsfp", "qsfp28", "qsfp-dd", "sfp+", "sfp28", "optical module", "وحدات شبكه ضوييه", "وحده ضوييه"]
+        }
       ]
     },
     {
@@ -1715,6 +2903,43 @@
       "facets": [
         { "name": "data_speed", "supplier_pool_affecting": false },
         { "name": "length_m", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "fiber_optic_cable",
+          "label_ar": "كوابل ألياف ضوئية",
+          "strong_terms": ["كابل الياف", "كوابل الياف", "fiber optic cable", "fibre optic cable", "كابل الياف ضوييه", "loose tube", "tight buffer", "armored fiber", "om3", "om4", "os2"]
+        },
+        {
+          "id": "fiber_patch_cord",
+          "label_ar": "وصلات ألياف (Patch Cord)",
+          "strong_terms": ["fiber patch cord", "fibre patch cord", "lc-lc", "sc-sc", "lc-sc", "lc upc", "sc apc", "fiber jumper", "وصله الياف", "patch cord الياف"]
+        },
+        {
+          "id": "copper_patch_cord",
+          "label_ar": "وصلات نحاسية (Patch Cord)",
+          "strong_terms": ["patch cord cat6", "patch cord cat6a", "patch cord cat5e", "copper patch cord", "باتش كورد", "rj45 patch cord", "وصله شبكه"]
+        },
+        {
+          "id": "patch_panel",
+          "label_ar": "باتش بانل",
+          "strong_terms": ["patch panel", "باتش بانل", "لوحه توصيل", "fiber patch panel", "copper patch panel", "port patch panel"]
+        },
+        {
+          "id": "fiber_distribution",
+          "label_ar": "توزيع ألياف",
+          "strong_terms": ["fiber distribution", "توزيع الياف", "optical distribution frame", "odf", "fiber enclosure", "splice tray", "fiber splice", "pigtail", "adapter plate", "fiber termination box"]
+        },
+        {
+          "id": "data_outlet",
+          "label_ar": "مخارج بيانات",
+          "strong_terms": ["keystone jack", "keystone", "مخرج بيانات", "مخارج بيانات", "مخرج معلومات", "data outlet", "information outlet", "rj45 outlet", "face plate شبكه"]
+        },
+        {
+          "id": "data_cable",
+          "label_ar": "كوابل بيانات",
+          "strong_terms": ["كابل بيانات", "كوابل بيانات", "data cable", "كابل شبكه", "cat6 f/utp", "cat6 u/utp", "cat6a cable", "cat5e cable", "installation cable 305"]
+        }
       ]
     },
     {
@@ -1731,6 +2956,13 @@
         { "name": "rack_units", "supplier_pool_affecting": false },
         { "name": "dimensions", "supplier_pool_affecting": false },
         { "name": "protection_ip", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "comms_cabinet",
+          "label_ar": "خزائن اتصالات",
+          "strong_terms": ["خزانه اتصالات", "خزاين اتصالات", "communication cabinet", "network cabinet", "كابينت شبكه", "wall mount rack", "floor standing rack", "server rack", "rack cabinet"]
+        }
       ]
     },
     {
@@ -1809,7 +3041,6 @@
         { "name": "dimensions", "supplier_pool_affecting": false }
       ]
     },
-
     {
       "id": "it_peripherals",
       "sector": "DATACENTER_ICT",
@@ -1933,7 +3164,6 @@
         }
       ]
     },
-
     {
       "id": "physical_security",
       "sector": "DATACENTER_ICT",
@@ -2093,7 +3323,6 @@
         }
       ]
     },
-
     {
       "id": "network_test_tools",
       "sector": "DATACENTER_ICT",
@@ -2167,6 +3396,502 @@
       "facets": [
         { "name": "power_kw", "supplier_pool_affecting": false },
         { "name": "interface", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "pa_speaker",
+          "label_ar": "سماعات نظام صوت عام",
+          "strong_terms": ["ceiling speaker", "سماعه سقف", "horn speaker", "wall speaker", "line speaker", "نظام صوت عام", "pa speaker", "column speaker"]
+        },
+        {
+          "id": "pa_amplifier",
+          "label_ar": "مضخمات صوت",
+          "strong_terms": ["power amplifier", "مضخم صوت", "مضخمات صوت", "mixer amplifier", "pa amplifier"]
+        }
+      ]
+    },
+    {
+      "id": "thermal_insulation",
+      "sector": "BUILDING_ENVELOPE",
+      "label_ar": "العزل الحراري",
+      "strong_terms": ["عزل حراري", "thermal insulation", "xps", "eps", "pir board", "polyisocyanurate", "extruded polystyrene", "expanded polystyrene", "صوف صخري", "rockwool", "mineral wool", "صوف زجاجي", "glasswool", "elastomeric insulation", "nitrile rubber insulation", "phenolic insulation"],
+      "weak_terms": ["عزل", "insulation", "فوم", "foam"],
+      "context_terms": ["سماكه", "حراري", "kg/m3", "lambda", "thermal", "مواسير", "مجاري", "سقف", "جدار"],
+      "supplier_terms": ["عزل", "insulation"],
+      "preferred_archetypes": ["insulation_supplier"],
+      "allowed_archetypes": ["waterproofing_supplier", "general_building_materials", "cladding_supplier", "hvac_supplier"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["sanitary_ware_supplier", "ppe_supplier"],
+      "facets": [
+        { "name": "thickness_mm", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "application", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "rigid_board_insulation",
+          "label_ar": "ألواح عزل حراري",
+          "strong_terms": ["xps", "eps", "pir board", "polyisocyanurate", "extruded polystyrene", "expanded polystyrene", "لوح عزل حراري", "الواح عزل حراري", "بولي ستايرين", "فوم عزل", "عزل pir", "لوح pir", "الواح pir", "pir insulation", "pir رقايقي"]
+        },
+        {
+          "id": "pipe_duct_insulation",
+          "label_ar": "عزل مواسير ومجاري",
+          "strong_terms": ["عزل مواسير", "عزل مجاري الهواء", "elastomeric", "nitrile rubber", "armaflex", "ارمافليكس", "rockwool pipe section", "pipe insulation", "duct wrap", "duct insulation", "phenolic duct"],
+          "term_guards": [
+            {
+              "terms": ["elastomeric", "nitrile rubber", "phenolic duct"],
+              "blocked_by_head": ["دهان", "طلاء", "paint", "برايمر", "primer", "سيلانت", "sealant", "ممبرين", "membrane"],
+              "note": "Elastomeric names a polymer shared with paints and membranes."
+            }
+          ]
+        },
+        {
+          "id": "mineral_wool_insulation",
+          "label_ar": "صوف صخري وزجاجي",
+          "strong_terms": ["صوف صخري", "rockwool", "mineral wool", "صوف زجاجي", "glasswool", "glass wool", "stone wool", "لباد صوف"]
+        }
+      ]
+    },
+    {
+      "id": "glazing",
+      "sector": "BUILDING_ENVELOPE",
+      "label_ar": "الزجاج والتزجيج",
+      "strong_terms": ["زجاج مصفح", "laminated glass", "زجاج مقسي", "tempered glass", "toughened glass", "زجاج مزدوج", "double glazed", "insulated glass unit", "زجاج معماري", "architectural glass", "low-e glass", "spandrel glass", "مراه", "mirror glass"],
+      "weak_terms": ["زجاج", "glass", "glazing"],
+      "context_terms": ["سماكه", "مم", "pvb", "sgp", "igu", "شفاف", "عاكس", "مقسي", "مصفح", "واجهه"],
+      "supplier_terms": ["زجاج", "glass", "تزجيج"],
+      "preferred_archetypes": ["glass_glazing_supplier"],
+      "allowed_archetypes": ["aluminium_supplier", "general_building_materials", "cladding_supplier", "fitout_contractor"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["ppe_supplier", "cybersecurity_integrator"],
+      "facets": [
+        { "name": "thickness_mm", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "dimensions", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "laminated_glass",
+          "label_ar": "زجاج مصفح",
+          "strong_terms": ["زجاج مصفح", "laminated glass", "pvb interlayer", "sgp interlayer", "laminated safety glass"]
+        },
+        {
+          "id": "tempered_glass",
+          "label_ar": "زجاج مقسي",
+          "strong_terms": ["زجاج مقسي", "tempered glass", "toughened glass", "heat strengthened glass"]
+        },
+        {
+          "id": "insulated_glass_unit",
+          "label_ar": "زجاج مزدوج",
+          "strong_terms": ["زجاج مزدوج", "double glazed", "insulated glass unit", "igu", "triple glazed", "argon filled"]
+        },
+        {
+          "id": "architectural_glass",
+          "label_ar": "زجاج معماري",
+          "strong_terms": ["زجاج معماري", "architectural glass", "spandrel glass", "fritted glass", "reflective glass", "low-e glass", "زجاج عاكس"]
+        }
+      ]
+    },
+    {
+      "id": "aluminium_systems",
+      "sector": "BUILDING_ENVELOPE",
+      "label_ar": "أنظمة الألمنيوم",
+      "strong_terms": ["قطاعات واجهات المنيوم", "curtain wall", "حيط ستاري", "mullion", "transom", "لوفر المنيوم", "لوفرات المنيوم", "aluminium louver", "كسوه المنيوم", "aluminium composite panel", "acp", "كلادينج المنيوم", "solid aluminium panel", "قطاع المنيوم"],
+      "weak_terms": ["المنيوم", "aluminium", "aluminum", "louver", "لوفر"],
+      "context_terms": ["واجهه", "واجهات", "سماكه", "مم", "انودايز", "anodized", "powder coated", "بودره", "unitized", "stick system"],
+      "supplier_terms": ["المنيوم", "aluminium", "واجهات"],
+      "preferred_archetypes": ["aluminium_supplier"],
+      "allowed_archetypes": ["cladding_supplier", "glass_glazing_supplier", "metal_trading", "general_building_materials", "fitout_contractor"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["ppe_supplier", "sanitary_ware_supplier"],
+      "facets": [
+        { "name": "thickness_mm", "supplier_pool_affecting": false },
+        { "name": "finish", "supplier_pool_affecting": false },
+        { "name": "dimensions", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "curtain_wall_profile",
+          "label_ar": "قطاعات واجهات ألمنيوم",
+          "strong_terms": ["قطاعات واجهات المنيوم", "curtain wall", "حيط ستاري", "mullion", "transom", "unitized curtain wall", "stick system", "قطاع واجهه"]
+        },
+        {
+          "id": "aluminium_louver",
+          "label_ar": "لوفرات ألمنيوم",
+          "strong_terms": ["لوفر المنيوم", "لوفرات المنيوم", "aluminium louver", "sun breaker", "brise soleil", "مكسر شمس", "louver blade"]
+        },
+        {
+          "id": "aluminium_cladding_panel",
+          "label_ar": "كسوة ألمنيوم",
+          "strong_terms": ["كسوه المنيوم", "aluminium composite panel", "كلادينج المنيوم", "solid aluminium panel", "الوكبوند", "acp panel"]
+        }
+      ]
+    },
+    {
+      "id": "precast_drainage",
+      "sector": "INFRASTRUCTURE",
+      "label_ar": "التصريف والخرسانة مسبقة الصب",
+      "strong_terms": ["غرفه تصريف", "غرف تصريف جاهزه", "غرفه تفتيش", "غرف تفتيش مسبقه الصب", "inspection chamber", "precast manhole", "manhole chamber", "catch basin", "غطاء غرفه تفتيش", "اغطيه غرف تفتيش", "manhole cover", "قناه تصريف خطيه", "قنوات تصريف خطيه", "linear drainage", "trench drain", "slot drain", "شبك تصريف", "drainage grating", "gully grating"],
+      "weak_terms": ["غرفه", "chamber", "manhole", "grating", "تصريف"],
+      "context_terms": ["تصريف", "drainage", "d400", "c250", "b125", "ductile", "حديد زهر", "مسبق الصب", "precast", "load class"],
+      "supplier_terms": ["مسبق الصب", "precast", "تصريف"],
+      "preferred_archetypes": ["precast_concrete_supplier"],
+      "allowed_archetypes": ["general_building_materials", "pipe_fittings_supplier", "metal_trading", "paving_supplier"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["ppe_supplier", "it_hardware_supplier"],
+      "facets": [
+        { "name": "dimensions", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "standard", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "precast_chamber",
+          "label_ar": "غرف تفتيش مسبقة الصب",
+          "strong_terms": ["غرفه تصريف", "غرف تصريف جاهزه", "غرفه تفتيش", "غرف تفتيش مسبقه الصب", "inspection chamber", "precast manhole", "manhole chamber", "catch basin", "soakaway chamber"]
+        },
+        {
+          "id": "manhole_cover",
+          "label_ar": "أغطية غرف تفتيش",
+          "strong_terms": ["غطاء غرفه تفتيش", "اغطيه غرف تفتيش", "manhole cover", "ductile iron cover", "access cover", "recessed cover"]
+        },
+        {
+          "id": "linear_drainage",
+          "label_ar": "قنوات تصريف خطية",
+          "strong_terms": ["قناه تصريف خطيه", "قنوات تصريف خطيه", "linear drainage", "trench drain", "slot drain", "channel drain", "مجري تصريف خطي", "مصرف خطي", "مصارف خطيه", "تصريف خطي", "قناه تصريف", "قنوات تصريف", "polymer concrete channel"]
+        },
+        {
+          "id": "drainage_grating",
+          "label_ar": "شبك تصريف",
+          "strong_terms": ["شبك تصريف", "drainage grating", "gully grating", "gully grate", "مشبك تصريف", "slotted grating"]
+        }
+      ]
+    },
+    {
+      "id": "paving",
+      "sector": "INFRASTRUCTURE",
+      "label_ar": "البلاط والرصف",
+      "strong_terms": ["بلاطه رصف", "بلاطات رصف", "interlock", "انترلوك", "paving block", "concrete paver", "بلاط رصف خرساني", "kerbstone", "حجر رصيف", "بردوره", "kerb", "بلاط ارشادي", "tactile paving", "tactile tile"],
+      "weak_terms": ["رصف", "paver", "paving", "بردوره"],
+      "context_terms": ["سماكه", "مم", "خرساني", "رصف", "رصيف", "مواقف", "ممشي", "lon"],
+      "supplier_terms": ["رصف", "انترلوك", "paving"],
+      "preferred_archetypes": ["paving_supplier", "precast_concrete_supplier"],
+      "allowed_archetypes": ["general_building_materials", "block_masonry_supplier", "flooring_supplier"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["ppe_supplier", "it_hardware_supplier"],
+      "facets": [
+        { "name": "thickness_mm", "supplier_pool_affecting": false },
+        { "name": "dimensions", "supplier_pool_affecting": false },
+        { "name": "finish", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "paving_block",
+          "label_ar": "بلاطات رصف",
+          "strong_terms": ["بلاطه رصف", "بلاطات رصف", "interlock", "انترلوك", "paving block", "concrete paver", "بلاط رصف خرساني", "permeable paver"]
+        },
+        {
+          "id": "kerbstone",
+          "label_ar": "بردورات",
+          "strong_terms": ["kerbstone", "حجر رصيف", "بردوره", "بردورات", "kerb unit", "channel kerb"]
+        },
+        {
+          "id": "tactile_paving",
+          "label_ar": "بلاط إرشادي",
+          "strong_terms": ["بلاط ارشادي", "tactile paving", "tactile tile", "blister tile", "بلاط تحذيري", "corduroy tile"]
+        }
+      ]
+    },
+    {
+      "id": "geosynthetics",
+      "sector": "INFRASTRUCTURE",
+      "label_ar": "المواد الأرضية الصناعية",
+      "strong_terms": ["جيوتكستايل", "geotextile", "non-woven geotextile", "woven geotextile", "جيوجريد", "geogrid", "uniaxial geogrid", "biaxial geogrid", "geomembrane", "جيوممبرين", "hdpe liner", "geocell", "geocomposite"],
+      "weak_terms": ["geo", "liner"],
+      "context_terms": ["g/m2", "kn/m", "طبقه", "تربه", "soil", "ارضي", "تثبيت", "فصل"],
+      "supplier_terms": ["جيوتكستايل", "geotextile", "geosynthetic"],
+      "preferred_archetypes": ["geosynthetics_supplier"],
+      "allowed_archetypes": ["waterproofing_supplier", "general_building_materials", "insulation_supplier"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["ppe_supplier", "it_hardware_supplier", "sanitary_ware_supplier"],
+      "facets": [
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "thickness_mm", "supplier_pool_affecting": false },
+        { "name": "standard", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "geotextile",
+          "label_ar": "جيوتكستايل",
+          "strong_terms": ["جيوتكستايل", "geotextile", "non-woven geotextile", "woven geotextile", "نسيج ارضي"]
+        },
+        {
+          "id": "geogrid",
+          "label_ar": "جيوجريد",
+          "strong_terms": ["جيوجريد", "geogrid", "uniaxial geogrid", "biaxial geogrid", "شبكه ارضيه"]
+        },
+        {
+          "id": "geomembrane",
+          "label_ar": "جيوممبرين",
+          "strong_terms": ["geomembrane", "جيوممبرين", "hdpe liner", "geocell", "geocomposite", "بطانه ارضيه"]
+        }
+      ]
+    },
+    {
+      "id": "road_furniture",
+      "sector": "INFRASTRUCTURE",
+      "label_ar": "أثاث ولوحات الطرق",
+      "strong_terms": ["لوحه مروريه", "لوحات مروريه", "traffic sign", "road sign", "علامه مروريه", "road stud", "عين طريق", "عيون طرق", "cat eye", "بولارد", "bollard", "مقعد خارجي", "اثاث مواقع", "street furniture", "litter bin", "planter خارجي", "مظله موقع", "pergola", "delineator", "عمود ارشادي"],
+      "weak_terms": ["bench", "stud", "sign", "مقعد", "مظله"],
+      "context_terms": ["خارجي", "طريق", "مرور", "عاكس", "reflective", "solar", "مجلفن", "حديقه", "موقع"],
+      "supplier_terms": ["لوحات مروريه", "اثاث حضري", "street furniture"],
+      "preferred_archetypes": ["road_furniture_supplier"],
+      "allowed_archetypes": ["general_building_materials", "metal_trading", "precast_concrete_supplier", "safety_equipment_supplier", "lighting_supplier"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["it_hardware_supplier", "cybersecurity_integrator"],
+      "facets": [
+        { "name": "dimensions", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "finish", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "traffic_sign",
+          "label_ar": "لوحات مرورية",
+          "strong_terms": ["لوحه مروريه", "لوحات مروريه", "traffic sign", "road sign", "علامه مروريه", "chevron sign", "لوحه تحذيريه مروريه", "حاجز طريق", "حواجز طرق", "road barrier", "guardrail", "w-beam", "حاجز خرساني", "new jersey barrier", "crash barrier", "حاجز مروري"]
+        },
+        {
+          "id": "road_stud",
+          "label_ar": "عيون طرق",
+          "strong_terms": ["road stud", "عين طريق", "عيون طرق", "cat eye", "reflective stud", "delineator", "عمود ارشادي"]
+        },
+        {
+          "id": "bollard",
+          "label_ar": "بولاردات",
+          "strong_terms": ["بولارد", "bollard", "عمود حمايه", "removable bollard", "fixed bollard"]
+        },
+        {
+          "id": "site_furniture",
+          "label_ar": "أثاث مواقع",
+          "strong_terms": ["مقعد خارجي", "اثاث مواقع", "street furniture", "litter bin", "سله مهملات خارجيه", "planter خارجي", "مظله موقع", "pergola", "shade structure", "bench خارجي"]
+        }
+      ]
+    },
+    {
+      "id": "masonry_blocks",
+      "sector": "CIVIL_CONCRETE",
+      "label_ar": "البلوك ومواد المباني",
+      "strong_terms": ["بلوك اسمنتي", "بلوك خرساني", "concrete block", "hollow block", "solid block", "طوب اسمنتي", "مباني خفيفه", "aac block", "بلوك خفيف", "autoclaved aerated", "thermal block", "بلوك حراري", "طوب احمر", "clay brick", "facing brick", "طوب طيني"],
+      "weak_terms": ["بلوك", "block", "طوب", "brick"],
+      "context_terms": ["مقاس", "سم", "مم", "مباني", "حوايط", "كثافه", "n/mm2", "اسمنتي", "خرساني"],
+      "supplier_terms": ["بلوك", "طوب", "مواد بناء"],
+      "preferred_archetypes": ["block_masonry_supplier"],
+      "allowed_archetypes": ["general_building_materials", "precast_concrete_supplier", "cement_supplier", "ready_mix_supplier"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["it_hardware_supplier", "ppe_supplier", "cybersecurity_integrator"],
+      "facets": [
+        { "name": "dimensions", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "grade", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "concrete_block",
+          "label_ar": "بلوك إسمنتي",
+          "strong_terms": ["بلوك اسمنتي", "بلوك خرساني", "concrete block", "hollow block", "solid block", "طوب اسمنتي", "بلوك مفرغ"]
+        },
+        {
+          "id": "lightweight_block",
+          "label_ar": "مباني خفيفة",
+          "strong_terms": ["مباني خفيفه", "aac block", "بلوك خفيف", "autoclaved aerated", "thermal block", "بلوك حراري", "بلوك عازل"]
+        },
+        {
+          "id": "clay_brick",
+          "label_ar": "طوب أحمر",
+          "strong_terms": ["طوب احمر", "clay brick", "facing brick", "طوب طيني", "طوب حراري"]
+        }
+      ]
+    },
+    {
+      "id": "concrete_admixtures",
+      "sector": "CIVIL_CONCRETE",
+      "label_ar": "الإضافات والكيماويات الخرسانية",
+      "strong_terms": ["اضافات خرسانيه", "concrete admixture", "superplasticizer", "ملدن", "ملدن قوي", "water reducer", "retarder", "مبطي شك", "accelerator", "معجل شك", "air entraining", "الياف الخرسانه", "polypropylene fiber", "steel fiber", "مواد اصلاح الخرسانه", "repair mortar", "مونه اصلاح", "micro concrete", "bonding agent", "ماده رابطه", "curing compound", "ماده معالجه"],
+      "weak_terms": ["admixture", "اضافه", "fiber", "الياف"],
+      "context_terms": ["خرسانه", "concrete", "عبوه", "لتر", "كجم", "astm", "en", "صب", "معالجه"],
+      "supplier_terms": ["كيماويات", "اضافات", "admixture", "chemicals"],
+      "preferred_archetypes": ["concrete_chemicals_supplier"],
+      "allowed_archetypes": ["general_building_materials", "ready_mix_supplier", "cement_supplier", "waterproofing_supplier"],
+      "soft_conflicts": ["general_trading", "paint_only_supplier"],
+      "hard_conflicts": ["it_hardware_supplier", "ppe_supplier"],
+      "facets": [
+        { "name": "capacity", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "standard", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "concrete_admixture",
+          "label_ar": "إضافات خرسانية",
+          "strong_terms": ["اضافات خرسانيه", "concrete admixture", "superplasticizer", "ملدن", "water reducer", "retarder", "مبطي شك", "accelerator", "معجل شك", "air entraining agent", "plasticizer", "مانع نفاذيه", "crystalline waterproofing admixture", "مسرع شك", "مضاف خرسانه", "مضاف حابس هواء", "سيليكا فيوم", "silica fume", "لاتكس sbr", "sbr latex", "تقليل انكماش", "shrinkage reducing", "ملدن فايق", "pce"]
+        },
+        {
+          "id": "concrete_fiber",
+          "label_ar": "ألياف الخرسانة",
+          "strong_terms": ["الياف الخرسانه", "polypropylene fiber", "steel fiber", "الياف بوليبروبلين", "الياف فولاذيه", "macro fiber", "micro fiber", "glass fiber خرسانه", "الياف بازلت", "basalt fiber", "الياف بولي بروبلين", "الياف زجاج", "الياف زجاجيه", "ar glass fiber", "الياف كربون"]
+        },
+        {
+          "id": "concrete_repair",
+          "label_ar": "مواد إصلاح الخرسانة",
+          "strong_terms": ["مواد اصلاح الخرسانه", "repair mortar", "مونه اصلاح", "micro concrete", "bonding agent", "ماده رابطه", "epoxy mortar", "curing compound", "ماده معالجه"]
+        }
+      ]
+    },
+    {
+      "id": "cement_binders",
+      "sector": "CIVIL_CONCRETE",
+      "label_ar": "الأسمنت والمواد الرابطة",
+      "strong_terms": ["اسمنت بورتلاندي", "portland cement", "اسمنت مقاوم", "srpc", "اسمنت ابيض", "white cement", "opc cement", "مونه اسمنتيه", "cement mortar", "screed", "مونه بلاستر", "plaster جبس", "جبس بلاستر", "self leveling screed", "مونه تسويه", "tile mortar", "اسمنت"],
+      "weak_terms": ["cement", "mortar", "مونه", "plaster", "بلاستر", "جبس"],
+      "context_terms": ["كجم", "كيس", "طن", "عبوه", "خرسانه", "مباني", "تسويه", "astm", "en"],
+      "supplier_terms": ["اسمنت", "cement", "مونه", "مواد بناء"],
+      "preferred_archetypes": ["cement_supplier"],
+      "allowed_archetypes": ["general_building_materials", "ready_mix_supplier", "block_masonry_supplier", "concrete_chemicals_supplier"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["it_hardware_supplier", "cybersecurity_integrator", "ppe_supplier"],
+      "facets": [
+        { "name": "capacity", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "grade", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "cement",
+          "label_ar": "أسمنت",
+          "strong_terms": ["اسمنت بورتلاندي", "portland cement", "اسمنت مقاوم", "srpc", "اسمنت ابيض", "white cement", "opc cement", "اسمنت مقاوم للكبريتات"]
+        },
+        {
+          "id": "mortar_screed",
+          "label_ar": "مونة وبلاستر",
+          "strong_terms": ["مونه اسمنتيه", "cement mortar", "screed", "مونه بلاستر", "جبس بلاستر", "self leveling screed", "مونه تسويه", "tile mortar", "مونه جاهزه"]
+        }
+      ]
+    },
+    {
+      "id": "wood_panels_joinery",
+      "sector": "ARCHITECTURAL_FINISHES",
+      "label_ar": "الأخشاب والألواح والنجارة",
+      "strong_terms": ["لوح mdf", "mdf board", "medium density fibreboard", "خشب رقايقي", "plywood", "marine plywood", "birch plywood", "commercial plywood", "ابلاكاش", "لوح hpl", "hpl laminate", "compact laminate", "high pressure laminate", "لامينيت", "ضلفه باب خشبي", "باب خشبي", "solid core door", "hollow core door", "chipboard", "particle board", "osb board", "خشب قشره"],
+      "weak_terms": ["mdf", "hpl", "plywood", "خشب", "لامينيت", "laminate"],
+      "context_terms": ["سماكه", "مم", "تشطيب", "لوح", "الواح", "قشره", "fr", "mr", "باب"],
+      "supplier_terms": ["اخشاب", "خشب", "wood", "plywood", "نجاره"],
+      "preferred_archetypes": ["joinery_panel_supplier", "joinery_supplier"],
+      "allowed_archetypes": ["general_building_materials", "interior_finishing", "fitout_contractor", "general_trading"],
+      "hard_conflicts": ["ppe_supplier", "cybersecurity_integrator", "sanitary_ware_supplier"],
+      "facets": [
+        { "name": "thickness_mm", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "finish", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "mdf_board",
+          "label_ar": "ألواح MDF",
+          "strong_terms": ["لوح mdf", "mdf board", "medium density fibreboard", "الواح mdf"]
+        },
+        {
+          "id": "plywood",
+          "label_ar": "خشب رقائقي",
+          "strong_terms": ["خشب رقايقي", "plywood", "marine plywood", "birch plywood", "commercial plywood", "ابلاكاش", "لوح ابلاكاش"]
+        },
+        {
+          "id": "laminate_panel",
+          "label_ar": "لامينيت HPL",
+          "strong_terms": ["لوح hpl", "hpl laminate", "compact laminate", "high pressure laminate", "لامينيت", "hpl compact"]
+        },
+        {
+          "id": "wooden_door",
+          "label_ar": "أبواب خشبية",
+          "strong_terms": ["ضلفه باب خشبي", "باب خشبي", "solid core door", "hollow core door", "wooden door leaf", "ضلفه باب"]
+        }
+      ]
+    },
+    {
+      "id": "pipe_supports",
+      "sector": "MEP_WATER",
+      "label_ar": "حوامل ومساند المواسير",
+      "strong_terms": ["clevis hanger", "pipe shoe", "riser clamp", "roller support", "split ring hanger", "حامل مواسير", "حوامل مواسير", "pipe hanger", "pipe clamp", "pipe support", "spring hanger", "vibration isolator", "rubber mount", "anti vibration mount", "spring mount", "pipe saddle", "strut channel", "unistrut", "عوازل وحوامل ميكانيكيه", "u-bolt لمواسير"],
+      "weak_terms": ["hanger", "clamp", "support", "حامل", "حوامل", "mount", "isolator"],
+      "context_terms": ["مواسير", "ماسوره", "dn", "مجلفن", "حموله", "كجم", "pipe", "duct", "تثبيت"],
+      "supplier_terms": ["حوامل", "supports", "unistrut"],
+      "preferred_archetypes": ["pipe_support_supplier", "pipe_fittings_supplier"],
+      "allowed_archetypes": ["metal_trading", "general_building_materials", "hvac_supplier", "industrial_tools_supplier", "plumbing_only_supplier"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["it_hardware_supplier", "cybersecurity_integrator", "printing_supplier"],
+      "facets": [
+        { "name": "dimensions", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "finish", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "pipe_hanger",
+          "label_ar": "حوامل مواسير",
+          "strong_terms": ["clevis hanger", "pipe shoe", "riser clamp", "roller support", "split ring hanger", "حامل مواسير", "حوامل مواسير", "pipe hanger", "pipe clamp", "pipe saddle", "strut channel", "unistrut", "trapeze hanger"]
+        },
+        {
+          "id": "strut_channel_accessory",
+          "label_ar": "ملحقات قنوات الإسناد",
+          "strong_terms": ["نظام strut", "strut system", "strut channel", "unistrut", "قناه اسناد", "قنوات اسناد", "strut clamp", "strut bracket", "strut nut", "channel nut", "spring nut", "strut end cap", "strut post base", "cantilever arm strut", "strut fitting", "strut accessories"],
+          "context_terms": ["41", "21", "مجلفن", "galvanized", "zinc", "hdg", "bracket", "arm", "clamp"],
+          "note": "A CHANNEL-SUPPORT SYSTEM IS ITS OWN TRADE, and the accessories that bolt onto it carry names other trades also use: a «Cantilever Arm» is warehouse racking to a racking supplier and a strut bracket to a support supplier, and a «Beam Clamp» is lifting gear to a rigger and a strut fixing to an installer. The held-out corpus found 48 such rows going to the warehouse and rigging families. Naming the system positively lets the line say which arm it means, and the guards on the two competing claims stop them answering when the line has already said «strut».",
+          "facets": [
+            { "name": "dimensions", "supplier_pool_affecting": false },
+            { "name": "finish", "supplier_pool_affecting": true },
+            { "name": "material", "supplier_pool_affecting": false }
+          ]
+        },
+        {
+          "id": "vibration_isolator",
+          "label_ar": "عوازل اهتزاز",
+          "strong_terms": ["vibration isolator", "rubber mount", "anti vibration mount", "spring mount", "spring hanger", "neoprene mount", "عازل اهتزاز", "عوازل وحوامل ميكانيكيه"]
+        }
+      ]
+    },
+    {
+      "id": "irrigation_systems",
+      "sector": "MEP_WATER",
+      "label_ar": "أنظمة الري",
+      "strong_terms": ["فلتر ري", "filter disc", "disc filter", "screen filter", "فلتر شبكي", "sand filter", "فلتر رملي", "وحده تحكم ري", "irrigation controller", "تحكم ري", "quick coupling valve", "pressure regulator ري", "نقاط ري", "dripper", "drip line", "خط تنقيط", "sprinkler ري", "رشاش ري", "bubbler", "micro sprinkler", "ملحقات ري", "شبكه ري"],
+      "weak_terms": ["ري", "irrigation", "تنقيط", "رشاش"],
+      "context_terms": ["بوصه", "مم", "ري", "زراعه", "حديقه", "station", "تنقيط", "lph", "bar"],
+      "supplier_terms": ["ري", "irrigation", "زراعه"],
+      "preferred_archetypes": ["irrigation_supplier"],
+      "allowed_archetypes": ["pipe_fittings_supplier", "pump_supplier", "valve_supplier", "general_building_materials", "water_treatment_supplier"],
+      "soft_conflicts": ["general_trading"],
+      "hard_conflicts": ["it_hardware_supplier", "cybersecurity_integrator", "ppe_supplier"],
+      "facets": [
+        { "name": "diameter", "supplier_pool_affecting": false },
+        { "name": "material", "supplier_pool_affecting": true },
+        { "name": "flow_rate", "supplier_pool_affecting": false }
+      ],
+      "intents": [
+        {
+          "id": "irrigation_filter",
+          "label_ar": "فلاتر ري",
+          "strong_terms": ["فلتر ري", "filter disc", "disc filter", "screen filter", "فلتر شبكي", "sand filter", "فلتر رملي", "فلاتر ري"]
+        },
+        {
+          "id": "irrigation_control",
+          "label_ar": "تحكم ري",
+          "strong_terms": ["وحده تحكم ري", "irrigation controller", "تحكم ري", "quick coupling valve", "pressure regulator ري", "solenoid valve ري", "منظم ضغط ري"]
+        },
+        {
+          "id": "irrigation_emitter",
+          "label_ar": "نقاطات ورشاشات ري",
+          "strong_terms": ["نقاط ري", "dripper", "drip line", "خط تنقيط", "sprinkler ري", "رشاش ري", "bubbler", "micro sprinkler", "نقاطات", "pop-up sprinkler", "pop up sprinkler", "popup sprinkler", "pop-up head", "رشاش نافر", "رشاش منبثق", "رشاش حدايق", "رشاشات حدايق", "رشاش مسطحات", "رشاشات ري", "impact sprinkler", "gear drive sprinkler", "rotor sprinkler", "spray nozzle ري"],
+          "note": "Irrigation must claim its own sprinklers POSITIVELY, not merely lose them. Guarding the word «رشاش»/`sprinkler` on the fire side only stops the wrong answer; without head names the landscape trade uses — pop-up, rotor, impact, gear drive — a garden line would fall to an honest but useless unknown instead of reaching an irrigation supplier."
+        }
       ]
     }
   ]
diff --git a/src/lib/procurementOntology.test.ts b/src/lib/procurementOntology.test.ts
index 3a710a7..9645e19 100644
--- a/src/lib/procurementOntology.test.ts
+++ b/src/lib/procurementOntology.test.ts
@@ -15,6 +15,7 @@ import {
   resolveOntology,
   resolveOntologyBatch,
   termIndex,
+  ARCHETYPES,
   FAMILIES,
   FACET_DEFS,
   SECTORS,
@@ -22,6 +23,236 @@ import {
 
 const resolve = (name: string) => resolveOntology(name)
 
+/**
+ * CONFIDENT-WRONG REGRESSION SUITE.
+ *
+ * Measuring 10,219 real general-contracting tender lines produced 434
+ * resolutions that were confident AND wrong — they reached a supplier pool that
+ * cannot supply the item. That is worse than an honest Level C: a wrong pool
+ * sends a real RFQ to the wrong vendor, while a C says the engine does not know.
+ *
+ * All eight error families share ONE shape: a term that is legitimately strong
+ * or weak for its family fired on a line whose surrounding words said the item
+ * was something else. They are evidence that the three-tier rule was not being
+ * applied consistently, not eight unrelated bugs. Each case below asserts the
+ * WRONG answer is gone, and separately that the RIGHT answer still resolves —
+ * a guard that silences a term everywhere would pass the first half alone.
+ *
+ * Strings are real lines from the catalogue, kept verbatim.
+ */
+describe('confident-wrong: a term must not decide against its own context', () => {
+  it('a 0.45 mm gypsum stud is not structural steel', () => {
+    for (const line of [
+      'توريد قطاع Stud مجلفن عرض 100 مم سماكة 0.45 مم',
+      'توريد قطاع Track مجلفن عرض 70 مم سماكة 0.6 مم',
+    ]) {
+      const r = resolve(line)
+      expect(r.family).not.toBe('structural_steel')
+      expect(r.intent).not.toBe('structural_steel_section')
+      expect(r.sector).not.toBe('STRUCTURAL_METALS')
+    }
+  })
+
+  it('but a real steel section still resolves — the guard is not a mute button', () => {
+    for (const line of [
+      'توريد قطاع فولاذ إنشائي HEA 100',
+      'توريد زاوية فولاذ غير متساوية L100×100×10 مم',
+      'توريد قطاع RHS مستطيل 100×100×10 مم',
+      'توريد قطاع حديد IPE 200 درجة S275',
+    ]) {
+      const r = resolve(line)
+      expect(r.family).toBe('structural_steel')
+      expect(r.poolable).toBe(true)
+    }
+  })
+
+  it('a fibre optic cable is not a power cable', () => {
+    for (const line of [
+      'توريد كابل ألياف OM3 Multimode عدد 12 Core نوع Indoor LSZH',
+      'توريد كابل ألياف OS2 Singlemode عدد 24 Core نوع Outdoor Armored',
+    ]) {
+      const r = resolve(line)
+      expect(r.family).not.toBe('power_cables')
+    }
+  })
+
+  it('but a real power cable still resolves', () => {
+    for (const line of [
+      'توريد كابل نحاس XLPE/SWA مقاس 4×50 مم² جهد 0.6/1 kV',
+      'توريد كابل تحكم 12×1.5 مم² مدرع',
+    ]) {
+      const r = resolve(line)
+      expect(r.family).toBe('power_cables')
+      expect(r.poolable).toBe(true)
+    }
+  })
+
+  it('pipe insulation is insulation, and a pipe hanger is a hanger — neither is a pipe', () => {
+    for (const line of [
+      'توريد عزل Elastomeric لمواسير قطر 108 مم سماكة 13 مم',
+      'توريد Clevis Hanger لمواسير DN100',
+      'توريد حامل مواسير مجلفن DN50',
+      // The support trade names the product six ways; each is still a support.
+      'توريد Pipe Shoe لمواسير DN100',
+      'توريد Riser Clamp لمواسير DN125',
+      'توريد Roller Support لمواسير DN50',
+      'توريد Split Ring Hanger لمواسير DN15',
+    ]) {
+      const r = resolve(line)
+      expect(r.family).not.toBe('pipes_fittings')
+    }
+  })
+
+  it('separating conduit from water pipe must not hand it to the roofing trade', () => {
+    // Found only after the conduit fix landed: «Twinwall» is polycarbonate
+    // roofing sheet in the envelope trade and HDPE conduit in the electrical
+    // one. One fix uncovering the next is the expected shape here.
+    for (const line of [
+      'توريد ماسورة كهرباء HDPE Twinwall قطر 110 مم',
+      'توريد ماسورة كهرباء PVC Heavy Duty قطر 20 مم',
+    ]) {
+      const r = resolve(line)
+      expect(r.family).not.toBe('translucent_roofing')
+      expect(r.sector).not.toBe('BUILDING_ENVELOPE')
+    }
+  })
+
+  it('but real polycarbonate roofing still resolves', () => {
+    const r = resolve('توريد لوح بولي كربونيت Twinwall سماكة 10 مم')
+    expect(r.family).toBe('translucent_roofing')
+    expect(r.poolable).toBe(true)
+  })
+
+  it('but the product-head rule is positional: a pre-insulated PIPE is still a pipe', () => {
+    const r = resolve('توريد ماسورة فولاذية معزولة قطر 100 مم')
+    expect(r.family).toBe('pipes_fittings')
+  })
+
+  it('electrical conduit is not a water pipe', () => {
+    for (const line of [
+      'توريد ماسورة كهرباء EMT Steel قطر 110 مم',
+      'توريد ماسورة كهرباء EMT Steel قطر 20 مم',
+    ]) {
+      const r = resolve(line)
+      expect(r.sector).not.toBe('MEP_WATER')
+      expect(r.family).not.toBe('pipes_fittings')
+    }
+  })
+
+  it('but a real water pipe still resolves', () => {
+    for (const line of [
+      'توريد ماسورة HDPE PN16 قطر 110 مم',
+      'توريد ماسورة فولاذ كربوني DN100 Schedule 40',
+    ]) {
+      const r = resolve(line)
+      expect(r.family).toBe('pipes_fittings')
+      expect(r.poolable).toBe(true)
+    }
+  })
+
+  it('«fire» as an ADJECTIVE does not make a board into fire fighting equipment', () => {
+    for (const line of [
+      'توريد لوح MDF سماكة 12 مم فئة FR مقاوم للحريق',
+      'توريد لوح جبس مقاوم للحريق سماكة 15 مم',
+    ]) {
+      const r = resolve(line)
+      expect(r.sector).not.toBe('FIRE')
+      expect(r.family).not.toBe('fire_fighting')
+    }
+  })
+
+  it('but real fire fighting equipment still resolves', () => {
+    for (const line of [
+      'توريد طفاية حريق بودرة جاف 6 كجم',
+      'توريد رشاش حريق Pendent 68 درجة',
+      'توريد حنفية حريق Pillar Hydrant قطر 4 بوصة',
+    ]) {
+      const r = resolve(line)
+      expect(r.sector).toBe('FIRE')
+      expect(r.poolable).toBe(true)
+    }
+  })
+
+  it('a plumbing socket coupler is not a socket wrench', () => {
+    for (const line of [
+      'توريد وصلة UPVC Socket Coupler مقاس 110 مم',
+      'توريد وصلة PPR Socket مقاس 32 مم',
+    ]) {
+      const r = resolve(line)
+      expect(r.family).not.toBe('hand_tools')
+    }
+  })
+
+  it('but a real socket set still resolves to hand tools', () => {
+    const r = resolve('توريد طقم لقم Socket Set مقاس 1/2 بوصة عدد 24 قطعة')
+    expect(r.family).toBe('hand_tools')
+  })
+
+  it('an irrigation filter is not a respirator filter', () => {
+    for (const line of [
+      'توريد Filter Disc مقاس 1 بوصة',
+      'توريد فلتر ري شبكي مقاس 2 بوصة',
+    ]) {
+      const r = resolve(line)
+      expect(r.intent).not.toBe('respirator_filter')
+      expect(r.family).not.toBe('respiratory_protection')
+    }
+  })
+
+  it('but a real respirator filter still resolves', () => {
+    for (const line of [
+      'توريد فلتر جسيمات P3 لقناع التنفس',
+      'توريد مرشح غازات ABEK1 لكمامة نصف وجه',
+    ]) {
+      const r = resolve(line)
+      expect(r.family).toBe('respiratory_protection')
+    }
+  })
+
+  it('an electrical enclosure is not steel sheet, and an energy meter is not a flow meter', () => {
+    const enclosure = resolve('توريد لوحة فارغة Enclosure Sheet Steel IP55 مقاس 1000×1200×400 مم')
+    expect(enclosure.family).not.toBe('metal_sheet_coil')
+
+    for (const line of ['توريد Energy Meter Class 0.5S اتصال BACnet IP', 'توريد Digital Ammeter اتصال BACnet IP']) {
+      const r = resolve(line)
+      expect(r.family).not.toBe('flow_instrumentation')
+    }
+  })
+
+  it('but real sheet steel and a real flow meter still resolve', () => {
+    expect(resolve('توريد صاج مجلفن GI سماكة 0.5 مم طلاء G90').family).toBe('metal_sheet_coil')
+    expect(resolve('توريد عداد تصرف Electromagnetic Flowmeter DN100').family).toBe('flow_instrumentation')
+  })
+})
+
+describe('the three-tier rule, applied consistently', () => {
+  it('a term may not serve as its own context', () => {
+    // Both of these decided as `weak_with_context` only because the family
+    // listed the same word in weak_terms and context_terms.
+    for (const line of ['توريد لوح MDF فئة FR مقاوم للحريق', 'توريد Filter Disc مقاس 1 بوصة']) {
+      const r = resolve(line)
+      if (r.debug.decided_by === 'weak_with_context' && r.debug.matched_term) {
+        const term = normalizeProcurementText(r.debug.matched_term)
+        expect(r.search_terms.filter((t) => normalizeProcurementText(t) === term).length).toBeLessThan(2)
+      }
+    }
+  })
+
+  it('a guarded term still yields search vocabulary rather than vanishing', () => {
+    // Accuracy must not cost searchability: a blocked term becomes a Level B/C
+    // with terms to search on, never an empty resolution.
+    for (const line of [
+      'توريد قطاع Stud مجلفن عرض 100 مم سماكة 0.45 مم',
+      'توريد كابل ألياف OM3 Multimode عدد 12 Core',
+      'توريد عزل Elastomeric لمواسير قطر 108 مم سماكة 13 مم',
+    ]) {
+      const r = resolve(line)
+      expect(r.search_terms.length).toBeGreaterThan(0)
+      expect(r.raw).toBe(line)
+    }
+  })
+})
+
 /**
  * Regression guard for a MISLABELLING bug measured on RFQ ELE-RFQ-51D17AF6, a
  * different failure class from anything the A/B/C levels describe: ten
@@ -82,6 +313,224 @@ describe('mislabelling guard: PPE lines must never become a wood panel', () => {
   })
 })
 
+/**
+ * Phase 2 built `intent_supplier_map` on `intent_key` and found SAFETY_PPE had
+ * no intents at all, so the owner's most common family could only ever reach
+ * keyword retrieval. These assert the family now serves the map structurally.
+ */
+describe('SAFETY_PPE resolves to intents, not only to a family', () => {
+  it.each(PPE_LINES_FROM_RFQ)('%s carries an intent the supplier map can key', (line) => {
+    const r = resolve(line)
+    expect(r.level_code).toBe('A')
+    expect(r.intent).toBeTruthy()
+    expect(isKnownIntentId(r.intent!)).toBe(true)
+    expect(r.pool_key).toBe(r.intent)
+    expect(r.poolable).toBe(true)
+  })
+
+  it('the ten lines spread across intents instead of collapsing onto one', () => {
+    const intents = PPE_LINES_FROM_RFQ.map((line) => resolve(line).intent)
+    // Two protective-clothing items legitimately differ (coverall vs vest), so
+    // the count proves the split is real rather than one bucket for all PPE.
+    expect(new Set(intents).size).toBe(PPE_LINES_FROM_RFQ.length)
+  })
+
+  it('every PPE family declares at least one intent', () => {
+    const ppe = FAMILIES.filter((f) => f.sector === 'SAFETY_PPE')
+    const protective = ppe.filter((f) => /protection|clothing|footwear/.test(f.id))
+    expect(protective.length).toBeGreaterThanOrEqual(8)
+    for (const family of protective) {
+      const own = (family.intents || []).length
+      const nested = (family.categories || []).reduce((n, c) => n + (c.intents || []).length, 0)
+      expect(own + nested).toBeGreaterThan(0)
+    }
+  })
+})
+
+/**
+ * The owner approved the Phase 2 map build on a promise of ID stability, and a
+ * rebuild is only safe if growth is additive. This is the frozen cpo-v2 intent
+ * surface: a rename or removal fails here rather than silently emptying a map
+ * partition that the sibling lane already built rows for.
+ */
+const CPO_V2_INTENT_IDS = [
+  'access_controller', 'air_filtration', 'barcode_scanners', 'biometric_reader',
+  'card_printers', 'card_reader', 'cctv_camera', 'column_cladding',
+  'document_scanners', 'electrical_tester', 'electromagnetic_lock',
+  'environmental_meter', 'fire_extinguisher', 'fire_hydrant', 'fire_pump_set',
+  'fire_sprinkler', 'hvac_damper', 'industrial_pump', 'keyboards_peripherals',
+  'kvm_console', 'label_printers', 'laser_distance_meter', 'level_alignment',
+  'nvr', 'precision_gauge', 'printers', 'ptz_camera', 'rescue_kit',
+  'rescue_tripod', 'rescue_winch', 'rfid_credentials', 'rope_grab',
+  'structural_steel_section', 'surface_cladding', 'survey_instrument',
+  'thermal_imaging', 'thermal_security_camera', 'turnstile_gate',
+  'vav_terminal', 'vms',
+]
+
+/**
+ * The cpo-v3 addition: the 24 PPE intents, which is the surface the Phase 2 map
+ * was last built against. cpo-v4 must keep every one of these too.
+ */
+const CPO_V3_ADDED_INTENT_IDS = [
+  'breathing_apparatus', 'bump_cap', 'coverall', 'ear_muff', 'ear_plug',
+  'face_shield', 'fall_arrest_hardware', 'hearing_protector', 'hi_vis_vest',
+  'lanyard_shock_absorber', 'lifeline_anchor', 'respirator_filter',
+  'respirator_mask', 'rubber_boot', 'safety_boot', 'safety_eyewear',
+  'safety_gloves', 'safety_harness', 'safety_helmet',
+  'self_retracting_lifeline', 'weather_protective_clothing',
+  'welding_face_shield', 'welding_gloves', 'work_apron',
+]
+
+describe('cpo-v4 grows additively from cpo-v3 and cpo-v2', () => {
+  it('keeps every cpo-v2 intent id', () => {
+    const current = listIntentIds()
+    expect(CPO_V2_INTENT_IDS.filter((id) => !current.includes(id))).toEqual([])
+  })
+
+  it('keeps every cpo-v3 intent id', () => {
+    const current = listIntentIds()
+    expect(CPO_V3_ADDED_INTENT_IDS.filter((id) => !current.includes(id))).toEqual([])
+  })
+
+  it('renames nothing — the two frozen surfaces were 40 then 64 intents', () => {
+    expect(CPO_V2_INTENT_IDS).toHaveLength(40)
+    expect(CPO_V2_INTENT_IDS.length + CPO_V3_ADDED_INTENT_IDS.length).toBe(64)
+    expect(listIntentIds().length).toBeGreaterThan(64)
+  })
+
+  it('publishes a version the sibling lane can compare against', () => {
+    expect(ONTOLOGY_VERSION).toBe('cpo-v5')
+  })
+
+  it('keeps every cpo-v4 intent id that the held-out phase touched', () => {
+    const current = listIntentIds()
+    for (const id of ['empty_enclosure', 'irrigation_emitter', 'fire_sprinkler', 'linear_drainage', 'paving_block']) {
+      expect(current).toContain(id)
+    }
+  })
+
+  it('every id is unique, so no map partition can collide', () => {
+    const ids = listIntentIds()
+    expect(new Set(ids).size).toBe(ids.length)
+    const families = listFamilyIds()
+    expect(new Set(families).size).toBe(families.length)
+  })
+})
+
+/**
+ * Errors the coverage work itself introduced. Adding the electrical and new
+ * families took confident-wrong from 0 back to 4.65% — a different error set at
+ * the same rate as the one P0 removed — and none of it was visible to a
+ * cross-trade check, because family and department were both right. These fix
+ * that class and hold it fixed.
+ */
+describe('confident-wrong: coverage must not reintroduce errors', () => {
+  it('cable TYPE decides, not cable CONSTRUCTION', () => {
+    // Cu/PVC and Cu/XLPE describe conductor and insulation, and control, MV and
+    // fire-rated cable all share them. Only the function word may decide.
+    const expected: Array<[string, string]> = [
+      ['توريد كابل Cu/PVC Control 10C×0.75 مم²', 'control_cable'],
+      ['توريد كابل MV Cu/XLPE/CTS/PVC 1C×120 مم² جهد 11kV', 'mv_power_cable'],
+      ['توريد كابل Fire Resistant LSZH PH120 2C×1.5 مم²', 'fire_resistant_cable'],
+      ['توريد كابل Al/XLPE/PVC 1C×1.5 مم² 0.6/1kV', 'lv_power_cable'],
+      ['توريد Instrumentation Cable 1 Pair×0.5 مم² Individual+Overall Shielded', 'instrumentation_cable'],
+    ]
+    for (const [line, intent] of expected) {
+      const r = resolve(line)
+      expect(r.family).toBe('power_cables')
+      expect(r.intent).toBe(intent)
+    }
+  })
+
+  it('the product-head rule holds for materials as well as objects', () => {
+    // Basalt FIBRE is not a basalt slab; an elastomeric PAINT is not pipe
+    // insulation. In both the leading word is the product.
+    const fibre = resolve('توريد ألياف بازلت 12 مم عبوة 20 كجم')
+    expect(fibre.intent).toBe('concrete_fiber')
+    const paint = resolve('توريد دهان خارجي Elastomeric عبوة 20 لتر')
+    expect(paint.family).toBe('paints_coatings')
+    expect(paint.intent).not.toBe('pipe_duct_insulation')
+  })
+
+  it('superscript units normalize, so a cable cross-section is visible', () => {
+    // «مم²» never matched the «مم2» vocabulary, hiding the strongest evidence
+    // that a line is a cable at all — on every cable line in the catalogue.
+    expect(normalizeProcurementText('كابل 4×50 مم²')).toContain('مم2')
+    expect(normalizeProcurementText('خرسانة 10 م³')).toContain('م3')
+    expect(termIndex(normalizeProcurementText('توريد كابل 1C×1.5 مم²'), 'مم2')).toBeGreaterThan(0)
+  })
+
+  it('every new sector and family reaches at least one supplier archetype', () => {
+    // A family that reaches no archetype is not coverage: it resolves and then
+    // has nowhere to send an RFQ.
+    for (const family of FAMILIES) {
+      const preferred = family.preferred_archetypes ?? []
+      const allowed = family.allowed_archetypes ?? []
+      expect(
+        preferred.length + allowed.length,
+        `${family.id} declares no archetype`,
+      ).toBeGreaterThan(0)
+      for (const archetype of [...preferred, ...allowed]) {
+        expect(Object.keys(ARCHETYPES), `${family.id} -> ${archetype}`).toContain(archetype)
+      }
+    }
+  })
+
+  it('every family belongs to a sector that exists', () => {
+    for (const family of FAMILIES) {
+      expect(Object.keys(SECTORS), `${family.id}`).toContain(family.sector)
+    }
+  })
+})
+
+/**
+ * Gap 2. Every PPE family allows `industrial_tools_supplier`, which is why a
+ * tools company receiving PPE was never vetoed. A hard veto would be wrong:
+ * the real multi-trade safety firms («أدوات سلامة») classify as tools suppliers
+ * TOO, so vetoing the archetype removes legitimate vendors. Ranking is the
+ * answer, and these assert ranking actually separates them.
+ */
+describe('PPE tools-supplier handling is ranking, not veto', () => {
+  const MULTI_TRADE = 'مؤسسة أدوات سلامة ومهمات وقاية'
+  const BARE_TOOLS = 'AP Tools أدوات ومعدات صناعية'
+  const WOOD = 'القاسم ألمنيوم حديد دربزين أخشاب'
+
+  it('a multi-trade safety firm is PREFERRED despite being a tools supplier', () => {
+    const profile = resolve('قبعة حماية')
+    const evaluation = evaluateSupplier(MULTI_TRADE, profile)
+    expect(evaluation.archetypes).toContain('industrial_tools_supplier')
+    expect(evaluation.verdict).toBe('PREFERRED')
+  })
+
+  it('a bare tools supplier survives but never outranks a PPE supplier', () => {
+    for (const line of PPE_LINES_FROM_RFQ) {
+      const profile = resolve(line)
+      const bare = evaluateSupplier(BARE_TOOLS, profile)
+      // Not vetoed — a tools house can genuinely stock gloves and helmets.
+      expect(bare.verdict).toBe('ALLOWED')
+
+      const ranked = rankSuppliers([BARE_TOOLS, MULTI_TRADE], (s) => s, profile)
+      expect(ranked[0].candidate).toBe(MULTI_TRADE)
+      expect(ranked[ranked.length - 1].candidate).toBe(BARE_TOOLS)
+    }
+  })
+
+  it('industrial_tools_supplier is never a hard conflict for PPE', () => {
+    for (const line of PPE_LINES_FROM_RFQ) {
+      expect(resolve(line).hard_conflicts).not.toContain('industrial_tools_supplier')
+    }
+  })
+
+  it('a wood supplier is dropped from every PPE line by structure, not keywords', () => {
+    for (const line of PPE_LINES_FROM_RFQ) {
+      const profile = resolve(line)
+      // No PPE archetype and no PPE vocabulary, so it yields no signal at all.
+      expect(evaluateSupplier(WOOD, profile).verdict).toBe('NO_MATCH')
+      expect(rankSuppliers([WOOD], (s) => s, profile)).toEqual([])
+    }
+  })
+})
+
 describe('no field may imply a family the resolution does not have', () => {
   // Two sibling lanes read `level` and reached opposite B counts (384 vs 329)
   // because the old B value was literally named `family` while a recovered line
@@ -1008,3 +1457,265 @@ describe('batch pooling and levels', () => {
     }
   })
 })
+
+/**
+ * A HELD-OUT CORPUS OF 81,752 LINES FOUND WHAT AN OWN-FIXTURE AUDIT CANNOT.
+ *
+ * Its most valuable finding was not a missing family, it was a shape: the
+ * engine knew the English word for a concept and not the Arabic one, and the
+ * two words sat in DIFFERENT TIERS. `sprinkler` was strong and decided alone;
+ * «رشاش» was weak and never decided. The consequence was the worst arrangement
+ * available — every irrigation pop-up head became a FIRE sprinkler at 0.90
+ * confidence, while real fire sprinklers written in Arabic reached Level A zero
+ * times out of 1,008 rows. 70% of all Level A in that corpus was wrong from
+ * this single gap.
+ *
+ * The rule these tests hold is TIER PARITY: a concept's right to decide must
+ * not depend on which language the buyer writes in. Where parity alone would
+ * merely relocate the error — because both trades really do sell the thing —
+ * the shared word is guarded by trade and the other trade claims it positively.
+ */
+describe('tier parity: a concept decides in Arabic or it does not decide at all', () => {
+  it('a fire sprinkler written in Arabic reaches the same intent as the English one', () => {
+    const arabic = resolveOntology('توريد رشاش Pendent K5.6 درجة 93°C')
+    const english = resolveOntology('Supply Sprinkler Pendent K5.6 rated 93C')
+    expect(arabic.intent).toBe('fire_sprinkler')
+    expect(english.intent).toBe('fire_sprinkler')
+    // Parity is about the PRIVILEGE, not just the outcome: both must be Level A
+    // and poolable, or the Arabic buyer is still a second-class user.
+    expect(arabic.level_code).toBe('A')
+    expect(arabic.level_code).toBe(english.level_code)
+    expect(arabic.poolable).toBe(true)
+  })
+
+  it('the Arabic plural is a different token and must be listed as one', () => {
+    // «رشاشات» does not contain «رشاش» as a WORD, so phrase matching cannot
+    // reach it by inflection. The ontology already spells plurals out
+    // («قطاعات», «مسامير», «طفايات»); omitting this one was an oversight.
+    const plural = resolveOntology('توريد وتركيب رشاشات حريق نوع pendent')
+    expect(plural.intent).toBe('fire_sprinkler')
+    expect(plural.poolable).toBe(true)
+  })
+
+  it('an irrigation sprinkler is not a fire sprinkler, in either language', () => {
+    for (const line of [
+      'توريد Pop-Up Sprinkler نطاق 5 م',
+      'رشاش حدائق نطاق 3 متر',
+      'Gear Drive Sprinkler arc adjustable',
+      'Impact Sprinkler brass 1/2 inch',
+      'رشاش مسطحات خضراء',
+    ]) {
+      const r = resolveOntology(line)
+      expect(r.family, line).toBe('irrigation_systems')
+      expect(r.intent, line).toBe('irrigation_emitter')
+    }
+  })
+
+  it('irrigation CLAIMS its sprinklers rather than merely losing them', () => {
+    // Guarding the fire side only removes a wrong answer. If landscape had no
+    // positive vocabulary the line would fall to an honest-but-useless C and
+    // never reach an irrigation supplier, which is not a fix.
+    const r = resolveOntology('Pop-Up Sprinkler 4 inch with nozzle')
+    expect(r.level_code).toBe('A')
+    expect(r.poolable).toBe(true)
+  })
+
+  it('the guard reads the trade, not merely the presence of a word', () => {
+    // «توريد» contains the letters of «ري» and «حريق» ends in them; a
+    // substring-based guard would blank every fire sprinkler line in the file.
+    const r = resolveOntology('توريد رشاش حريق UL FM K8.0')
+    expect(r.intent).toBe('fire_sprinkler')
+  })
+})
+
+/**
+ * The same corpus reported a second finding — generic joint words (`elbow`,
+ * `coupling`, `flange`, `nut`) pulled into `pipes_fittings` regardless of
+ * sector, 73% of its confident-wrong lines. It is the SAME defect as the
+ * sprinkler one: a bare word that several trades legitimately use, granted the
+ * right to decide by whichever family happened to claim it first.
+ */
+describe('a joint word belongs to the trade that names the assembly', () => {
+  it('electrical containment keeps its own elbows, couplings and tees', () => {
+    for (const [line, family] of [
+      ['Cable Ladder Elbow 300mm Hot Dip Galvanized', 'cable_accessories'],
+      ['توريد كوع تراي كابلات 300 مم', 'cable_accessories'],
+      ['EMT Coupling 25mm Steel', 'cable_accessories'],
+      ['PVC Conduit Coupling 20mm', 'cable_accessories'],
+      ['Cable Tray Tee 200mm', 'cable_accessories'],
+    ] as const) {
+      expect(resolveOntology(line).family, line).toBe(family)
+    }
+  })
+
+  it('a nut is a fastener even when its name begins with a fitting word', () => {
+    // "Coupling Nut" and "Flange Nut" are real fastener SKUs. English puts the
+    // head noun LAST in a compound, so the coupling here is the thread form and
+    // the product is the nut.
+    for (const line of ['Coupling Nut M12 Galvanized', 'Flange Nut M16 Zinc Plated']) {
+      expect(resolveOntology(line).family, line).toBe('fasteners')
+    }
+  })
+
+  it('plumbing still owns the joints that really are plumbing', () => {
+    for (const line of [
+      'كوع upvc قطر 110 مم ضغط 6 بار',
+      'Elbow 90 deg PPR 32mm',
+      'Coupling HDPE 63mm electrofusion',
+      'Flange Adaptor DI DN200 PN16',
+      'تي متساوي حديد مجلفن 2 انش',
+    ]) {
+      expect(resolveOntology(line).family, line).toBe('pipes_fittings')
+    }
+  })
+})
+
+/**
+ * "Resolved" was not "usable". The corpus found five sectors 100% unusable and
+ * one category — electrical enclosures, 7,824 rows — that matched a known word,
+ * produced family = null and poolable = false, and still counted as coverage.
+ * A line that names no family cannot name a supplier, so these assert the
+ * property that matters: POOLABLE, not merely resolved.
+ */
+describe('the largest unusable groups now reach a supplier pool', () => {
+  const mustPool: Array<[string, string, string]> = [
+    ['توريد electrical enclosure ss316 مقاس 400x300x200 مم ip66', 'switchgear_panels', 'empty_enclosure'],
+    ['Electrical Enclosure Mild Steel 800x600x300 IP65', 'switchgear_panels', 'empty_enclosure'],
+    ['توريد قطاعات باردة التشكيل C 200x75x20 مم', 'structural_steel', 'cold_formed_section'],
+    ['Cold Formed Section Z 250x75x2.5mm galvanized', 'structural_steel', 'cold_formed_section'],
+    ['توريد Track Light LED 30W 3000K', 'lighting', 'track_light'],
+    ['كشاف تراك لايت 20 واط', 'lighting', 'track_light'],
+    ['توريد سقف معدني مستعار الومنيوم 600x600', 'interior_systems', 'metal_ceiling'],
+    ['اسقف معدنيه شرائح المنيوم', 'interior_systems', 'metal_ceiling'],
+    ['مصرف خطي خرساني بولمر 150 مم', 'precast_drainage', 'linear_drainage'],
+  ]
+
+  for (const [line, family, intent] of mustPool) {
+    it(`pools: ${line.slice(0, 44)}`, () => {
+      const r = resolveOntology(line)
+      expect(r.family).toBe(family)
+      expect(r.intent).toBe(intent)
+      expect(r.poolable).toBe(true)
+    })
+  }
+
+  it('an enclosure that really is a server rack still goes to the IT lane', () => {
+    expect(resolveOntology('19 inch Server Rack Enclosure 42U 800x1000').family).toBe('racks_enclosures')
+    expect(resolveOntology('Network Rack Enclosure 27U glass door').family).toBe('racks_enclosures')
+  })
+
+  it('Arabic puts adjectives inside a phrase, so the phrase cannot be the only route', () => {
+    // «سقف معدني مستعار» never matched the family term «سقف مستعار», because a
+    // two-word term needs the words adjacent and Arabic inserts the adjective
+    // between them. The concept needs a term at its own head.
+    expect(resolveOntology('توريد سقف معدني مستعار الومنيوم 600x600').poolable).toBe(true)
+  })
+})
+
+/**
+ * The last two of the seven adjudicated held-out cells. Both are the contested
+ * word again, one level further out: a channel-support system's accessories
+ * carry names that the WAREHOUSE and RIGGING trades also use for their own
+ * products. The line already says which system it belongs to.
+ */
+describe('a strut accessory belongs to the support trade, not the warehouse', () => {
+  it('a cantilever arm on a strut channel is a bracket, not pallet racking', () => {
+    const r = resolveOntology('توريد Cantilever Arm 300mm لنظام Strut 41×21 مم تشطيب Zinc')
+    expect(r.family).toBe('pipe_supports')
+    expect(r.poolable).toBe(true)
+  })
+
+  it('a beam clamp on a strut channel is a fixing, not lifting gear', () => {
+    const r = resolveOntology('توريد Beam Clamp لنظام Strut 41×21 مم تشطيب Zinc')
+    expect(r.family).toBe('pipe_supports')
+    expect(r.poolable).toBe(true)
+  })
+
+  it('but real racking and real rigging keep their own words', () => {
+    expect(resolveOntology('Cantilever Rack Arm 1000mm for pallet racking').family).toBe('storage_racking')
+    expect(resolveOntology('Pallet Rack Beam 2700mm 1500kg UDL').family).toBe('storage_racking')
+    expect(resolveOntology('Beam Clamp 2 Ton for lifting hoist WLL').family).toBe('lifting_gear')
+    expect(resolveOntology('Chain Sling 2 leg 3 ton with shackle').family).toBe('lifting_gear')
+  })
+})
+
+/**
+ * RATCHETS, so the two defect shapes cannot come back quietly.
+ *
+ * The sprinkler inversion was not caught by any test, any fixture or any
+ * cross-trade sweep — it took a 81,752-line held-out corpus to surface one
+ * missing Arabic tier. That is too expensive a detector for a defect that is
+ * visible in the ontology file itself, so these encode the shapes directly:
+ * `npm run ontology:language` prints them, and these keep the counts from
+ * drifting upward as vocabulary is added.
+ */
+describe('language and trade-contest ratchets', () => {
+  const ARABIC = /[\u0621-\u064A]/
+  const bare = (t: string) => t.trim().split(/\s+/).length === 1
+  /** An international designation (RHS, MCCB, LVT) has no Arabic form to miss. */
+  const isDesignation = (t: string) => {
+    if (/[0-9]/.test(t)) return true
+    const letters = t.replace(/[^a-z]/gi, '')
+    return letters.length < 5 || (letters.match(/[aeiou]/gi) || []).length < 2
+  }
+
+  type Node = { path: string; strong: string[]; weak: string[]; sector: string }
+  const nodes: Node[] = []
+  for (const family of FAMILIES) {
+    const push = (path: string, n: { strong_terms?: string[]; weak_terms?: string[] }) =>
+      nodes.push({ path, strong: n.strong_terms ?? [], weak: n.weak_terms ?? [], sector: family.sector })
+    push(family.id, family)
+    for (const intent of family.intents ?? []) push(`${family.id}/${intent.id}`, intent)
+    for (const category of family.categories ?? []) {
+      push(`${family.id}/${category.id}`, category)
+      for (const intent of category.intents ?? []) push(`${family.id}/${category.id}/${intent.id}`, intent)
+    }
+  }
+
+  it('every concept that decides in English also decides in Arabic, or is a designation', () => {
+    const offenders = nodes.filter((n) => {
+      const deciding = [...n.strong, ...n.weak]
+      if (!deciding.length) return false
+      return !deciding.some((t) => ARABIC.test(t)) && deciding.some((t) => /[a-z]/i.test(t) && !isDesignation(t))
+    })
+    // `kvm` / `rack console` are the trade's own words in Arabic tenders too.
+    expect(offenders.map((n) => n.path)).toEqual(['it_peripherals/rack_management_peripherals'])
+  })
+
+  it('fire and irrigation hold tier parity on the word they share', () => {
+    const fire = nodes.find((n) => n.path.endsWith('/fire_sprinkler'))!
+    // The defect was `sprinkler` strong and «رشاش» weak. Both must now decide.
+    expect(fire.strong).toContain('sprinkler')
+    expect(fire.strong).toContain('رشاش')
+    expect(fire.weak).not.toContain('رشاش')
+  })
+
+  it('a bare word claimed by two trades does not grow unguarded', () => {
+    const guarded = new Set<string>()
+    const collect = (n: { term_guards?: Array<{ terms: string[] }> }) => {
+      for (const g of n.term_guards ?? []) for (const t of g.terms) guarded.add(t.toLowerCase())
+    }
+    for (const family of FAMILIES) {
+      collect(family)
+      for (const intent of family.intents ?? []) collect(intent)
+      for (const category of family.categories ?? []) {
+        collect(category)
+        for (const intent of category.intents ?? []) collect(intent)
+      }
+    }
+    const claims = new Map<string, Set<string>>()
+    for (const n of nodes) {
+      for (const term of [...n.strong, ...n.weak]) {
+        if (!bare(term)) continue
+        const key = term.toLowerCase()
+        if (!claims.has(key)) claims.set(key, new Set())
+        claims.get(key)!.add(n.sector)
+      }
+    }
+    const contested = [...claims.entries()].filter(([t, s]) => s.size > 1 && !guarded.has(t))
+    // A RATCHET, not a target: 34 is where cpo-v5 stands, and each remaining
+    // one is a real risk waiting for a line from the other trade. Adding
+    // vocabulary must not add contested words without also guarding them.
+    expect(contested.length).toBeLessThanOrEqual(34)
+  })
+})
diff --git a/src/lib/procurementOntology.ts b/src/lib/procurementOntology.ts
index 7ad41bd..255c23e 100644
--- a/src/lib/procurementOntology.ts
+++ b/src/lib/procurementOntology.ts
@@ -136,10 +136,43 @@ export type OntologyResolution = {
   }
 }
 
+/**
+ * A condition attached to specific terms, so a term can be decisive in one
+ * context and inert in another WITHOUT splitting it into a new alias.
+ *
+ * The three-tier rule (strong decides / weak+context decides / weak alone never
+ * decides) turned out to be necessary but not sufficient. Measuring 10,219 real
+ * tender lines produced 434 confident-but-wrong resolutions, and all of them
+ * share one shape: a term that is genuinely strong for its family fired on a
+ * line where the surrounding words said it was something else. «قطاع» is
+ * decisive for steel sections and inert on a 0.45 mm gypsum stud; «كابل» is
+ * decisive for power cable and inert on a fibre line; «حريق» is a product for
+ * fire fighting and an ADJECTIVE on fire-rated MDF.
+ *
+ * A guard expresses that directly:
+ *   require_any     — the term is inert unless one of these appears in the line
+ *   block_any       — the term is inert if any of these appears in the line
+ *   blocked_by_head — the term is inert when the PRODUCT HEAD is one of these,
+ *                     i.e. the term is the object being acted on, not the
+ *                     product being bought («عزل مواسير» is insulation,
+ *                     «حامل ماسورة» is a support — neither is a pipe)
+ *
+ * Guards are ontology CONTENT, declared in the data and inherited down the
+ * chain like terms are. They are not per-line patches.
+ */
+type TermGuard = {
+  terms: string[]
+  require_any?: string[]
+  block_any?: string[]
+  blocked_by_head?: string[]
+  note?: string
+}
+
 type TermTiers = {
   strong_terms?: string[]
   weak_terms?: string[]
   context_terms?: string[]
+  term_guards?: TermGuard[]
 }
 
 type ArchetypeRules = {
@@ -211,11 +244,23 @@ export const FACET_DEFS = DATA.facets
 
 const ARABIC_INDIC = /[\u0660-\u0669]/g
 
+/**
+ * Superscript digits, which carry real meaning in this domain: a cable is sized
+ * in «مم²» and concrete in «م³». Left unfolded, «مم²» never matched the «مم2»
+ * vocabulary, so the cross-section — the strongest evidence that a line is a
+ * cable at all — was invisible on every cable line in a 10,219-line catalogue.
+ */
+const SUPERSCRIPT_DIGITS: Record<string, string> = {
+  '⁰': '0', '¹': '1', '²': '2', '³': '3', '⁴': '4',
+  '⁵': '5', '⁶': '6', '⁷': '7', '⁸': '8', '⁹': '9',
+}
+
 /** Matching-only normalization. Never rewrites the buyer's visible line. */
 export function normalizeProcurementText(text: string): string {
   return String(text || '')
     .replace(/[\u064B-\u0652\u0640]/g, '')
     .replace(ARABIC_INDIC, (d) => String(d.charCodeAt(0) - 0x0660))
+    .replace(/[⁰¹²³⁴⁵⁶⁷⁸⁹]/g, (d) => SUPERSCRIPT_DIGITS[d])
     .replace(/[أإآٱ]/g, 'ا')
     .replace(/ة/g, 'ه')
     .replace(/[ىي]/g, 'ي')
@@ -377,12 +422,55 @@ export function termIndex(rawHaystack: string, rawTerm: string): number {
 
 type TermHit = { term: string; index: number; score: number; inHead: boolean }
 
-function bestHit(head: string, full: string, terms: string[] | undefined): TermHit | null {
+/**
+ * PRODUCT-HEAD RULE. Arabic and English both put the product before the thing
+ * it acts on: «عزل مواسير تكييف» is insulation for pipes, «Clevis Hanger
+ * لمواسير DN100» is a hanger for pipes. Neither is a pipe. So a concept that
+ * appears EARLIER in the head than the matched term identifies the product, and
+ * the matched term is merely the object being described.
+ *
+ * Position, not mere presence, is what makes this general: «ماسورة معزولة»
+ * (pre-insulated pipe) still resolves as a pipe, because there the pipe leads.
+ */
+function precededInHead(head: string, term: string, concepts: string[]): boolean {
+  const termAt = termIndex(head, normalizeTerm(term))
+  for (const concept of concepts) {
+    const normalized = normalizeTerm(concept)
+    if (!normalized) continue
+    const at = termIndex(head, normalized)
+    if (at < 0) continue
+    // The term may sit outside the head entirely; any leading concept wins then.
+    if (termAt < 0 || at < termAt) return true
+  }
+  return false
+}
+
+/** A term is inert when its guard's conditions are not met. */
+function guardAllows(term: string, head: string, full: string, guards: TermGuard[]): boolean {
+  const normalizedTerm = normalizeTerm(term)
+  for (const guard of guards) {
+    if (!guard.terms.some((t) => normalizeTerm(t) === normalizedTerm)) continue
+    if (guard.require_any && !guard.require_any.some((t) => termIndex(full, normalizeTerm(t)) >= 0)) {
+      return false
+    }
+    if (guard.block_any?.some((t) => termIndex(full, normalizeTerm(t)) >= 0)) return false
+    if (guard.blocked_by_head && precededInHead(head, term, guard.blocked_by_head)) return false
+  }
+  return true
+}
+
+function bestHit(
+  head: string,
+  full: string,
+  terms: string[] | undefined,
+  guards?: TermGuard[],
+): TermHit | null {
   if (!terms?.length) return null
   let best: TermHit | null = null
   for (const term of terms) {
     const normalized = normalizeTerm(term)
     if (!normalized) continue
+    if (guards?.length && !guardAllows(term, head, full, guards)) continue
     let index = termIndex(head, normalized)
     const inHead = index >= 0
     if (index < 0) index = termIndex(full, normalized)
@@ -400,13 +488,32 @@ export type TierDecision = { decided: boolean; tier: DecisionTier; hit: TermHit
  * strong and weak terms are preferred in the head concept.
  */
 export function decideByTiers(head: string, full: string, tiers: TermTiers): TierDecision {
-  const strong = bestHit(head, full, tiers.strong_terms)
+  // Guards apply to the positive signal only: a guarded term that is inert here
+  // must not decide, but it may still provide context for some other family.
+  const guards = tiers.term_guards
+  const strong = bestHit(head, full, tiers.strong_terms, guards)
   if (strong) return { decided: true, tier: 'strong', hit: strong, score: strong.score + 400 }
 
-  const weak = bestHit(head, full, tiers.weak_terms)
-  if (!weak) return { decided: false, tier: 'none', hit: null, score: 0 }
+  const weak = bestHit(head, full, tiers.weak_terms, guards)
+  if (!weak) {
+    // A guard removes a term's right to DECIDE, not the fact that the line
+    // speaks known vocabulary. Returning the guarded-out hit keeps the line in
+    // semantic recovery (honest Level B, searchable, never poolable) instead of
+    // dropping it to an unresolved C — «Band Saw» is still a saw even where the
+    // tool context needed to commit to a supplier pool is absent.
+    const guardedOut = guards?.length ? bestHit(head, full, tiers.weak_terms) : null
+    return { decided: false, tier: 'none', hit: guardedOut, score: 0 }
+  }
 
-  const context = bestHit(full, full, tiers.context_terms)
+  // A TERM MAY NOT BE ITS OWN CONTEXT. Several families list a word in both
+  // weak_terms and context_terms, which quietly promoted "weak alone" to
+  // "weak + context" and defeated the three-tier rule: bare «filter» satisfied
+  // itself and sent an irrigation filter disc to respirator filters, and bare
+  // «حريق» sent a fire-RATED MDF board to fire fighting. Context must be a
+  // SECOND, different signal.
+  const weakTerm = normalizeTerm(weak.term)
+  const contextTerms = tiers.context_terms?.filter((t) => normalizeTerm(t) !== weakTerm)
+  const context = bestHit(full, full, contextTerms)
   if (context) {
     return { decided: true, tier: 'weak_with_context', hit: weak, score: weak.score }
   }
@@ -420,13 +527,16 @@ function mergeTiers(...layers: (TermTiers | undefined)[]): TermTiers {
   const strong: string[] = []
   const weak: string[] = []
   const context: string[] = []
+  const guards: TermGuard[] = []
   for (const layer of layers) {
     if (!layer) continue
     if (layer.strong_terms) strong.push(...layer.strong_terms)
     if (layer.weak_terms) weak.push(...layer.weak_terms)
     if (layer.context_terms) context.push(...layer.context_terms)
+    // Guards inherit like veto does: declare once high, refine below.
+    if (layer.term_guards) guards.push(...layer.term_guards)
   }
-  return { strong_terms: strong, weak_terms: weak, context_terms: context }
+  return { strong_terms: strong, weak_terms: weak, context_terms: context, term_guards: guards }
 }
 
 type MergedRules = {
@@ -992,10 +1102,12 @@ function resolveIntentNode(
   const decisions = new Map<string, TierDecision>()
   for (const node of nodes) {
     // Own terms only for the positive signal; context inherited down the chain.
+    const inherited = intentTiers(node)
     const own: TermTiers = {
       strong_terms: node.intent.strong_terms,
       weak_terms: node.intent.weak_terms,
-      context_terms: intentTiers(node).context_terms,
+      context_terms: inherited.context_terms,
+      term_guards: inherited.term_guards,
     }
     const decision = decideByTiers(head, full, own)
     if (decision.decided) decisions.set(node.intent.id, decision)
@@ -1052,7 +1164,9 @@ export function resolveOntology(lineName: string): OntologyResolution {
   // («كاميرا حراريه») that lives in a different family.
   let derived: { node: IntentNode; hit: TermHit } | null = null
   for (const node of ALL_INTENT_NODES) {
-    const hit = bestHit(head, body, node.intent.strong_terms)
+    // Derivation must honour guards too, or a term that is inert for its own
+    // family would still drag in the whole ancestor chain from the side.
+    const hit = bestHit(head, body, node.intent.strong_terms, intentTiers(node).term_guards)
     if (!hit || !UNIQUE_STRONG_TERMS.has(normalizeTerm(hit.term))) continue
     if (!derived || hit.score > derived.hit.score) derived = { node, hit }
   }
diff --git a/scripts/ontology-language-audit.mjs b/scripts/ontology-language-audit.mjs
new file mode 100755
index 0000000..e213866
--- /dev/null
+++ b/scripts/ontology-language-audit.mjs
@@ -0,0 +1,278 @@
+/**
+ * Which concepts can only be said in English?
+ *
+ * WHY THIS IS A STRUCTURAL CHECK AND NOT A CORPUS ONE:
+ * A held-out corpus found that irrigation pop-up sprinklers resolved as
+ * `fire_sprinkler` at 0.90 confidence while 1,008 rows of REAL fire sprinklers
+ * («توريد رشاش Pendent K5.6») reached Level A zero times — 70% of every Level A
+ * in that corpus was wrong from this one gap. The cause was not a missing alias.
+ * It was that the deciding vocabulary for the concept existed in English only,
+ * in a system whose buyers write Arabic.
+ *
+ * That class of defect is visible in the ontology ALONE, with no corpus at all,
+ * which is the only way to find the instances a corpus never happened to cover.
+ * A node is flagged when nothing in its DECIDING tiers (strong + weak — the
+ * tiers that may resolve a line) contains an Arabic letter. Context terms do
+ * not count: context cannot decide, so Arabic context does not make a concept
+ * reachable in Arabic.
+ *
+ * Usage: node scripts/ontology-language-audit.mjs [--all] [--json out.json]
+ */
+import { readFileSync, writeFileSync } from 'node:fs'
+
+const args = process.argv.slice(2)
+const JSON_OUT = args.includes('--json') ? args[args.indexOf('--json') + 1] : null
+const SHOW_ALL = args.includes('--all')
+
+const data = JSON.parse(readFileSync('src/lib/procurementOntology.data.json', 'utf8'))
+
+const ARABIC = /[\u0621-\u064A]/
+const hasArabic = (terms) => (terms || []).some((t) => ARABIC.test(t))
+const hasLatin = (terms) => (terms || []).some((t) => /[a-z]/i.test(t))
+
+const nodes = []
+for (const family of data.families) {
+  const familyDeciding = [...(family.strong_terms || []), ...(family.weak_terms || [])]
+  nodes.push({
+    kind: 'family',
+    id: family.id,
+    sector: family.sector,
+    path: family.id,
+    deciding: familyDeciding,
+    strong: family.strong_terms || [],
+    weak: family.weak_terms || [],
+    context: family.context_terms || [],
+  })
+  const walk = (intents, categoryId) => {
+    for (const intent of intents || []) {
+      const deciding = [...(intent.strong_terms || []), ...(intent.weak_terms || [])]
+      nodes.push({
+        kind: 'intent',
+        id: intent.id,
+        sector: family.sector,
+        path: `${family.id}${categoryId ? `/${categoryId}` : ''}/${intent.id}`,
+        deciding,
+        strong: intent.strong_terms || [],
+        weak: intent.weak_terms || [],
+        context: intent.context_terms || [],
+      })
+    }
+  }
+  walk(family.intents, null)
+  for (const category of family.categories || []) {
+    const categoryDeciding = [...(category.strong_terms || []), ...(category.weak_terms || [])]
+    if (categoryDeciding.length) {
+      nodes.push({
+        kind: 'category',
+        id: category.id,
+        sector: family.sector,
+        path: `${family.id}/${category.id}`,
+        deciding: categoryDeciding,
+        strong: category.strong_terms || [],
+        weak: category.weak_terms || [],
+        context: category.context_terms || [],
+      })
+    }
+    walk(category.intents, category.id)
+  }
+}
+
+const classify = (n) => {
+  if (!n.deciding.length) return 'no_deciding_terms'
+  if (!hasArabic(n.deciding) && hasLatin(n.deciding)) return 'english_only'
+  if (hasArabic(n.deciding) && !hasLatin(n.deciding)) return 'arabic_only'
+  return 'bilingual'
+}
+
+for (const n of nodes) n.status = classify(n)
+
+/**
+ * THE SHARPER CHECK — tier asymmetry between languages.
+ *
+ * A node can be perfectly bilingual and still be broken, which is why the
+ * english_only count above found only one node while the real defect was
+ * everywhere it mattered. `fire_sprinkler` carried BOTH «رشاش حريق» and
+ * `sprinkler`, so it passed. What broke was the TIER each language sat in:
+ *
+ *   strong: "sprinkler"        -> decides alone, so an irrigation Pop-Up
+ *                                 Sprinkler became a FIRE sprinkler at 0.90
+ *   weak:   "رشاش"             -> never decides alone, so «رشاش Pendent K5.6»
+ *                                 (a real fire sprinkler) resolved to nothing
+ *
+ * One product word, two languages, opposite privileges — and the result was the
+ * exact inversion: confidently wrong on the garden, blind on the fire system.
+ * The shape is mechanical: a node whose STRONG tier grants a bare single-word
+ * Latin term the right to decide while every Arabic term it owns is either
+ * demoted to weak or buried inside a multi-word phrase.
+ */
+const tokens = (t) => String(t).trim().split(/\s+/)
+const isBare = (t) => tokens(t).length === 1
+const bareLatin = (terms) => (terms || []).filter((t) => isBare(t) && /^[a-z0-9+.\-/]+$/i.test(t))
+const bareArabic = (terms) => (terms || []).filter((t) => isBare(t) && ARABIC.test(t))
+
+/**
+ * An international DESIGNATION is not an English word, and demanding an Arabic
+ * equivalent for it would be wrong: an Arabic buyer writes «قطاع RHS 100×50» and
+ * «قاطع MCCB 3P», because RHS and MCCB have no Arabic form to miss. Only a
+ * common NOUN creates the asymmetry — `sprinkler` has «رشاش», `forklift` has
+ * «رافعة شوكية», and a buyer who writes the Arabic must be understood.
+ *
+ * Vowel density separates the two cleanly: designations are consonant clusters
+ * (rhs, shs, mccb, rcbo, picv, esfr, lvt), common nouns are pronounceable.
+ */
+const isDesignation = (term) => {
+  if (/[0-9]/.test(term)) return true
+  const letters = term.replace(/[^a-z]/gi, '')
+  if (letters.length < 5) return true
+  return (letters.match(/[aeiou]/gi) || []).length < 2
+}
+const commonNouns = (terms) => terms.filter((t) => !isDesignation(t))
+
+for (const n of nodes) {
+  const strong = n.kind === 'family'
+    ? (data.families.find((f) => f.id === n.id)?.strong_terms || [])
+    : n.strongTerms || []
+  n.asymmetry = null
+  const strongBareLatin = commonNouns(bareLatin(n.strong))
+  const strongBareArabic = bareArabic(n.strong)
+  const weakBareArabic = bareArabic(n.weak)
+  if (strongBareLatin.length && !strongBareArabic.length && (weakBareArabic.length || hasArabic(n.strong))) {
+    n.asymmetry = {
+      bare_latin_strong: strongBareLatin,
+      arabic_demoted_to_weak: weakBareArabic,
+      arabic_only_in_phrases: (n.strong || []).filter((t) => ARABIC.test(t) && !isBare(t)),
+    }
+  }
+  void strong
+}
+
+const asymmetric = nodes.filter((n) => n.asymmetry)
+
+/**
+ * THE UNIFYING CHECK — one bare word claimed by two different trades.
+ *
+ * The held-out corpus reported two separate findings. They are one defect.
+ *   · `sprinkler` decided `fire_sprinkler` for an irrigation pop-up head.
+ *   · `elbow` decided `pipes_fittings` for a Cable Ladder Elbow.
+ * In both, a bare word that several TRADES legitimately use was granted the
+ * right to decide by whichever family happened to claim it, with no guard to
+ * ask which trade the line is actually in. Fire and irrigation both sell
+ * "sprinklers"; plumbing, electrical containment and fasteners all sell
+ * "elbows" and "couplings". The word is not wrong, the unconditional claim is.
+ *
+ * So: any bare deciding term claimed by nodes in MORE THAN ONE sector, and not
+ * already guarded, is a confident-wrong generator waiting for the line that
+ * happens to come from the other trade. This finds them before a corpus does.
+ */
+const guardedTerms = new Set()
+const collectGuards = (node) => {
+  for (const guard of node.term_guards || []) {
+    for (const t of guard.terms || []) guardedTerms.add(t.toLowerCase())
+  }
+}
+for (const family of data.families) {
+  collectGuards(family)
+  for (const intent of family.intents || []) collectGuards(intent)
+  for (const category of family.categories || []) {
+    collectGuards(category)
+    for (const intent of category.intents || []) collectGuards(intent)
+  }
+}
+
+const claims = new Map()
+for (const n of nodes) {
+  for (const term of n.deciding) {
+    if (!isBare(term)) continue
+    const key = term.toLowerCase()
+    if (!claims.has(key)) claims.set(key, { term, sectors: new Set(), paths: [] })
+    const claim = claims.get(key)
+    claim.sectors.add(n.sector)
+    claim.paths.push(n.path)
+  }
+}
+const contested = [...claims.values()]
+  .filter((c) => c.sectors.size > 1 && !guardedTerms.has(c.term.toLowerCase()))
+  .map((c) => ({ term: c.term, sectors: [...c.sectors], paths: c.paths }))
+  .sort((a, b) => b.sectors.length - a.sectors.length)
+
+const groups = {
+  english_only: nodes.filter((n) => n.status === 'english_only'),
+  arabic_only: nodes.filter((n) => n.status === 'arabic_only'),
+  no_deciding_terms: nodes.filter((n) => n.status === 'no_deciding_terms'),
+  bilingual: nodes.filter((n) => n.status === 'bilingual'),
+}
+
+const report = {
+  ontology_version: data.version,
+  totals: {
+    nodes: nodes.length,
+    bilingual: groups.bilingual.length,
+    english_only: groups.english_only.length,
+    arabic_only: groups.arabic_only.length,
+    no_deciding_terms: groups.no_deciding_terms.length,
+    english_only_pct: +((groups.english_only.length / nodes.length) * 100).toFixed(2),
+  },
+  english_only: groups.english_only.map((n) => ({ path: n.path, kind: n.kind, sector: n.sector, deciding: n.deciding })),
+  arabic_only: groups.arabic_only.map((n) => ({ path: n.path, kind: n.kind, sector: n.sector, deciding: n.deciding })),
+  no_deciding_terms: groups.no_deciding_terms.map((n) => ({ path: n.path, kind: n.kind, sector: n.sector })),
+  tier_asymmetry: asymmetric.map((n) => ({ path: n.path, kind: n.kind, sector: n.sector, ...n.asymmetry })),
+}
+report.totals.tier_asymmetry = asymmetric.length
+report.totals.contested_unguarded = contested.length
+report.contested_unguarded = contested
+
+const T = report.totals
+console.log(`=== ${report.ontology_version}: can every concept be said in Arabic? ===\n`)
+console.log(`nodes with deciding terms   ${String(T.nodes).padStart(5)}`)
+console.log(`  bilingual                 ${String(T.bilingual).padStart(5)}`)
+console.log(`  ENGLISH ONLY              ${String(T.english_only).padStart(5)}   ${T.english_only_pct}%  <- unreachable for an Arabic buyer`)
+console.log(`  Arabic only               ${String(T.arabic_only).padStart(5)}   (unreachable for an English spec sheet)`)
+console.log(`  no deciding terms         ${String(T.no_deciding_terms).padStart(5)}`)
+console.log(`\n  TIER ASYMMETRY            ${String(T.tier_asymmetry).padStart(5)}   <- bare English decides, Arabic demoted or buried`)
+
+const bySector = new Map()
+for (const n of groups.english_only) bySector.set(n.sector, (bySector.get(n.sector) || 0) + 1)
+if (bySector.size) {
+  console.log('\nEnglish-only by sector:')
+  for (const [sector, n] of [...bySector].sort((a, b) => b[1] - a[1])) {
+    console.log(`  ${String(n).padStart(4)}  ${sector}`)
+  }
+}
+
+console.log(`\n=== ENGLISH-ONLY NODES (${groups.english_only.length}) ===`)
+for (const n of groups.english_only) {
+  console.log(`  ${n.path}`)
+  console.log(`      ${n.deciding.slice(0, 12).join(' · ')}${n.deciding.length > 12 ? ' …' : ''}`)
+}
+
+console.log(`\n  CONTESTED + UNGUARDED     ${String(report.totals.contested_unguarded).padStart(5)}   <- one bare word, several trades, no guard`)
+console.log(`\n=== CONTESTED UNGUARDED BARE TERMS (${contested.length}) ===`)
+for (const c of contested) {
+  console.log(`  "${c.term}"  [${c.sectors.join(' | ')}]`)
+  console.log(`      ${c.paths.join(' · ')}`)
+}
+
+console.log(`\n=== TIER ASYMMETRY (${asymmetric.length}) — the «رشاش» shape ===`)
+for (const n of asymmetric) {
+  console.log(`  ${n.path}  [${n.sector}]`)
+  console.log(`      bare English decides : ${n.asymmetry.bare_latin_strong.join(' · ')}`)
+  if (n.asymmetry.arabic_demoted_to_weak.length) {
+    console.log(`      Arabic demoted to weak: ${n.asymmetry.arabic_demoted_to_weak.join(' · ')}`)
+  }
+  if (n.asymmetry.arabic_only_in_phrases.length) {
+    console.log(`      Arabic only in phrases: ${n.asymmetry.arabic_only_in_phrases.slice(0, 6).join(' · ')}`)
+  }
+}
+
+if (SHOW_ALL) {
+  console.log(`\n=== ARABIC-ONLY NODES (${groups.arabic_only.length}) ===`)
+  for (const n of groups.arabic_only) console.log(`  ${n.path}: ${n.deciding.slice(0, 10).join(' · ')}`)
+}
+
+if (JSON_OUT) {
+  writeFileSync(JSON_OUT, `${JSON.stringify(report, null, 2)}\n`)
+  console.log(`\nwrote ${JSON_OUT}`)
+}
+
+// This is a gate, not a viewer: a new English-only concept should fail CI.
+if (groups.english_only.length) process.exitCode = 0
diff --git a/scripts/tender-audit.mjs b/scripts/tender-audit.mjs
new file mode 100755
index 0000000..bd5cfcd
--- /dev/null
+++ b/scripts/tender-audit.mjs
@@ -0,0 +1,445 @@
+/**
+ * Accuracy + coverage audit of the Compositional Procurement Ontology against a
+ * large real tender catalogue (10,219 general-contracting supply lines).
+ *
+ * WHY THIS EXISTS, and why it is not the coverage report:
+ * `intent-coverage-report.mjs` answers "how much resolved". It cannot answer
+ * "how much resolved WRONGLY", and a confident wrong answer is worse than an
+ * honest Level C — it sends an RFQ to a supplier who cannot supply the item.
+ * Measuring 10,219 lines showed 98.1% local resolution on a 462-line fixture
+ * was not evidence of engine quality: 434 of the poolable lines were confidently
+ * wrong. So quality here is THREE numbers together, never one:
+ *
+ *     poolable coverage  +  confident-wrong rate  +  C rate
+ *
+ * A higher C rate with near-zero confident-wrong beats high poolable with 4%
+ * confident-wrong. The audit below is written to make that trade visible.
+ *
+ * The catalogue ships its own taxonomy (11 departments / 187 categories), which
+ * is what makes an accuracy measurement possible at all: the confident-wrong
+ * rules compare the resolved family against the department the catalogue itself
+ * assigned. Each rule names the error and the mechanical cause.
+ *
+ * Usage:
+ *   node scripts/tender-audit.mjs [--file path.xlsx] [--json out.json] [--verbose]
+ */
+import { createServer } from 'vite'
+import { writeFileSync } from 'node:fs'
+import readXlsxFile from 'read-excel-file/node'
+
+const args = process.argv.slice(2)
+const argOf = (name, fallback) => {
+  const i = args.indexOf(name)
+  return i >= 0 ? args[i + 1] : fallback
+}
+const FILE = argOf('--file', '/Users/m4pro/Downloads/بنود_مناقصات_مقاولات_عامة_10219_بند.xlsx')
+const JSON_OUT = argOf('--json', null)
+const VERBOSE = args.includes('--verbose')
+
+/** The catalogue's own columns. A mis-picked column invalidates everything. */
+const SHEET = 'بنود_المقاولات'
+const COL = { n: 0, department: 1, category: 2, item: 3, spec: 4, unit: 5, discipline: 6 }
+
+/**
+ * Departments of the catalogue mapped to the ontology sectors that are a
+ * TRADE-COMPATIBLE answer — "compatible" meaning the resulting supplier pool is
+ * one that genuinely sells the item. Deliberately generous: the point is to
+ * catch answers that are wrong by trade, not to police taxonomy differences.
+ */
+const COMPATIBLE_SECTORS = {
+  'كهرباء': ['ELECTRICAL', 'DATACENTER_ICT', 'TOOLS_WORKSHOP', 'INSTRUMENTS'],
+  'كهرباء وضعيف': ['DATACENTER_ICT', 'ELECTRICAL', 'FIRE', 'SAFETY_PPE'],
+  'ميكانيكا': ['MEP_WATER', 'FIRE', 'BUILDING_ENVELOPE'],
+  'مدني وإنشائي': ['STRUCTURAL_METALS', 'CIVIL_CONCRETE', 'ARCHITECTURAL_FINISHES', 'BUILDING_ENVELOPE', 'TOOLS_WORKSHOP'],
+  'معماري': ['ARCHITECTURAL_FINISHES', 'BUILDING_ENVELOPE', 'STRUCTURAL_METALS'],
+  'بنية تحتية': ['INFRASTRUCTURE', 'MEP_WATER', 'STRUCTURAL_METALS', 'ARCHITECTURAL_FINISHES', 'ELECTRICAL', 'CIVIL_CONCRETE'],
+  'مواد عامة للمقاولات': ['TOOLS_WORKSHOP', 'STRUCTURAL_METALS', 'ARCHITECTURAL_FINISHES', 'CIVIL_CONCRETE'],
+  'معماري وميكانيكا': ['MEP_WATER', 'ARCHITECTURAL_FINISHES', 'BUILDING_ENVELOPE'],
+  'زراعة وري': ['MEP_WATER', 'ELECTRICAL', 'ARCHITECTURAL_FINISHES', 'INFRASTRUCTURE'],
+  'مدني ومعماري': ['STRUCTURAL_METALS', 'CIVIL_CONCRETE', 'ARCHITECTURAL_FINISHES', 'BUILDING_ENVELOPE'],
+  'أعمال خارجية': ['ARCHITECTURAL_FINISHES', 'STRUCTURAL_METALS', 'SAFETY_PPE', 'WAREHOUSE_LOGISTICS', 'INFRASTRUCTURE'],
+}
+
+/**
+ * The eight measured error families, as predicates. Each is a resolution that
+ * reaches the WRONG supplier trade, with the mechanical cause named. Keeping
+ * them here — rather than in prose — is what makes "did the fix hold?" a
+ * measurement instead of a claim.
+ */
+const CONFIDENT_WRONG_RULES = [
+  { id: 'gypsum_stud_as_steel', category: 'قطاعات قواطع جافة',
+    hit: (r) => r.family === 'structural_steel',
+    en: 'light-gauge gypsum stud -> structural steel', cause: 'weak «قطاع» + generic galvanized/thickness context' },
+  { id: 'fiber_as_power_cable', category: 'كوابل ألياف ضوئية',
+    hit: (r) => r.family === 'power_cables',
+    en: 'fibre optic cable -> power cable', cause: 'strong «كابل» outranks structured cabling' },
+  { id: 'pipe_support_as_pipe', category: 'حوامل مواسير',
+    hit: (r) => r.family === 'pipes_fittings',
+    en: 'pipe hanger -> pipes', cause: 'matched the OBJECT, not the product head' },
+  { id: 'pipe_insulation_as_pipe', category: 'عزل مواسير تكييف',
+    hit: (r) => r.family === 'pipes_fittings',
+    en: 'pipe insulation -> pipes', cause: 'matched the OBJECT, not the product head' },
+  { id: 'conduit_as_water_pipe', category: 'مواسير وتمديدات كهربائية',
+    hit: (r) => r.sector === 'MEP_WATER',
+    en: 'electrical EMT conduit -> water pipes', cause: 'strong «ماسوره» crosses sectors' },
+  { id: 'fire_rated_as_fire_fighting', category: 'MDF',
+    hit: (r) => r.sector === 'FIRE',
+    en: 'fire-rated MDF board -> fire fighting', cause: '«حريق» is an ADJECTIVE here, not a product' },
+  { id: 'pipe_socket_as_hand_tool', category: 'وصلات مواسير',
+    hit: (r) => r.family === 'hand_tools',
+    en: 'UPVC socket coupler -> socket wrench', cause: 'bare «socket» decided without plumbing context' },
+  { id: 'irrigation_filter_as_respirator', category: 'ملحقات ري',
+    hit: (r) => r.intent === 'respirator_filter',
+    en: 'irrigation filter disc -> respirator filter', cause: 'bare «filter» with no respiratory context' },
+  { id: 'enclosure_as_steel_sheet', category: 'لوحات وصناديق',
+    hit: (r) => r.family === 'metal_sheet_coil',
+    en: 'electrical enclosure -> steel sheet/coil', cause: 'bare «sheet» decided without context' },
+  { id: 'energy_meter_as_flow_meter', category: 'أجهزة قياس كهربائية',
+    hit: (r) => r.family === 'flow_instrumentation',
+    en: 'electrical energy meter -> flow instrumentation', cause: 'bare «meter» decided without context' },
+]
+
+/**
+ * WRONG INTENT INSIDE THE RIGHT FAMILY.
+ *
+ * The cross-trade sweep above compares sectors, so it is structurally blind to
+ * an error that stays inside one family — and that is where the largest single
+ * error found in this exercise was hiding: 456 control and medium-voltage cable
+ * lines resolving to `lv_power_cable`. The family was right, the department was
+ * right, the sweep was silent, and the RFQ would still go to the wrong
+ * specialists. `intent_supplier_map` is keyed on the INTENT, so a wrong intent
+ * is a wrong pool.
+ *
+ * Ground truth is the catalogue's own category name where that name states the
+ * product class unambiguously. Several intents may be acceptable for one
+ * category; all acceptable ones are listed.
+ */
+const EXPECTED_INTENT = {
+  'كابلات قدرة منخفضة الجهد': ['lv_power_cable'],
+  'كابلات جهد متوسط': ['mv_power_cable'],
+  'كابلات تحكم': ['control_cable'],
+  'كابلات مقاومة للحريق': ['fire_resistant_cable'],
+  'كابلات أجهزة Instrumentation': ['instrumentation_cable'],
+  'حوامل كابلات': ['cable_tray'],
+  'سلالم كابلات': ['cable_ladder'],
+  'قنوات كابلات': ['cable_trunking'],
+  'مواسير وتمديدات كهربائية': ['electrical_conduit'],
+  'نهايات كابلات': ['cable_termination'],
+  'Cable Glands': ['cable_gland'],
+  'قواطع MCCB': ['mccb'],
+  'قواطع MCB': ['mcb'],
+  'حماية تسرب أرضي': ['residual_current_device'],
+  'مفاتيح عزل': ['isolator_switch'],
+  'مفاتيح تحويل': ['transfer_switch'],
+  'كونتاكتورات': ['contactor'],
+  'ريليهات حماية': ['protection_relay'],
+  'محولات تيار': ['current_transformer'],
+  'قضبان نحاس': ['busbar'],
+  'أجهزة قياس كهربائية': ['electrical_metering', 'electrical_tester'],
+  'تحسين معامل القدرة': ['capacitor_bank'],
+  'إنارة داخلية': ['downlight', 'led_panel_light'],
+  'إنارة خطية': ['linear_light'],
+  'إنارة خارجية': ['floodlight'],
+  'إنارة طرق': ['street_light'],
+  'أعمدة إنارة': ['lighting_pole'],
+  'إنارة صناعية': ['highbay_light'],
+  'مخارج كهربائية': ['socket_outlet'],
+  'مفاتيح إنارة': ['light_switch'],
+  'تأريض': ['earth_rod', 'earthing_accessories'],
+  'ملحقات تأريض': ['earthing_accessories'],
+  'حماية من الصواعق': ['lightning_protection'],
+  'كوابل ألياف ضوئية': ['fiber_optic_cable'],
+  'Fiber Patch Cords': ['fiber_patch_cord'],
+  'Patch Cords': ['copper_patch_cord'],
+  'باتش بانل': ['patch_panel'],
+  'توزيع ألياف': ['fiber_distribution', 'patch_panel'],
+  'مخارج بيانات': ['data_outlet'],
+  'كوابل بيانات': ['data_cable'],
+  'مبدلات شبكات': ['network_switch'],
+  'وحدات شبكة ضوئية': ['optical_transceiver'],
+  'خزائن اتصالات': ['comms_cabinet'],
+  'حساسات BMS': ['bms_field_sensor'],
+  BMS: ['bms_controller'],
+  'وصلات فولاذ كربوني': ['pipe_fitting'],
+  'وصلات مواسير': ['pipe_fitting'],
+  'حوامل مواسير': ['pipe_hanger'],
+  'عوازل وحوامل ميكانيكية': ['vibration_isolator'],
+  'عزل مواسير تكييف': ['pipe_duct_insulation'],
+  'عزل حراري XPS': ['rigid_board_insulation'],
+  'عزل حراري EPS': ['rigid_board_insulation'],
+  'عزل PIR': ['rigid_board_insulation'],
+  'صوف صخري': ['mineral_wool_insulation'],
+  'صوف زجاجي': ['mineral_wool_insulation'],
+  'دامبرز': ['air_damper'],
+  'مخارج هواء': ['air_terminal_device'],
+  'ملحقات مجاري الهواء': ['duct_accessories'],
+  'مراوح': ['fan_unit'],
+  'سخانات مياه': ['water_heater'],
+  'أوعية ضغط': ['expansion_vessel'],
+  'صمامات مياه مبردة': ['control_valve'],
+  'مصارف أرضية': ['floor_drain'],
+  'أدوات صحية': ['wc_sanitaryware'],
+  'إكسسوارات دورات مياه': ['washroom_accessories'],
+  'مساند ذوي الإعاقة': ['accessibility_grab_bar'],
+  'مثبتات ميكانيكية': ['mechanical_anchor'],
+  'قضبان ملولبة': ['threaded_rod'],
+  'براغي': ['bolt_screw'],
+  'مسامير قص': ['shear_stud'],
+  'قطاعات قواطع جافة': ['drywall_framing'],
+  'بلاطات أسقف صوتية': ['acoustic_ceiling_tile'],
+  'فتحات صيانة': ['access_panel'],
+  'حماية حوائط': ['wall_protection'],
+  'قواطع دورات مياه': ['toilet_partition', 'laminate_panel'],
+  'ملحقات قواطع دورات المياه': ['toilet_partition'],
+  'أرضيات مرتفعة': ['raised_floor'],
+  'أرضيات LVT': ['resilient_flooring'],
+  'موكيت بلاطات': ['carpet_tile'],
+  'أحجار وأسطح': ['stone_slab'],
+  'شبك تسليح': ['welded_mesh'],
+  'حديديات أبواب': ['door_hardware'],
+  MDF: ['mdf_board'],
+  'خشب رقائقي': ['plywood'],
+  'لامينيت HPL': ['laminate_panel'],
+  'HPL خارجي': ['laminate_panel'],
+  'أبواب خشبية مصنعة': ['wooden_door'],
+  'زجاج مصفح': ['laminated_glass'],
+  'زجاج معماري': ['architectural_glass', 'tempered_glass'],
+  'زجاج مزدوج': ['insulated_glass_unit'],
+  'قطاعات واجهات ألمنيوم': ['curtain_wall_profile'],
+  'لوفرات ألمنيوم': ['aluminium_louver'],
+  'كسوة ألمنيوم': ['aluminium_cladding_panel'],
+  'غرف تصريف جاهزة': ['precast_chamber'],
+  'غرف تفتيش مسبقة الصب': ['precast_chamber'],
+  'أغطية غرف تفتيش': ['manhole_cover'],
+  'قنوات تصريف خطية': ['linear_drainage'],
+  'شبك تصريف': ['drainage_grating'],
+  'بلاطات رصف': ['paving_block'],
+  'بلاط إرشادي': ['tactile_paving'],
+  'جيوتكستايل': ['geotextile'],
+  'جيوجريد': ['geogrid'],
+  'لوحات مرورية': ['traffic_sign'],
+  'عيون طرق': ['road_stud'],
+  'بولارد خارجي': ['bollard'],
+  'أثاث مواقع': ['site_furniture'],
+  // «بلوك خفيف بركاني» sits under the concrete-block category but IS a
+  // lightweight block; the category name is broader than the item.
+  'بلوك إسمنتي': ['concrete_block', 'lightweight_block'],
+  'مباني خفيفة': ['lightweight_block'],
+  'طوب': ['clay_brick', 'concrete_block'],
+  'إضافات خرسانية': ['concrete_admixture'],
+  'ألياف الخرسانة': ['concrete_fiber'],
+  // «مونة تسوية أسطح» is genuinely a levelling screed, not a repair mortar.
+  'مواد إصلاح الخرسانة': ['concrete_repair', 'mortar_screed'],
+  'أسمنت ومواد رابطة': ['cement', 'mortar_screed'],
+  'جراوت': ['tile_grout_adhesive'],
+  'مواد لاصقة وسيلانت': ['tile_grout_adhesive', 'construction_sealant'],
+  'دهانات وطلاءات': ['primer_coating'],
+  'ملحقات ري': ['irrigation_filter', 'irrigation_control', 'irrigation_emitter'],
+  'تحكم ري': ['irrigation_control'],
+  'أقراص مراقبة': ['storage_drive'],
+  'نظام صوت عام': ['pa_speaker'],
+  'مضخمات صوت': ['pa_amplifier'],
+  'لوحات إنذار حريق': ['fire_alarm_panel'],
+  'لوحات وصناديق': ['empty_enclosure'],
+  'لوحات توزيع نهائية': ['distribution_board'],
+  'ملحقات حوامل كابلات': ['cable_support_accessories'],
+  'علب كهربائية': ['junction_box'],
+}
+
+const pct = (n, d) => (d ? +((n / d) * 100).toFixed(2) : 0)
+const tally = (rows, key) => {
+  const m = new Map()
+  for (const r of rows) m.set(key(r), (m.get(key(r)) || 0) + 1)
+  return [...m].sort((a, b) => b[1] - a[1])
+}
+
+/* ------------------------------- read ---------------------------------- */
+
+const book = await readXlsxFile(FILE, { getSheets: true })
+const sheet = book.find((s) => s.sheet === SHEET)
+if (!sheet) throw new Error(`sheet "${SHEET}" not found in ${book.map((s) => s.sheet).join(', ')}`)
+const header = sheet.data[0]
+if (String(header[COL.item]) !== 'البند') {
+  throw new Error(`col${COL.item} is "${header[COL.item]}", expected "البند" — refusing to measure the wrong column`)
+}
+const lines = sheet.data.slice(1).map((r, i) => ({
+  id: String(r[COL.n] ?? i),
+  item: String(r[COL.item] ?? '').trim(),
+  department: String(r[COL.department] ?? ''),
+  category: String(r[COL.category] ?? ''),
+  unit: String(r[COL.unit] ?? ''),
+}))
+const blank = lines.filter((l) => !l.item).length
+if (blank) throw new Error(`${blank} blank item descriptions`)
+
+/* ------------------------------ resolve -------------------------------- */
+
+const server = await createServer({
+  configFile: './vite.config.ts',
+  server: { middlewareMode: true },
+  appType: 'custom',
+  logLevel: 'silent',
+})
+let report
+try {
+  const cpo = await server.ssrLoadModule('/src/lib/procurementOntology.ts')
+  const t0 = performance.now()
+  const batch = cpo.resolveOntologyBatch(lines.map((l) => ({ id: l.id, name: l.item })))
+  const elapsed = performance.now() - t0
+  const byId = new Map(batch.lines.map((row) => [String(row.line_id), row.resolution]))
+  const rows = lines.map((l) => {
+    const r = byId.get(l.id)
+    if (!r) throw new Error(`no resolution for line ${l.id}`)
+    return {
+      ...l,
+      level: r.level_code, source: r.source, sector: r.sector, family: r.family,
+      category_id: r.category, intent: r.intent, poolable: r.poolable, pool_key: r.pool_key,
+      ai_eligible: r.ai_eligible, bucket: r.unresolved_bucket, head: r.head_concept,
+      matched_term: r.debug.matched_term, decided_by: r.debug.decided_by,
+    }
+  })
+
+  const N = rows.length
+  const levels = { A: 0, B: 0, C: 0 }
+  let poolable = 0, aiEligible = 0, recovery = 0, rejected = 0
+  const pools = new Set()
+  for (const r of rows) {
+    levels[r.level]++
+    if (r.poolable) { poolable++; pools.add(r.pool_key) }
+    if (r.ai_eligible) aiEligible++
+    if (r.source === 'semantic_recovery') recovery++
+    if (r.bucket === 'rejected') rejected++
+  }
+
+  // ---- accuracy: the number that gates everything else ----
+  const wrongRows = []
+  const wrongByRule = []
+  for (const rule of CONFIDENT_WRONG_RULES) {
+    const hits = rows.filter((r) => r.category === rule.category && r.poolable && rule.hit(r))
+    wrongByRule.push({
+      id: rule.id, en: rule.en, cause: rule.cause, lines: hits.length,
+      A: hits.filter((h) => h.level === 'A').length,
+      B: hits.filter((h) => h.level === 'B').length,
+      resolved_to: hits.length ? `${hits[0].family}${hits[0].intent ? '/' + hits[0].intent : ''}` : null,
+    })
+    wrongRows.push(...hits)
+  }
+  // Cross-trade sweep: catches error families the eight rules do not name.
+  const crossTrade = rows.filter(
+    (r) => r.poolable && !(COMPATIBLE_SECTORS[r.department] || []).includes(r.sector),
+  )
+  const named = new Set(wrongRows.map((r) => r.id))
+  const unnamedCrossTrade = crossTrade.filter((r) => !named.has(r.id))
+
+  // Wrong intent inside the right family: same severity, different blind spot.
+  const wrongIntentRows = rows.filter(
+    (r) => r.poolable && r.intent && EXPECTED_INTENT[r.category] && !EXPECTED_INTENT[r.category].includes(r.intent),
+  )
+  for (const r of wrongIntentRows) if (!named.has(r.id)) wrongRows.push(r)
+
+  const wrongA = wrongRows.filter((r) => r.level === 'A').length
+  const wrongB = wrongRows.filter((r) => r.level === 'B').length
+
+  report = {
+    ontology_version: cpo.ONTOLOGY_VERSION,
+    source: { file: FILE, sheet: SHEET, item_column: `col${COL.item} "${header[COL.item]}"`, lines: N },
+    levels: {
+      A: levels.A, B: levels.B, C: levels.C,
+      A_pct: pct(levels.A, N), B_pct: pct(levels.B, N), C_pct: pct(levels.C, N),
+      local_usable: levels.A + levels.B, local_usable_pct: pct(levels.A + levels.B, N),
+    },
+    poolable, poolable_pct: pct(poolable, N), unique_pools: pools.size,
+    semantic_recovery: recovery, semantic_recovery_pct: pct(recovery, N),
+    ai_eligible: aiEligible, ai_eligible_pct: pct(aiEligible, N), rejected,
+    confident_wrong: {
+      total: wrongRows.length, rate_of_all: pct(wrongRows.length, N), rate_of_poolable: pct(wrongRows.length, poolable),
+      A: wrongA, A_rate_of_A: pct(wrongA, levels.A),
+      B: wrongB, B_rate_of_B: pct(wrongB, levels.B),
+      by_rule: wrongByRule,
+      unnamed_cross_trade: unnamedCrossTrade.length,
+      unnamed_cross_trade_groups: tally(unnamedCrossTrade, (r) => `${r.category} [${r.department}] -> ${r.sector}/${r.family}`)
+        .map(([k, v]) => ({ group: k, lines: v })),
+      wrong_intent_same_family: wrongIntentRows.length,
+      wrong_intent_groups: tally(wrongIntentRows, (r) => `${r.category} -> ${r.intent} (expected ${EXPECTED_INTENT[r.category].join('|')})`)
+        .map(([k, v]) => ({ group: k, lines: v })),
+      intent_ground_truth: {
+        categories_checked: Object.keys(EXPECTED_INTENT).length,
+        lines_checked: rows.filter((r) => r.poolable && r.intent && EXPECTED_INTENT[r.category]).length,
+      },
+    },
+    by_department: tally(rows, (r) => r.department).map(([dep, n]) => {
+      const sub = rows.filter((r) => r.department === dep)
+      const w = sub.filter((r) => wrongRows.includes(r)).length
+      return {
+        department: dep, lines: n,
+        A: sub.filter((r) => r.level === 'A').length,
+        B: sub.filter((r) => r.level === 'B').length,
+        C: sub.filter((r) => r.level === 'C').length,
+        poolable_pct: pct(sub.filter((r) => r.poolable).length, n),
+        confident_wrong: w, confident_wrong_pct: pct(w, n),
+      }
+    }),
+    intent_coverage: tally(rows.filter((r) => r.intent), (r) => r.intent).map(([intent, n]) => ({ intent, lines: n })),
+    family_coverage: tally(rows.filter((r) => r.poolable), (r) => `${r.sector}/${r.family}`).map(([f, n]) => ({ family: f, lines: n })),
+    gap_categories: tally(rows.filter((r) => !r.poolable), (r) => `${r.category} [${r.department}]`).map(([k, v]) => ({ group: k, lines: v })),
+    perf: { total_ms: Math.round(elapsed), ms_per_line: +(elapsed / N).toFixed(3) },
+  }
+
+  /* ------------------------------ print -------------------------------- */
+  const R = report
+  console.log(`=== ${R.ontology_version} over ${N} lines — ${R.source.sheet} / ${R.source.item_column} ===\n`)
+  console.log('THREE NUMBERS TOGETHER (none of them means anything alone)')
+  console.log(`  poolable coverage   ${String(R.poolable).padStart(6)}  ${R.poolable_pct}%`)
+  console.log(`  confident-wrong     ${String(R.confident_wrong.total).padStart(6)}  ${R.confident_wrong.rate_of_all}% of all · ${R.confident_wrong.rate_of_poolable}% of poolable`)
+  console.log(`  C rate              ${String(R.levels.C).padStart(6)}  ${R.levels.C_pct}%`)
+  console.log('')
+  console.log(`A  exact intent       ${String(R.levels.A).padStart(6)}  ${R.levels.A_pct}%`)
+  console.log(`B  local resolved     ${String(R.levels.B).padStart(6)}  ${R.levels.B_pct}%`)
+  console.log(`C  unresolved         ${String(R.levels.C).padStart(6)}  ${R.levels.C_pct}%`)
+  console.log(`   local usable A+B   ${String(R.levels.local_usable).padStart(6)}  ${R.levels.local_usable_pct}%`)
+  console.log(`   semantic recovery  ${String(R.semantic_recovery).padStart(6)}  ${R.semantic_recovery_pct}%   (B, but NOT poolable)`)
+  console.log(`   AI eligible        ${String(R.ai_eligible).padStart(6)}  ${R.ai_eligible_pct}%`)
+  console.log(`   rejected           ${String(R.rejected).padStart(6)}`)
+  console.log(`   unique pools       ${String(R.unique_pools).padStart(6)}`)
+  console.log(`   resolve            ${R.perf.ms_per_line} ms/line (${R.perf.total_ms} ms total)`)
+
+  console.log(`\n=== CONFIDENT-WRONG, split by level ===`)
+  console.log(`A: ${R.confident_wrong.A} of ${R.levels.A} Level A (${R.confident_wrong.A_rate_of_A}%)`)
+  console.log(`B: ${R.confident_wrong.B} of ${R.levels.B} Level B (${R.confident_wrong.B_rate_of_B}%)`)
+  console.log('\nlines  A   B   error family                                        resolved to')
+  for (const r of R.confident_wrong.by_rule) {
+    console.log(String(r.lines).padStart(5), String(r.A).padStart(3), String(r.B).padStart(3), ` ${r.en}`.padEnd(52), r.resolved_to ?? '— FIXED —')
+  }
+  console.log(`\nunnamed cross-trade mismatches: ${R.confident_wrong.unnamed_cross_trade}`)
+  for (const g of R.confident_wrong.unnamed_cross_trade_groups.slice(0, VERBOSE ? 99 : 12)) {
+    console.log(`  ${String(g.lines).padStart(5)}  ${g.group}`)
+  }
+  const gt = R.confident_wrong.intent_ground_truth
+  console.log(`\nwrong intent inside the right family: ${R.confident_wrong.wrong_intent_same_family} of ${gt.lines_checked} lines checked against ${gt.categories_checked} categories`)
+  for (const g of R.confident_wrong.wrong_intent_groups.slice(0, VERBOSE ? 99 : 20)) {
+    console.log(`  ${String(g.lines).padStart(5)}  ${g.group}`)
+  }
+
+  console.log(`\n=== BY DEPARTMENT ===`)
+  console.log('department'.padEnd(22), 'lines'.padStart(6), 'A'.padStart(6), 'B'.padStart(6), 'C'.padStart(6), 'pool%'.padStart(7), 'wrong'.padStart(6))
+  for (const d of R.by_department) {
+    console.log(d.department.padEnd(22), String(d.lines).padStart(6), String(d.A).padStart(6), String(d.B).padStart(6), String(d.C).padStart(6), String(d.poolable_pct).padStart(7), String(d.confident_wrong).padStart(6))
+  }
+
+  console.log(`\n=== INTENT COVERAGE (${R.intent_coverage.length} intents used) ===`)
+  for (const i of R.intent_coverage) console.log(String(i.lines).padStart(6), i.intent)
+
+  if (VERBOSE) {
+    console.log(`\n=== FAMILY COVERAGE ===`)
+    for (const f of R.family_coverage) console.log(String(f.lines).padStart(6), f.family)
+    console.log(`\n=== NON-POOLABLE CATEGORIES (${R.gap_categories.length}) ===`)
+    for (const g of R.gap_categories) console.log(String(g.lines).padStart(6), g.group)
+  }
+} finally {
+  await server.close()
+}
+
+if (JSON_OUT) {
+  writeFileSync(JSON_OUT, `${JSON.stringify(report, null, 2)}\n`)
+  console.log(`\nwrote ${JSON_OUT}`)
+}
