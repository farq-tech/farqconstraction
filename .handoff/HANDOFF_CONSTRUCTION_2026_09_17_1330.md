# تسليم — فرق للبناء — 2026-09-17 — 13:30

يصف هذا المستند **الحالة المقيسة** لا المرجوّة. كل رقم يحمل وقته ومصدره (واجهة / خادم / قاعدة). **أي رقم بلا وقت غير موثوق.** ويحلّ محلّ تسليم 13:00 فيما يتعارضان.

---

## 0. حقائق اليوم التي تغيّر الحساب — اقرأها قبل أي شيء

| الحقيقة | الدليل | ما يترتّب |
|---|---|---|
| **`api.farq.sa` تخدمه خدمة Railway اسمها `farq-api-test-oregon`** | قائمة النطاقات في Railway 13:25: النطاق المخصّص `api.farq.sa` معلّق على هذه الخدمة وحدها، وDNS: `api.farq.sa → 1uh3z0xb.up.railway.app` | الاسم مضلّل. **هذه هي خدمة الإنتاج المشتركة**، لا خادم اختبار. |
| **الرابط المستقل ينادي `https://api.farq.sa` مباشرة** | حزمة `farq-construction.vercel.app` تحمل 4 إشارات إلى `api.farq.sa` | أي نشر لكود القراءة يمرّ عبر الخادم المشترك. **يحتاج إذن المالك الصريح.** لا يوجد «خادم آخر» يُنشر عليه بلا أثر. |
| **الإنتاج على `cpo-v2` لا `v6`** | تقرير وكيل الإيداع: بصمة الحمولة على `main` = `539dfd4b5ffbf59b`. تأكيد مستقل: وحدات المنفذ الثلاث (`procurement-ontology-port.js`، `canonical-intent-contract.js`، `supplier-eligibility-gate.js`) **غائبة عن `origin/main`** | الفجوة ثمانية أجيال. كل تقدير نشر بُني اليوم على «أربعة» خاطئ. |
| **لا كود منشور يقرأ الخريطة إطلاقًا** | على `origin/main` الإشارتان الوحيدتان إلى `construction.intent_supplier_map` هما الهجرة وتراجعها؛ صفر أسطر تطبيقية (`git grep` 13:20) | `procurement_intent` فارغ لأن الجدول **لا يُستعلَم**، لا لأنه فارغ. و`map_refused` لن يظهر لأن الرفض يحتاج منفذًا يعمل. |
| **الخريطة موجودة في قاعدة الإنتاج** | 257,952 صفًّا، بصمة `96046b41d2b96b40`، كلها نشطة، بُنيت 08:18 (تقرير الوكيل الأول) | المنفذ في `44d8acc97` يتوقّع هذه البصمة بالحرف. الناقص **وحدة القراءة** فقط. |
| **`v10` يُستورد بلا تعطّل** ويحسم «درابزين حديدي» إلى `handrail_balustrade` بثقة 0.90، وكذلك جيرانه («درابزين حديد»، «دربزين حديدي»، handrail…) | استيراد حقيقي في عملية جديدة (وكيل الأنطولوجيا) | **تحفّظ مهم:** الحسم شرط لازم لا كافٍ. الخادم يرفض تأكيد المطابقة عند غياب المورد (القسم 4.4)، فقد تبقى «مادة غير محدّدة» بعد نشر v10 إن لم يوجد مورّد. لا يُقال «السبب نشر v10 فقط» حتى يُقاس. |
| **عطل `v11` كان وهمًا** | الثابت المسبّب لا يظهر في أي التزام؛ كان حالة عابرة في شجرة عمل. عملها محفوظ على `wip/ontology-cpo-v11-parked` | القرار «v10 هو ما يُنشر» يبقى، لكن طريق v11 غير مسدود. |
| **`supplier_mapping_hash` لم يُتحقق منه من الحمولة** | يُبنى من مصنوعات جهة الموردين ولا يُحسب من حمولة الطلب | مُسلَّم من مسار الخريطة لا مؤكَّد من مسار الأنطولوجيا. |
| **مشروع Supabase «farq-main» ليس الإنتاج** | طلب واحد وحدث واحد فيه؛ الإنتاج على `CONSTRUCTION_DB_URL` (`db-target.js`) | لا يُقاس شيء للبناء من Supabase. |

---

## 1. أين يعمل التطبيق

- الرابط المستقل: `https://farq-construction.vercel.app` → ينادي `https://api.farq.sa` (خدمة `farq-api-test-oregon`، بيئة `production`، مشروع Railway «Farq»).
- فرع الواجهة: `release/standalone-construction`، شجرة العمل `/Volumes/Extreme SSD/cunstraction/.release-wt`.
- الـAPI: `origin/main` = `a666cebb3`. فرع النشر المرشّح `release/cpo-v10-api` = `44d8acc97`. شجرة v10 كاملة كالتزام على `wip/ontology-cpo-v10-and-reports-2026-09-17` وعلى origin بنفس البصمتين — **لا حاجة لإعادة بناء ولا رقعة.**

---

## 2. قرارات المالك — نافذة

1. `farq.sa/construction` مسار قديم لا يُلمس.
2. **الخادم المشترك لا يُعدَّل بلا إذن صريح.** والحقيقة الجديدة: لا يوجد خادم غيره يخدم الرابط. فنشر وحدات القراءة **قرار المالك**، لا قرار وكيل.
3. `v10` هو ما يُنشر؛ `v11` موقوف (وطريقه غير مسدود).
4. دليل الفئة لا يُرقّى إلى دليل بند.
5. الدقة قبل التغطية.
6. «صفر موردين» مسموح؛ `ZERO_BECAUSE_MAPPING_BROKEN` ممنوع.
7. **جديد:** كل وكيل يُعطى سؤالًا واحدًا وأمرًا بالتوقف بعده، ويقول «لم أستطع لأن كذا» بدل الصمت.

---

## 3. مودَع اليوم (13:00–13:30)

| المكان | الالتزام | ما فيه |
|---|---|---|
| واجهة، `release/standalone-construction` | `e9f093b` | فيكسچر مرجعي مستقل لكراسة مركز البيانات: 180 صفًّا بـPyMuPDF، لا فجوات. `.handoff/DATACENTER_GROUNDTRUTH_FIXTURE_2026_09_17.md` |
| واجهة | `bf5fb97` | تبويب السجل: تسميات عربية، شارة «مسجّل/مشتق»، عدّادات الإرسال، ونصّ فراغ صادق |
| واجهة | `554ae4b` | `.handoff/AUDIT_TIMELINE_DERIVED_2026_09_17.md` |
| API، فرع `fix/rfq-audit-timeline-2026-09-17` (شجرة `/Users/m4pro/farq-wt-audit-timeline`، أساسه `release/cpo-v10-api`) | `cdcf7f9cf` | **`audit_timeline` يُشتقّ من `dispatch_attempts` بدل `[]`** — 3 أسطر في `supabase-repository.js` + وحدة جديدة + 10 اختبارات. غير مدفوع، غير منشور. |

### لماذا كان `audit_timeline` فارغًا (مقيس 13:10)
الجدول الزمني يقرأ `procurement_audit_events` فقط، ومسار الإرسال لا يكتب فيه شيئًا. الطلبات التسعة كلها `[]` بما فيها طلب بـ162 دعوة و97 إرسالًا ناجحًا. الإصلاح يشتقّ الأحداث عند القراءة بلا كتابة، وكل حدث يحمل `source: RECORDED | DERIVED`. إعادة العرض على التسعة: المسودّات الأربع «أُنشئ» فقط وصفر أحداث إرسال؛ الخمسة المرسلة حدث إرسال واحد بعدّاداته. **بعد النشر يصبح «لم يُرسل شيء» مُثبَتًا من الجدول الزمني نفسه** (البوابة B).

---

## 4. الأرقام المقيسة

### الكراسة المرجعية (68 بندًا) على الرابط، 12:40 — من الواجهة
68/68 مقروءة بالكميات. 39 بندًا لها مورد مؤهل، 53 ترشيحًا. 29 «مادة غير محدّدة». المواصفات 66/68.

### كراسة مركز البيانات (180 بندًا، 802 كيلوبايت) على الرابط، 13:01 — من الواجهة مقابل المرجع المستقل
180/180 مقروءة في 12.5 ث. الكميات 180/180. الوحدات 164/180 والـ16 المتبقية «قطعة→عدد» تطبيع مقصود (`parseBoq.ts:54`). **151/180 (84%) فقدت اسم البند**: 120 تعرض عنوان الفئة، 150 تعرض شذرات المواصفة. الحسم 0/180، الموردون 0. **ليست رقم حسم للمحرّك.** كل كراسات PDF المتاحة ملوّثة، ولا توجد كراسة نظيفة.

### الدقة والدليل (من التسليم السابق، لم يتغيّر)
ملاءمة المادة 92.6%؛ **67.8% عُرضت بلا دليل**. 945/44,354 مورّدًا بدليل مقبول. `supplier_item_capabilities` يغطي 226 مورّدًا و1,432 بندًا.

---

## 5. الأعطال المفتوحة — مختصرة بالإشارة إلى تسليم 13:00 للأدلة

- **4.1** عدّاد الأهلية يقرأ `supplier_item_capabilities` فقط وهو فارغ للصحّي → نشر الخريطة **لا يحرّك صفر المراحيض**.
- **4.2** البوابة على البركة الكاملة قبل الترتيب = 112؛ على الخمسة = 0. الترتيب عطل مستقل.
- **4.3** `ITEM_NAME_SPECIALTY_MAP`: `tiles` فوق النمط الصحي الوحيد ولا كلمة «مرحاض». افحص بقية العائلات.
- **4.4** الخادم يدمج «لا مورد» مع «غير معروف». **يبقى العطل الذي يقرّر ماذا يعني «مادة غير محدّدة» بعد نشر v10.**
- **4.5** CORS يحجب `x-construction-demo-user` خارج الجلسة — إصلاحه في الخادم المشترك.
- **4.7** الخريطة لا تُستشار: **السبب المؤكَّد الآن غياب كود القراءة عن main**، لا الرفض الصامت.
- **4.8** القارئ يفقد عمود الوصف بصمت على تخطيط غير «اعتماد» (84%) وكل المؤشرات خضراء. **الحارس يُقاس على الكراستين وألّا يشتعل على المرجعية.** الفيكسچر جاهز (`e9f093b`).
- **جديد 4.10** اسم الخدمة `farq-api-test-oregon` يوحي باختبار وهي الإنتاج. أي حساب خطر بُني على الاسم باطل.

---

## 6. الخطوات التالية — بالترتيب، سؤال واحد لكل وكيل

0. **قرار المالك:** هل يُنشر `release/cpo-v10-api` (+ إصلاح الجدول الزمني) على `api.farq.sa` وهو الخادم المشترك؟ لا نشر قبل الجواب. إن نُشر: تحقّق بعد النشر بثلاثة أرقام بوقتها: `procurement_intent` غير فارغ، `map_refused` = 0، و«درابزين حديدي» على الرابط.
1. حارس فقدان عمود الوصف (مأخوذ) — يُقاس على الكراستين.
2. إيداع بوابة الأهلية ووصلها قبل الترتيب على البركة الكاملة (مأخوذ) — اذكر تكلفة الزمن بالرقم.
3. فصل «معروف بلا مورد» عن «غير معروف» (مأخوذ) — واقرأ العدد الحقيقي من الـ29 **بعد** نشر v10 لا قبله.
4. `ITEM_NAME_SPECIALTY_MAP` (مأخوذ).
5. اقتراح تعبئة القدرات الصحية بلا تنفيذ (مأخوذ).
6. اختبار استيراد (مأخوذ؛ خفّت أهميته بعد ثبوت أن عطل v11 وهم، لكنه رخيص).
7. **متاح:** CORS — يحتاج إذن المالك. المسترجع الهجين — مؤجَّل.

---

## 7. معيار النجاح
لا يُقال «تم» لأن Vercel أو Railway قال Ready. **أفتح الرابط → أرفع الكراسة → تظهر البنود بأسمائها → أفتح بندًا → أرى موردين → نشاطهم يناسب المادة.** وكل رقم يحمل وقته ومصدره.

## 8. قاعدتا اليوم
- لا تعرض رقمًا لا تستطيع أن تسمّي مصدره، ولا تجعل الفشل يشبه النجاح.
- **لا تحكم من الاسم بل من التوجيه والإعدادات.** خدمة اسمها test تخدم `api.farq.sa`.
