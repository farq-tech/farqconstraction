# Farq Construction — public Vite env
# Copy to `.env.local` for local overrides.

# Construction API (defaults to production Farq Construction API)
VITE_FARQ_API_BASE_URL=https://api.farq.sa

# Supabase farq-main (auth + construction schema)
VITE_SUPABASE_URL=https://mpgbvtaguerncgbzvpwg.supabase.co
VITE_SUPABASE_ANON_KEY=

# Explicit opt-in only. Never enable in production deploys.
VITE_ALLOW_PROTOTYPE_CHROME=false
